import { n as e, r as t } from "./rolldown-runtime-DArdT4gl.mjs";
import { $ as n, $a as r, $c as i, $i as a, $n as o, $o as s, $r as c, $s as l, $t as u, A as d, Aa as f, Ac as ee, Ai as p, An as m, Ao as h, Ar as te, As as ne, At as re, B as ie, Ba as ae, Bc as oe, Bi as se, Bn as ce, Bo as le, Br as ue, Bs as de, Bt as fe, C as pe, Ca as me, Cc as he, Ci as ge, Cn as _e, Co as ve, Cr as ye, Cs as be, Ct as xe, D as Se, Da as Ce, Dc as we, Di as Te, Dn as Ee, Do as De, Dr as Oe, Ds as ke, Dt as Ae, E as je, Ea as Me, Ec as Ne, Ei as Pe, En as Fe, Eo as Ie, Er as Le, Es as Re, Et as g, F as ze, Fa as Be, Fc as Ve, Fi as He, Fn as _, Fo as Ue, Fr as We, Fs as Ge, Ft as Ke, G as qe, Ga as Je, Gc as Ye, Gi as Xe, Gn as Ze, Go as Qe, Gr as $e, Gs as et, Gt as tt, H as nt, Ha as rt, Hc as it, Hi as at, Hn as ot, Ho as st, Hr as ct, Hs as lt, Ht as ut, I as dt, Ia as ft, Ic as pt, Ii as mt, In as v, Io as ht, Ir as gt, Is as _t, It as vt, J as yt, Ja as bt, Jc as xt, Ji as St, Jn as y, Jo as Ct, Jr as wt, Js as Tt, Jt as Et, K as Dt, Ka as Ot, Kc as kt, Ki as At, Kn as jt, Ko as Mt, Kr as Nt, Ks as Pt, Kt as Ft, L as It, La as Lt, Lc as Rt, Li as zt, Ln as Bt, Lo as Vt, Lr as Ht, Ls as Ut, Lt as b, M as Wt, Ma as Gt, Mc as Kt, Mi as qt, Mn as Jt, Mo as Yt, Mr as Xt, Ms as Zt, Mt as Qt, N as $t, Na as en, Nc as tn, Ni as nn, Nn as rn, No as an, Nr as on, Ns as sn, Nt as x, O as cn, Oa as ln, Oc as un, Oi as dn, On as fn, Oo as pn, Or as mn, Os as hn, Ot as gn, P as _n, Pa as vn, Pc as yn, Pi as bn, Pn as S, Po as xn, Pr as Sn, Ps as Cn, Pt as wn, Q as Tn, Qa as En, Qc as Dn, Qi as On, Qn as kn, Qo as An, Qr as jn, Qs as Mn, Qt as Nn, R as Pn, Ra as Fn, Rc as In, Ri as Ln, Rn, Ro as zn, Rr as Bn, Rs as Vn, Rt as Hn, S as Un, Sa as Wn, Sc as Gn, Si as Kn, Sn as C, So as qn, Sr as Jn, Ss as Yn, St as Xn, T as Zn, Ta as Qn, Tc as $n, Ti as er, Tn as tr, To as nr, Tr as rr, Ts as ir, Tt as ar, U as or, Ua as sr, Uc as cr, Ui as lr, Un as w, Uo as ur, Ur as dr, Us as fr, Ut as pr, V as mr, Va as hr, Vc as gr, Vi as _r, Vn as vr, Vo as yr, Vr as br, Vs as xr, Vt as Sr, W as Cr, Wa as wr, Wc as Tr, Wi as Er, Wn as Dr, Wo as Or, Wr as kr, Ws as Ar, Wt as jr, X as Mr, Xa as Nr, Xc as Pr, Xi as Fr, Xn as Ir, Xo as Lr, Xr as Rr, Xs as zr, Xt as Br, Y as Vr, Ya as Hr, Yc as Ur, Yi as Wr, Yn as Gr, Yo as Kr, Yr as qr, Ys as Jr, Yt as Yr, Z as Xr, Za as Zr, Zc as Qr, Zi as $r, Zn as ei, Zo as ti, Zr as ni, Zs as ri, Zt as ii, _ as ai, _a as oi, _c as si, _i as ci, _n as li, _o as ui, _r as di, _s as fi, _t as pi, a as mi, aa as hi, ac as gi, ai as _i, al as T, an as vi, ao as yi, ar as bi, as as xi, at as Si, b as Ci, ba as wi, bc as Ti, bi as Ei, bn as Di, bo as Oi, br as ki, bs as Ai, bt as ji, c as Mi, ca as Ni, cc as Pi, ci as Fi, cl as Ii, cn as Li, co as Ri, cr as zi, cs as Bi, ct as Vi, d as Hi, da as Ui, dc as Wi, di as Gi, dl as Ki, dn as qi, do as Ji, dr as Yi, ds as Xi, dt as Zi, ea as Qi, ec as $i, ei as ea, el as ta, en as na, eo as ra, er as ia, es as aa, et as oa, f as sa, fa as ca, fc as la, fi as ua, fl as E, fn as da, fo as fa, fr as pa, fs as ma, ft as ha, g as ga, ga as _a, gc as va, gi as ya, gl as ba, gn as xa, go as Sa, gr as Ca, gs as wa, gt as Ta, h as Ea, ha as Da, hc as Oa, hi as ka, hl as Aa, hn as ja, ho as Ma, hr as Na, hs as Pa, ht as Fa, i as Ia, ia as La, ic as Ra, ii as za, il as Ba, in as Va, io as Ha, ir as Ua, is as Wa, it as Ga, j as Ka, ja as qa, jc as Ja, ji as Ya, jn as Xa, jo as Za, jr as Qa, js as $a, jt as eo, k as to, ka as no, kc as ro, ki as io, kn as ao, ko as oo, kr as so, ks as co, kt as lo, l as uo, la as fo, lc as po, li as mo, ll as ho, ln as go, lo as _o, lr as vo, ls as yo, lt as bo, m as xo, ma as So, mc as Co, mi as wo, ml as D, mn as To, mo as Eo, mr as Do, ms as Oo, mt as ko, n as Ao, na as jo, nc as Mo, ni as No, nl as Po, nn as Fo, no as Io, nr as Lo, ns as Ro, nt as zo, o as Bo, oa as Vo, oc as Ho, oi as Uo, ol as Wo, on as Go, oo as Ko, or as O, os as qo, ot as Jo, p as Yo, pa as Xo, pc as Zo, pi as Qo, pl as $o, pn as es, po as ts, pr as ns, ps as rs, pt as is, q as as, qa as os, qc as ss, qi as cs, qn as ls, qo as us, qr as ds, qs as fs, qt as ps, r as ms, ra as hs, rc as gs, ri as _s, rn as vs, ro as ys, rr as bs, rs as xs, rt as Ss, s as Cs, sa as ws, sc as Ts, si as Es, sl as k, sn as Ds, so as Os, sr as A, ss as ks, st as As, t as js, ta as Ms, tc as Ns, ti as Ps, tl as Fs, tn as Is, to as Ls, tr as Rs, ts as zs, tt as Bs, u as Vs, ua as Hs, uc as Us, ui as Ws, ul as j, un as Gs, uo as Ks, ur as qs, us as Js, ut as Ys, v as Xs, va as Zs, vc as Qs, vi as $s, vn as ec, vo as tc, vr as nc, vs as rc, vt as ic, w as ac, wa as oc, wc as sc, wi as cc, wn as lc, wo as uc, wr as dc, ws as fc, wt as pc, x as mc, xa as hc, xc as gc, xi as _c, xn as M, xo as vc, xr as yc, xs as bc, xt as xc, y as Sc, ya as Cc, yc as wc, yi as Tc, yn as Ec, yo as Dc, yr as Oc, ys as kc, yt as Ac, z as jc, za as Mc, zc as Nc, zi as Pc, zn as Fc, zo as Ic, zr as Lc, zs as Rc, zt as zc } from "./schemas-PhCwx9tg.mjs";
import { t as N } from "./defineProperty-DxjBOsvz.mjs";
//#region node_modules/zod/v4/locales/ar.js
var Bc = () => {
	let e = {
		string: {
			unit: "حرف",
			verb: "أن يحوي"
		},
		file: {
			unit: "بايت",
			verb: "أن يحوي"
		},
		array: {
			unit: "عنصر",
			verb: "أن يحوي"
		},
		set: {
			unit: "عنصر",
			verb: "أن يحوي"
		},
		map: {
			unit: "عنصر",
			verb: "أن يحوي"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "مدخل",
		email: "بريد إلكتروني",
		url: "رابط",
		emoji: "إيموجي",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "تاريخ ووقت بمعيار ISO",
		date: "تاريخ بمعيار ISO",
		time: "وقت بمعيار ISO",
		duration: "مدة بمعيار ISO",
		ipv4: "عنوان IPv4",
		ipv6: "عنوان IPv6",
		mac: "عنوان MAC",
		cidrv4: "مدى عناوين بصيغة IPv4",
		cidrv6: "مدى عناوين بصيغة IPv6",
		base64: "نَص بترميز base64-encoded",
		base64url: "نَص بترميز base64url-encoded",
		json_string: "نَص على هيئة JSON",
		e164: "رقم هاتف بمعيار E.164",
		credit_card: "رقم بطاقة الائتمان",
		currency_code: "رمز العملة",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "مدخل"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `مدخلات غير مقبولة: يفترض إدخال instanceof ${e.expected}، ولكن تم إدخال ${i}` : `مدخلات غير مقبولة: يفترض إدخال ${t}، ولكن تم إدخال ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `مدخلات غير مقبولة: يفترض إدخال ${D(e.values[0])}` : `اختيار غير مقبول: يتوقع انتقاء أحد هذه الخيارات: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? ` أكبر من اللازم: يفترض أن تكون ${e.origin ?? "القيمة"} ${n} ${e.maximum.toString()} ${r.unit ?? "عنصر"}` : `أكبر من اللازم: يفترض أن تكون ${e.origin ?? "القيمة"} ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `أصغر من اللازم: يفترض لـ ${e.origin} أن يكون ${n} ${e.minimum.toString()} ${r.unit}` : `أصغر من اللازم: يفترض لـ ${e.origin} أن يكون ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `نَص غير مقبول: يجب أن يبدأ بـ "${e.prefix}"` : t.format === "ends_with" ? `نَص غير مقبول: يجب أن ينتهي بـ "${t.suffix}"` : t.format === "includes" ? `نَص غير مقبول: يجب أن يتضمَّن "${t.includes}"` : t.format === "regex" ? `نَص غير مقبول: يجب أن يطابق النمط ${t.pattern}` : `${n[t.format] ?? e.format} غير مقبول`;
			}
			case "not_multiple_of": return `رقم غير مقبول: يجب أن يكون من مضاعفات ${e.divisor}`;
			case "unrecognized_keys": return `معرف${e.keys.length > 1 ? "ات" : ""} غريب${e.keys.length > 1 ? "ة" : ""}: ${j(e.keys, "، ")}`;
			case "invalid_key": return `معرف غير مقبول في ${e.origin}`;
			case "invalid_union": return "مدخل غير مقبول";
			case "invalid_element": return `مدخل غير مقبول في ${e.origin}`;
			default: return "مدخل غير مقبول";
		}
	};
};
function Vc() {
	return { localeError: Bc() };
}
//#endregion
//#region node_modules/zod/v4/locales/az.js
var Hc = () => {
	let e = {
		string: {
			unit: "simvol",
			verb: "olmalıdır"
		},
		file: {
			unit: "bayt",
			verb: "olmalıdır"
		},
		array: {
			unit: "element",
			verb: "olmalıdır"
		},
		set: {
			unit: "element",
			verb: "olmalıdır"
		},
		map: {
			unit: "element",
			verb: "olmalıdır"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "email address",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datetime",
		date: "ISO date",
		time: "ISO time",
		duration: "ISO duration",
		ipv4: "IPv4 address",
		ipv6: "IPv6 address",
		mac: "MAC address",
		cidrv4: "IPv4 range",
		cidrv6: "IPv6 range",
		base64: "base64-encoded string",
		base64url: "base64url-encoded string",
		json_string: "JSON string",
		e164: "E.164 number",
		credit_card: "kredit kartı nömrəsi",
		currency_code: "valyuta kodu",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Yanlış dəyər: gözlənilən instanceof ${e.expected}, daxil olan ${i}` : `Yanlış dəyər: gözlənilən ${t}, daxil olan ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Yanlış dəyər: gözlənilən ${D(e.values[0])}` : `Yanlış seçim: aşağıdakılardan biri olmalıdır: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Çox böyük: gözlənilən ${e.origin ?? "dəyər"} ${n}${e.maximum.toString()} ${r.unit ?? "element"}` : `Çox böyük: gözlənilən ${e.origin ?? "dəyər"} ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Çox kiçik: gözlənilən ${e.origin} ${n}${e.minimum.toString()} ${r.unit}` : `Çox kiçik: gözlənilən ${e.origin} ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Yanlış mətn: "${t.prefix}" ilə başlamalıdır` : t.format === "ends_with" ? `Yanlış mətn: "${t.suffix}" ilə bitməlidir` : t.format === "includes" ? `Yanlış mətn: "${t.includes}" daxil olmalıdır` : t.format === "regex" ? `Yanlış mətn: ${t.pattern} şablonuna uyğun olmalıdır` : `Yanlış ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Yanlış ədəd: ${e.divisor} ilə bölünə bilən olmalıdır`;
			case "unrecognized_keys": return `Tanınmayan açar${e.keys.length > 1 ? "lar" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} daxilində yanlış açar`;
			case "invalid_union": return "Yanlış dəyər";
			case "invalid_element": return `${e.origin} daxilində yanlış dəyər`;
			default: return "Yanlış dəyər";
		}
	};
};
function Uc() {
	return { localeError: Hc() };
}
//#endregion
//#region node_modules/zod/v4/locales/be.js
function Wc(e, t, n, r) {
	let i = Math.abs(e), a = i % 10, o = i % 100;
	return o >= 11 && o <= 19 ? r : a === 1 ? t : a >= 2 && a <= 4 ? n : r;
}
var Gc = () => {
	let e = {
		string: {
			unit: {
				one: "сімвал",
				few: "сімвалы",
				many: "сімвалаў"
			},
			verb: "мець"
		},
		array: {
			unit: {
				one: "элемент",
				few: "элементы",
				many: "элементаў"
			},
			verb: "мець"
		},
		set: {
			unit: {
				one: "элемент",
				few: "элементы",
				many: "элементаў"
			},
			verb: "мець"
		},
		map: {
			unit: {
				one: "элемент",
				few: "элементы",
				many: "элементаў"
			},
			verb: "мець"
		},
		file: {
			unit: {
				one: "байт",
				few: "байты",
				many: "байтаў"
			},
			verb: "мець"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "увод",
		email: "email адрас",
		url: "URL",
		emoji: "эмодзі",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO дата і час",
		date: "ISO дата",
		time: "ISO час",
		duration: "ISO працягласць",
		ipv4: "IPv4 адрас",
		ipv6: "IPv6 адрас",
		mac: "MAC адрас",
		cidrv4: "IPv4 дыяпазон",
		cidrv6: "IPv6 дыяпазон",
		base64: "радок у фармаце base64",
		base64url: "радок у фармаце base64url",
		json_string: "JSON радок",
		e164: "нумар E.164",
		credit_card: "нумар крэдытнай карты",
		currency_code: "код валюты",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "увод"
	}, r = {
		nan: "NaN",
		number: "лік",
		array: "масіў"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Няправільны ўвод: чакаўся instanceof ${e.expected}, атрымана ${i}` : `Няправільны ўвод: чакаўся ${t}, атрымана ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Няправільны ўвод: чакалася ${D(e.values[0])}` : `Няправільны варыянт: чакаўся адзін з ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				if (r) {
					let t = Wc(Number(e.maximum), r.unit.one, r.unit.few, r.unit.many);
					return `Занадта вялікі: чакалася, што ${e.origin ?? "значэнне"} павінна ${r.verb} ${n}${e.maximum.toString()} ${t}`;
				}
				return `Занадта вялікі: чакалася, што ${e.origin ?? "значэнне"} павінна быць ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				if (r) {
					let t = Wc(Number(e.minimum), r.unit.one, r.unit.few, r.unit.many);
					return `Занадта малы: чакалася, што ${e.origin} павінна ${r.verb} ${n}${e.minimum.toString()} ${t}`;
				}
				return `Занадта малы: чакалася, што ${e.origin} павінна быць ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Няправільны радок: павінен пачынацца з "${t.prefix}"` : t.format === "ends_with" ? `Няправільны радок: павінен заканчвацца на "${t.suffix}"` : t.format === "includes" ? `Няправільны радок: павінен змяшчаць "${t.includes}"` : t.format === "regex" ? `Няправільны радок: павінен адпавядаць шаблону ${t.pattern}` : `Няправільны ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Няправільны лік: павінен быць кратным ${e.divisor}`;
			case "unrecognized_keys": return `Нераспазнаны ${e.keys.length > 1 ? "ключы" : "ключ"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Няправільны ключ у ${e.origin}`;
			case "invalid_union": return "Няправільны ўвод";
			case "invalid_element": return `Няправільнае значэнне ў ${e.origin}`;
			default: return "Няправільны ўвод";
		}
	};
};
function Kc() {
	return { localeError: Gc() };
}
//#endregion
//#region node_modules/zod/v4/locales/bg.js
var qc = () => {
	let e = {
		string: {
			unit: "символа",
			verb: "да съдържа"
		},
		file: {
			unit: "байта",
			verb: "да съдържа"
		},
		array: {
			unit: "елемента",
			verb: "да съдържа"
		},
		set: {
			unit: "елемента",
			verb: "да съдържа"
		},
		map: {
			unit: "елемента",
			verb: "да съдържа"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "вход",
		email: "имейл адрес",
		url: "URL",
		emoji: "емоджи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO време",
		date: "ISO дата",
		time: "ISO време",
		duration: "ISO продължителност",
		ipv4: "IPv4 адрес",
		ipv6: "IPv6 адрес",
		mac: "MAC адрес",
		cidrv4: "IPv4 диапазон",
		cidrv6: "IPv6 диапазон",
		base64: "base64-кодиран низ",
		base64url: "base64url-кодиран низ",
		json_string: "JSON низ",
		e164: "E.164 номер",
		credit_card: "номер на кредитна карта",
		currency_code: "код на валута",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "вход"
	}, r = {
		nan: "NaN",
		number: "число",
		array: "масив"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Невалиден вход: очакван instanceof ${e.expected}, получен ${i}` : `Невалиден вход: очакван ${t}, получен ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Невалиден вход: очакван ${D(e.values[0])}` : `Невалидна опция: очаквано едно от ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Твърде голямо: очаква се ${e.origin ?? "стойност"} да съдържа ${n}${e.maximum.toString()} ${r.unit ?? "елемента"}` : `Твърде голямо: очаква се ${e.origin ?? "стойност"} да бъде ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Твърде малко: очаква се ${e.origin} да съдържа ${n}${e.minimum.toString()} ${r.unit}` : `Твърде малко: очаква се ${e.origin} да бъде ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				if (t.format === "starts_with") return `Невалиден низ: трябва да започва с "${t.prefix}"`;
				if (t.format === "ends_with") return `Невалиден низ: трябва да завършва с "${t.suffix}"`;
				if (t.format === "includes") return `Невалиден низ: трябва да включва "${t.includes}"`;
				if (t.format === "regex") return `Невалиден низ: трябва да съвпада с ${t.pattern}`;
				let r = "Невалиден";
				return t.format === "emoji" && (r = "Невалидно"), t.format === "datetime" && (r = "Невалидно"), t.format === "date" && (r = "Невалидна"), t.format === "time" && (r = "Невалидно"), t.format === "duration" && (r = "Невалидна"), `${r} ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Невалидно число: трябва да бъде кратно на ${e.divisor}`;
			case "unrecognized_keys": return `Неразпознат${e.keys.length > 1 ? "и" : ""} ключ${e.keys.length > 1 ? "ове" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Невалиден ключ в ${e.origin}`;
			case "invalid_union": return "Невалиден вход";
			case "invalid_element": return `Невалидна стойност в ${e.origin}`;
			default: return "Невалиден вход";
		}
	};
};
function Jc() {
	return { localeError: qc() };
}
//#endregion
//#region node_modules/zod/v4/locales/bn.js
var Yc = () => {
	let e = {
		string: {
			unit: "অক্ষর",
			verb: "থাকতে হবে"
		},
		file: {
			unit: "বাইট",
			verb: "থাকতে হবে"
		},
		array: {
			unit: "আইটেম",
			verb: "থাকতে হবে"
		},
		set: {
			unit: "আইটেম",
			verb: "থাকতে হবে"
		},
		map: {
			unit: "এন্ট্রি",
			verb: "থাকতে হবে"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ইনপুট",
		email: "ইমেইল ঠিকানা",
		url: "URL",
		emoji: "ইমোজি",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO তারিখ ও সময়",
		date: "ISO তারিখ",
		time: "ISO সময়",
		duration: "ISO সময়কাল",
		ipv4: "IPv4 ঠিকানা",
		ipv6: "IPv6 ঠিকানা",
		mac: "MAC ঠিকানা",
		cidrv4: "IPv4 রেঞ্জ",
		cidrv6: "IPv6 রেঞ্জ",
		base64: "base64-এনকোডেড স্ট্রিং",
		base64url: "base64url-এনকোডেড স্ট্রিং",
		json_string: "JSON স্ট্রিং",
		e164: "E.164 নম্বর",
		credit_card: "ক্রেডিট কার্ড নম্বর",
		currency_code: "মুদ্রা কোড",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ইনপুট"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `অবৈধ ইনপুট: প্রত্যাশিত ${t}, প্রাপ্ত ${r[n] ?? n}`;
			}
			case "invalid_value": return e.values.length === 1 ? `অবৈধ ইনপুট: প্রত্যাশিত ${D(e.values[0])}` : `অবৈধ অপশন: ${j(e.values, " | ")} এর মধ্যে একটি প্রত্যাশিত`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `অনেক বড়: ${e.origin ?? "মান"} ${n}${e.maximum.toString()} ${r.unit ?? "এলিমেন্ট"} হতে হবে` : `অনেক বড়: ${e.origin ?? "মান"} ${n}${e.maximum.toString()} হতে হবে`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `অনেক ছোট: ${e.origin} ${n}${e.minimum.toString()} ${r.unit} হতে হবে` : `অনেক ছোট: ${e.origin} ${n}${e.minimum.toString()} হতে হবে`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `অবৈধ স্ট্রিং: "${t.prefix}" দিয়ে শুরু হতে হবে` : t.format === "ends_with" ? `অবৈধ স্ট্রিং: "${t.suffix}" দিয়ে শেষ হতে হবে` : t.format === "includes" ? `অবৈধ স্ট্রিং: "${t.includes}" অন্তর্ভুক্ত থাকতে হবে` : t.format === "regex" ? `অবৈধ স্ট্রিং: ${t.pattern} প্যাটার্ন মিলতে হবে` : `অবৈধ ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `অবৈধ নম্বর: ${e.divisor} এর গুণিতক হতে হবে`;
			case "unrecognized_keys": return `অচেনা কী${e.keys.length > 1 ? "গুলো" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} এ অবৈধ কী`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `অবৈধ ডিসক্রিমিনেটর মান। প্রত্যাশিত ${e.options.map((e) => `'${e}'`).join(" | ")}` : "অবৈধ ইনপুট";
			case "invalid_element": return `${e.origin} এ অবৈধ মান`;
			default: return "অবৈধ ইনপুট";
		}
	};
};
function Xc() {
	return { localeError: Yc() };
}
//#endregion
//#region node_modules/zod/v4/locales/ca.js
var Zc = () => {
	let e = {
		string: {
			unit: "caràcters",
			verb: "contenir"
		},
		file: {
			unit: "bytes",
			verb: "contenir"
		},
		array: {
			unit: "elements",
			verb: "contenir"
		},
		set: {
			unit: "elements",
			verb: "contenir"
		},
		map: {
			unit: "elements",
			verb: "contenir"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "entrada",
		email: "adreça electrònica",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data i hora ISO",
		date: "data ISO",
		time: "hora ISO",
		duration: "durada ISO",
		ipv4: "adreça IPv4",
		ipv6: "adreça IPv6",
		mac: "adreça MAC",
		cidrv4: "rang IPv4",
		cidrv6: "rang IPv6",
		base64: "cadena codificada en base64",
		base64url: "cadena codificada en base64url",
		json_string: "cadena JSON",
		e164: "número E.164",
		credit_card: "número de targeta de crèdit",
		currency_code: "codi de moneda",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "entrada"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Tipus invàlid: s'esperava instanceof ${e.expected}, s'ha rebut ${i}` : `Tipus invàlid: s'esperava ${t}, s'ha rebut ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Valor invàlid: s'esperava ${D(e.values[0])}` : `Opció invàlida: s'esperava una de ${j(e.values, " o ")}`;
			case "too_big": {
				let n = e.inclusive ? "com a màxim" : "menys de", r = t(e.origin);
				return r ? `Massa gran: s'esperava que ${e.origin ?? "el valor"} contingués ${n} ${e.maximum.toString()} ${r.unit ?? "elements"}` : `Massa gran: s'esperava que ${e.origin ?? "el valor"} fos ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? "com a mínim" : "més de", r = t(e.origin);
				return r ? `Massa petit: s'esperava que ${e.origin} contingués ${n} ${e.minimum.toString()} ${r.unit}` : `Massa petit: s'esperava que ${e.origin} fos ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Format invàlid: ha de començar amb "${t.prefix}"` : t.format === "ends_with" ? `Format invàlid: ha d'acabar amb "${t.suffix}"` : t.format === "includes" ? `Format invàlid: ha d'incloure "${t.includes}"` : t.format === "regex" ? `Format invàlid: ha de coincidir amb el patró ${t.pattern}` : `Format invàlid per a ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Número invàlid: ha de ser múltiple de ${e.divisor}`;
			case "unrecognized_keys": return `Clau${e.keys.length > 1 ? "s" : ""} no reconeguda${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Clau invàlida a ${e.origin}`;
			case "invalid_union": return "Entrada invàlida";
			case "invalid_element": return `Element invàlid a ${e.origin}`;
			default: return "Entrada invàlida";
		}
	};
};
function Qc() {
	return { localeError: Zc() };
}
//#endregion
//#region node_modules/zod/v4/locales/ckb.js
var $c = () => {
	let e = {
		string: {
			unit: "پیت",
			verb: "بێت"
		},
		file: {
			unit: "بایت",
			verb: "بێت"
		},
		array: {
			unit: "دانە",
			verb: "بێت"
		},
		set: {
			unit: "دانە",
			verb: "بێت"
		},
		map: {
			unit: "دانە",
			verb: "بێت"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "regex",
		email: "ئیمەیڵ",
		url: "بەستەر (URL)",
		emoji: "ئیمۆجی",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ڕێکەوت و کات",
		date: "ڕێکەوت",
		time: "کات",
		duration: "ماوە",
		ipv4: "ناونیشانی IPv4",
		ipv6: "ناونیشانی IPv6",
		mac: "ناونیشانی MAC",
		cidrv4: "مەودای IPv4",
		cidrv6: "مەودای IPv6",
		base64: "دەقی base64",
		base64url: "دەقی base64url",
		json_string: "دەقی JSON",
		e164: "ژمارەی E.164",
		credit_card: "ژمارەی کارتی کرێدیت",
		currency_code: "کۆدی دراو",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "تێکردە"
	}, r = {
		nan: "NaN",
		string: "نووسین",
		number: "ژمارە",
		boolean: "boolean",
		array: "array",
		object: "object",
		date: "ڕێکەوت",
		integer: "ژمارە",
		float: "ژمارە",
		null: "null",
		undefined: "undefined",
		function: "function",
		symbol: "symbol",
		unknown: "unknown",
		promise: "promise",
		void: "void",
		never: "never",
		map: "map",
		set: "set"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n, a = [
					"ا",
					"و",
					"ۆ",
					"وو",
					"ە",
					"ی",
					"ێ"
				].some((e) => i.endsWith(e)) ? "یە" : "ە", o = /^[a-zA-Z]+$/.test(i);
				return n === "null" || n === "undefined" ? "داواکراوە" : `چاوەڕوانکراوە ${t} بێت، بەڵام ${i}${o ? "" : a}`;
			}
			case "invalid_value": return e.values.length === 1 ? `بەهاکە نادرووستە: چاوەڕوانکراوە ${D(e.values[0])} بێت` : `هەڵبژاردەی نادروست: چاوەڕوانکراوە یەکێک بێت لە ${j(e.values, "|")}`;
			case "too_big": {
				let n = t(e.origin);
				return n ? `پێویستە بە لایەنی زۆرەوە ${e.maximum.toString()} ${n.unit} ${n.verb}` : `پێویستە بە لایەنی زۆرەوە ${e.maximum.toString()} بێت`;
			}
			case "too_small": {
				let n = t(e.origin);
				return n ? `پێویستە بە لایەنی کەمەوە ${e.minimum.toString()} ${n.unit} ${n.verb}` : `پێویستە بە لایەنی کەمەوە ${e.minimum.toString()} بێت`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `دەقی نادروست: پێویستە دەستپێبکات بە "${t.prefix}"` : t.format === "ends_with" ? `دەقی نادروست: پێویستە کۆتاییبێت بە "${t.suffix}"` : t.format === "includes" ? `دەقی نادروست: پێویستە "${t.includes}" لەخۆبگرێت` : t.format === "regex" ? `دەقی نادروست: پێویستە لەگەڵ پاتێرنی ${t.pattern} بگونجێت` : `بەهای ${n[t.format] ?? e.format} نادروستە`;
			}
			case "not_multiple_of": return `ژمارەی نادروست: دەبێت چەند هێندە بێت بۆ ${e.divisor}`;
			case "unrecognized_keys": return `کلیلی نەناسراو: ${j(e.keys, ", ")}`;
			case "invalid_key": return `کلیلی نادروست لە ${e.origin}`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `بەهای نەناسراو هەیە. بەهای چاوەڕوانکراو: ${e.options.map((e) => `'${e}'`).join(" | ")}` : "یەکگرتنی نادروست";
			case "invalid_element": return `${e.origin} بەهاکە نادروستە`;
			default: return "تێکردەی نادروست";
		}
	};
};
function el() {
	return { localeError: $c() };
}
//#endregion
//#region node_modules/zod/v4/locales/cs.js
var tl = () => {
	let e = {
		string: {
			unit: "znaků",
			verb: "mít"
		},
		file: {
			unit: "bajtů",
			verb: "mít"
		},
		array: {
			unit: "prvků",
			verb: "mít"
		},
		set: {
			unit: "prvků",
			verb: "mít"
		},
		map: {
			unit: "prvků",
			verb: "mít"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "regulární výraz",
		email: "e-mailová adresa",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "datum a čas ve formátu ISO",
		date: "datum ve formátu ISO",
		time: "čas ve formátu ISO",
		duration: "doba trvání ISO",
		ipv4: "IPv4 adresa",
		ipv6: "IPv6 adresa",
		mac: "MAC adresa",
		cidrv4: "rozsah IPv4",
		cidrv6: "rozsah IPv6",
		base64: "řetězec zakódovaný ve formátu base64",
		base64url: "řetězec zakódovaný ve formátu base64url",
		json_string: "řetězec ve formátu JSON",
		e164: "číslo E.164",
		credit_card: "číslo kreditní karty",
		currency_code: "kód měny",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "vstup"
	}, r = {
		nan: "NaN",
		number: "číslo",
		string: "řetězec",
		function: "funkce",
		array: "pole"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Neplatný vstup: očekáváno instanceof ${e.expected}, obdrženo ${i}` : `Neplatný vstup: očekáváno ${t}, obdrženo ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Neplatný vstup: očekáváno ${D(e.values[0])}` : `Neplatná možnost: očekávána jedna z hodnot ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Hodnota je příliš velká: ${e.origin ?? "hodnota"} musí mít ${n}${e.maximum.toString()} ${r.unit ?? "prvků"}` : `Hodnota je příliš velká: ${e.origin ?? "hodnota"} musí být ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Hodnota je příliš malá: ${e.origin ?? "hodnota"} musí mít ${n}${e.minimum.toString()} ${r.unit ?? "prvků"}` : `Hodnota je příliš malá: ${e.origin ?? "hodnota"} musí být ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Neplatný řetězec: musí začínat na "${t.prefix}"` : t.format === "ends_with" ? `Neplatný řetězec: musí končit na "${t.suffix}"` : t.format === "includes" ? `Neplatný řetězec: musí obsahovat "${t.includes}"` : t.format === "regex" ? `Neplatný řetězec: musí odpovídat vzoru ${t.pattern}` : `Neplatný formát ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Neplatné číslo: musí být násobkem ${e.divisor}`;
			case "unrecognized_keys": return `Neznámé klíče: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Neplatný klíč v ${e.origin}`;
			case "invalid_union": return "Neplatný vstup";
			case "invalid_element": return `Neplatná hodnota v ${e.origin}`;
			default: return "Neplatný vstup";
		}
	};
};
function nl() {
	return { localeError: tl() };
}
//#endregion
//#region node_modules/zod/v4/locales/da.js
var rl = () => {
	let e = {
		string: {
			unit: "tegn",
			verb: "havde"
		},
		file: {
			unit: "bytes",
			verb: "havde"
		},
		array: {
			unit: "elementer",
			verb: "indeholdt"
		},
		set: {
			unit: "elementer",
			verb: "indeholdt"
		},
		map: {
			unit: "elementer",
			verb: "indeholdt"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "e-mailadresse",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dato- og klokkeslæt",
		date: "ISO-dato",
		time: "ISO-klokkeslæt",
		duration: "ISO-varighed",
		ipv4: "IPv4-adresse",
		ipv6: "IPv6-adresse",
		mac: "MAC-adresse",
		cidrv4: "IPv4-spektrum",
		cidrv6: "IPv6-spektrum",
		base64: "base64-kodet streng",
		base64url: "base64url-kodet streng",
		json_string: "JSON-streng",
		e164: "E.164-nummer",
		credit_card: "kreditkortnummer",
		currency_code: "valutakode",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = {
		nan: "NaN",
		string: "streng",
		number: "tal",
		boolean: "boolean",
		array: "liste",
		object: "objekt",
		set: "sæt",
		file: "fil"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ugyldigt input: forventede instanceof ${e.expected}, fik ${i}` : `Ugyldigt input: forventede ${t}, fik ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ugyldig værdi: forventede ${D(e.values[0])}` : `Ugyldigt valg: forventede en af følgende ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", i = t(e.origin), a = r[e.origin] ?? e.origin;
				return i ? `For stor: forventede ${a ?? "value"} ${i.verb} ${n} ${e.maximum.toString()} ${i.unit ?? "elementer"}` : `For stor: forventede ${a ?? "value"} havde ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", i = t(e.origin), a = r[e.origin] ?? e.origin;
				return i ? `For lille: forventede ${a} ${i.verb} ${n} ${e.minimum.toString()} ${i.unit}` : `For lille: forventede ${a} havde ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ugyldig streng: skal starte med "${t.prefix}"` : t.format === "ends_with" ? `Ugyldig streng: skal ende med "${t.suffix}"` : t.format === "includes" ? `Ugyldig streng: skal indeholde "${t.includes}"` : t.format === "regex" ? `Ugyldig streng: skal matche mønsteret ${t.pattern}` : `Ugyldig ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Ugyldigt tal: skal være deleligt med ${e.divisor}`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Ukendte nøgler" : "Ukendt nøgle"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Ugyldig nøgle i ${e.origin}`;
			case "invalid_union": return "Ugyldigt input: matcher ingen af de tilladte typer";
			case "invalid_element": return `Ugyldig værdi i ${e.origin}`;
			default: return "Ugyldigt input";
		}
	};
};
function il() {
	return { localeError: rl() };
}
//#endregion
//#region node_modules/zod/v4/locales/de.js
var al = () => {
	let e = {
		string: {
			unit: "Zeichen",
			verb: "zu haben"
		},
		file: {
			unit: "Bytes",
			verb: "zu haben"
		},
		array: {
			unit: "Elemente",
			verb: "zu haben"
		},
		set: {
			unit: "Elemente",
			verb: "zu haben"
		},
		map: {
			unit: "Elemente",
			verb: "zu haben"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "Eingabe",
		email: "E-Mail-Adresse",
		url: "URL",
		emoji: "Emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-Datum und -Uhrzeit",
		date: "ISO-Datum",
		time: "ISO-Uhrzeit",
		duration: "ISO-Dauer",
		ipv4: "IPv4-Adresse",
		ipv6: "IPv6-Adresse",
		mac: "MAC-Adresse",
		cidrv4: "IPv4-Bereich",
		cidrv6: "IPv6-Bereich",
		base64: "Base64-codierter String",
		base64url: "Base64-URL-codierter String",
		json_string: "JSON-String",
		e164: "E.164-Nummer",
		credit_card: "Kreditkartennummer",
		currency_code: "Währungscode",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "Eingabe"
	}, r = {
		nan: "NaN",
		number: "Zahl",
		array: "Array"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ungültige Eingabe: erwartet instanceof ${e.expected}, erhalten ${i}` : `Ungültige Eingabe: erwartet ${t}, erhalten ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ungültige Eingabe: erwartet ${D(e.values[0])}` : `Ungültige Option: erwartet eine von ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Zu groß: erwartet, dass ${e.origin ?? "Wert"} ${n}${e.maximum.toString()} ${r.unit ?? "Elemente"} hat` : `Zu groß: erwartet, dass ${e.origin ?? "Wert"} ${n}${e.maximum.toString()} ist`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Zu klein: erwartet, dass ${e.origin} ${n}${e.minimum.toString()} ${r.unit} hat` : `Zu klein: erwartet, dass ${e.origin} ${n}${e.minimum.toString()} ist`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ungültiger String: muss mit "${t.prefix}" beginnen` : t.format === "ends_with" ? `Ungültiger String: muss mit "${t.suffix}" enden` : t.format === "includes" ? `Ungültiger String: muss "${t.includes}" enthalten` : t.format === "regex" ? `Ungültiger String: muss dem Muster ${t.pattern} entsprechen` : `Ungültig: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Ungültige Zahl: muss ein Vielfaches von ${e.divisor} sein`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Unbekannte Schlüssel" : "Unbekannter Schlüssel"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Ungültiger Schlüssel in ${e.origin}`;
			case "invalid_union": return "Ungültige Eingabe";
			case "invalid_element": return `Ungültiger Wert in ${e.origin}`;
			default: return "Ungültige Eingabe";
		}
	};
};
function ol() {
	return { localeError: al() };
}
//#endregion
//#region node_modules/zod/v4/locales/el.js
var sl = () => {
	let e = {
		string: {
			unit: "χαρακτήρες",
			verb: "να έχει"
		},
		file: {
			unit: "bytes",
			verb: "να έχει"
		},
		array: {
			unit: "στοιχεία",
			verb: "να έχει"
		},
		set: {
			unit: "στοιχεία",
			verb: "να έχει"
		},
		map: {
			unit: "καταχωρήσεις",
			verb: "να έχει"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "είσοδος",
		email: "διεύθυνση email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO ημερομηνία και ώρα",
		date: "ISO ημερομηνία",
		time: "ISO ώρα",
		duration: "ISO διάρκεια",
		ipv4: "διεύθυνση IPv4",
		ipv6: "διεύθυνση IPv6",
		mac: "διεύθυνση MAC",
		cidrv4: "εύρος IPv4",
		cidrv6: "εύρος IPv6",
		base64: "συμβολοσειρά κωδικοποιημένη σε base64",
		base64url: "συμβολοσειρά κωδικοποιημένη σε base64url",
		json_string: "συμβολοσειρά JSON",
		e164: "αριθμός E.164",
		credit_card: "αριθμός πιστωτικής κάρτας",
		currency_code: "κωδικός νομίσματος",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "είσοδος"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return typeof e.expected == "string" && /^[A-Z]/.test(e.expected) ? `Μη έγκυρη είσοδος: αναμενόταν instanceof ${e.expected}, λήφθηκε ${i}` : `Μη έγκυρη είσοδος: αναμενόταν ${t}, λήφθηκε ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Μη έγκυρη είσοδος: αναμενόταν ${D(e.values[0])}` : `Μη έγκυρη επιλογή: αναμενόταν ένα από ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Πολύ μεγάλο: αναμενόταν ${e.origin ?? "τιμή"} να έχει ${n}${e.maximum.toString()} ${r.unit ?? "στοιχεία"}` : `Πολύ μεγάλο: αναμενόταν ${e.origin ?? "τιμή"} να είναι ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Πολύ μικρό: αναμενόταν ${e.origin} να έχει ${n}${e.minimum.toString()} ${r.unit}` : `Πολύ μικρό: αναμενόταν ${e.origin} να είναι ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Μη έγκυρη συμβολοσειρά: πρέπει να ξεκινά με "${t.prefix}"` : t.format === "ends_with" ? `Μη έγκυρη συμβολοσειρά: πρέπει να τελειώνει με "${t.suffix}"` : t.format === "includes" ? `Μη έγκυρη συμβολοσειρά: πρέπει να περιέχει "${t.includes}"` : t.format === "regex" ? `Μη έγκυρη συμβολοσειρά: πρέπει να ταιριάζει με το μοτίβο ${t.pattern}` : `Μη έγκυρο: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Μη έγκυρος αριθμός: πρέπει να είναι πολλαπλάσιο του ${e.divisor}`;
			case "unrecognized_keys": return `Άγνωστ${e.keys.length > 1 ? "α" : "ο"} κλειδ${e.keys.length > 1 ? "ιά" : "ί"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Μη έγκυρο κλειδί στο ${e.origin}`;
			case "invalid_union": return "Μη έγκυρη είσοδος";
			case "invalid_element": return `Μη έγκυρη τιμή στο ${e.origin}`;
			default: return "Μη έγκυρη είσοδος";
		}
	};
};
function cl() {
	return { localeError: sl() };
}
//#endregion
//#region node_modules/zod/v4/locales/eo.js
var ll = () => {
	let e = {
		string: {
			unit: "karaktrojn",
			verb: "havi"
		},
		file: {
			unit: "bajtojn",
			verb: "havi"
		},
		array: {
			unit: "elementojn",
			verb: "havi"
		},
		set: {
			unit: "elementojn",
			verb: "havi"
		},
		map: {
			unit: "elementojn",
			verb: "havi"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "enigo",
		email: "retadreso",
		url: "URL",
		emoji: "emoĝio",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-datotempo",
		date: "ISO-dato",
		time: "ISO-tempo",
		duration: "ISO-daŭro",
		ipv4: "IPv4-adreso",
		ipv6: "IPv6-adreso",
		mac: "MAC-adreso",
		cidrv4: "IPv4-rango",
		cidrv6: "IPv6-rango",
		base64: "64-ume kodita karaktraro",
		base64url: "URL-64-ume kodita karaktraro",
		json_string: "JSON-karaktraro",
		e164: "E.164-nombro",
		credit_card: "kreditkarta numero",
		currency_code: "valuta kodo",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "enigo"
	}, r = {
		nan: "NaN",
		number: "nombro",
		array: "tabelo",
		null: "senvalora"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Nevalida enigo: atendiĝis instanceof ${e.expected}, riceviĝis ${i}` : `Nevalida enigo: atendiĝis ${t}, riceviĝis ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Nevalida enigo: atendiĝis ${D(e.values[0])}` : `Nevalida opcio: atendiĝis unu el ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Tro granda: atendiĝis ke ${e.origin ?? "valoro"} havu ${n}${e.maximum.toString()} ${r.unit ?? "elementojn"}` : `Tro granda: atendiĝis ke ${e.origin ?? "valoro"} havu ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Tro malgranda: atendiĝis ke ${e.origin} havu ${n}${e.minimum.toString()} ${r.unit}` : `Tro malgranda: atendiĝis ke ${e.origin} estu ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Nevalida karaktraro: devas komenciĝi per "${t.prefix}"` : t.format === "ends_with" ? `Nevalida karaktraro: devas finiĝi per "${t.suffix}"` : t.format === "includes" ? `Nevalida karaktraro: devas inkluzivi "${t.includes}"` : t.format === "regex" ? `Nevalida karaktraro: devas kongrui kun la modelo ${t.pattern}` : `Nevalida ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Nevalida nombro: devas esti oblo de ${e.divisor}`;
			case "unrecognized_keys": return `Nekonata${e.keys.length > 1 ? "j" : ""} ŝlosilo${e.keys.length > 1 ? "j" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Nevalida ŝlosilo en ${e.origin}`;
			case "invalid_union": return "Nevalida enigo";
			case "invalid_element": return `Nevalida valoro en ${e.origin}`;
			default: return "Nevalida enigo";
		}
	};
};
function ul() {
	return { localeError: ll() };
}
//#endregion
//#region node_modules/zod/v4/locales/es.js
var dl = () => {
	let e = {
		string: {
			unit: "caracteres",
			verb: "tener"
		},
		file: {
			unit: "bytes",
			verb: "tener"
		},
		array: {
			unit: "elementos",
			verb: "tener"
		},
		set: {
			unit: "elementos",
			verb: "tener"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "entrada",
		email: "dirección de correo electrónico",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "fecha y hora ISO",
		date: "fecha ISO",
		time: "hora ISO",
		duration: "duración ISO",
		ipv4: "dirección IPv4",
		ipv6: "dirección IPv6",
		mac: "dirección MAC",
		cidrv4: "rango IPv4",
		cidrv6: "rango IPv6",
		base64: "cadena codificada en base64",
		base64url: "URL codificada en base64",
		json_string: "cadena JSON",
		e164: "número E.164",
		credit_card: "número de tarjeta de crédito",
		currency_code: "código de moneda",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "entrada"
	}, r = {
		nan: "NaN",
		string: "texto",
		number: "número",
		boolean: "booleano",
		array: "arreglo",
		object: "objeto",
		set: "conjunto",
		file: "archivo",
		date: "fecha",
		bigint: "número grande",
		symbol: "símbolo",
		undefined: "indefinido",
		null: "nulo",
		function: "función",
		map: "mapa",
		record: "registro",
		tuple: "tupla",
		enum: "enumeración",
		union: "unión",
		literal: "literal",
		promise: "promesa",
		void: "vacío",
		never: "nunca",
		unknown: "desconocido",
		any: "cualquiera"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Entrada inválida: se esperaba instanceof ${e.expected}, recibido ${i}` : `Entrada inválida: se esperaba ${t}, recibido ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Entrada inválida: se esperaba ${D(e.values[0])}` : `Opción inválida: se esperaba una de ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", i = t(e.origin), a = r[e.origin] ?? e.origin;
				return i ? `Demasiado grande: se esperaba que ${a ?? "valor"} tuviera ${n}${e.maximum.toString()} ${i.unit ?? "elementos"}` : `Demasiado grande: se esperaba que ${a ?? "valor"} fuera ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", i = t(e.origin), a = r[e.origin] ?? e.origin;
				return i ? `Demasiado pequeño: se esperaba que ${a} tuviera ${n}${e.minimum.toString()} ${i.unit}` : `Demasiado pequeño: se esperaba que ${a} fuera ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Cadena inválida: debe comenzar con "${t.prefix}"` : t.format === "ends_with" ? `Cadena inválida: debe terminar en "${t.suffix}"` : t.format === "includes" ? `Cadena inválida: debe incluir "${t.includes}"` : t.format === "regex" ? `Cadena inválida: debe coincidir con el patrón ${t.pattern}` : `Inválido ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Número inválido: debe ser múltiplo de ${e.divisor}`;
			case "unrecognized_keys": return `Llave${e.keys.length > 1 ? "s" : ""} desconocida${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Llave inválida en ${r[e.origin] ?? e.origin}`;
			case "invalid_union": return "Entrada inválida";
			case "invalid_element": return `Valor inválido en ${r[e.origin] ?? e.origin}`;
			default: return "Entrada inválida";
		}
	};
};
function fl() {
	return { localeError: dl() };
}
//#endregion
//#region node_modules/zod/v4/locales/fa.js
var pl = () => {
	let e = {
		string: {
			unit: "کاراکتر",
			verb: "داشته باشد"
		},
		file: {
			unit: "بایت",
			verb: "داشته باشد"
		},
		array: {
			unit: "آیتم",
			verb: "داشته باشد"
		},
		set: {
			unit: "آیتم",
			verb: "داشته باشد"
		},
		map: {
			unit: "آیتم",
			verb: "داشته باشد"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ورودی",
		email: "آدرس ایمیل",
		url: "URL",
		emoji: "ایموجی",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "تاریخ و زمان ایزو",
		date: "تاریخ ایزو",
		time: "زمان ایزو",
		duration: "مدت زمان ایزو",
		ipv4: "IPv4 آدرس",
		ipv6: "IPv6 آدرس",
		mac: "MAC آدرس",
		cidrv4: "IPv4 دامنه",
		cidrv6: "IPv6 دامنه",
		base64: "base64-encoded رشته",
		base64url: "base64url-encoded رشته",
		json_string: "JSON رشته",
		e164: "E.164 عدد",
		credit_card: "شماره کارت اعتباری",
		currency_code: "کد ارز",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ورودی"
	}, r = {
		nan: "NaN",
		number: "عدد",
		array: "آرایه"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `ورودی نامعتبر: می‌بایست instanceof ${e.expected} می‌بود، ${i} دریافت شد` : `ورودی نامعتبر: می‌بایست ${t} می‌بود، ${i} دریافت شد`;
			}
			case "invalid_value": return e.values.length === 1 ? `ورودی نامعتبر: می‌بایست ${D(e.values[0])} می‌بود` : `گزینه نامعتبر: می‌بایست یکی از ${j(e.values, "|")} می‌بود`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `خیلی بزرگ: ${e.origin ?? "مقدار"} باید ${n}${e.maximum.toString()} ${r.unit ?? "عنصر"} باشد` : `خیلی بزرگ: ${e.origin ?? "مقدار"} باید ${n}${e.maximum.toString()} باشد`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `خیلی کوچک: ${e.origin} باید ${n}${e.minimum.toString()} ${r.unit} باشد` : `خیلی کوچک: ${e.origin} باید ${n}${e.minimum.toString()} باشد`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `رشته نامعتبر: باید با "${t.prefix}" شروع شود` : t.format === "ends_with" ? `رشته نامعتبر: باید با "${t.suffix}" تمام شود` : t.format === "includes" ? `رشته نامعتبر: باید شامل "${t.includes}" باشد` : t.format === "regex" ? `رشته نامعتبر: باید با الگوی ${t.pattern} مطابقت داشته باشد` : `${n[t.format] ?? e.format} نامعتبر`;
			}
			case "not_multiple_of": return `عدد نامعتبر: باید مضرب ${e.divisor} باشد`;
			case "unrecognized_keys": return `کلید${e.keys.length > 1 ? "های" : ""} ناشناس: ${j(e.keys, ", ")}`;
			case "invalid_key": return `کلید ناشناس در ${e.origin}`;
			case "invalid_union": return "ورودی نامعتبر";
			case "invalid_element": return `مقدار نامعتبر در ${e.origin}`;
			default: return "ورودی نامعتبر";
		}
	};
};
function ml() {
	return { localeError: pl() };
}
//#endregion
//#region node_modules/zod/v4/locales/fi.js
var hl = () => {
	let e = {
		string: {
			unit: "merkkiä",
			subject: "merkkijonon"
		},
		file: {
			unit: "tavua",
			subject: "tiedoston"
		},
		array: {
			unit: "alkiota",
			subject: "listan"
		},
		set: {
			unit: "alkiota",
			subject: "joukon"
		},
		map: {
			unit: "alkiota",
			subject: "kuvauksen"
		},
		number: {
			unit: "",
			subject: "luvun"
		},
		bigint: {
			unit: "",
			subject: "suuren kokonaisluvun"
		},
		int: {
			unit: "",
			subject: "kokonaisluvun"
		},
		date: {
			unit: "",
			subject: "päivämäärän"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "säännöllinen lauseke",
		email: "sähköpostiosoite",
		url: "URL-osoite",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-aikaleima",
		date: "ISO-päivämäärä",
		time: "ISO-aika",
		duration: "ISO-kesto",
		ipv4: "IPv4-osoite",
		ipv6: "IPv6-osoite",
		mac: "MAC-osoite",
		cidrv4: "IPv4-alue",
		cidrv6: "IPv6-alue",
		base64: "base64-koodattu merkkijono",
		base64url: "base64url-koodattu merkkijono",
		json_string: "JSON-merkkijono",
		e164: "E.164-luku",
		credit_card: "luottokortin numero",
		currency_code: "valuuttakoodi",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "templaattimerkkijono"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Virheellinen tyyppi: odotettiin instanceof ${e.expected}, oli ${i}` : `Virheellinen tyyppi: odotettiin ${t}, oli ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Virheellinen syöte: täytyy olla ${D(e.values[0])}` : `Virheellinen valinta: täytyy olla yksi seuraavista: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Liian suuri: ${r.subject} täytyy olla ${n}${e.maximum.toString()} ${r.unit}`.trim() : `Liian suuri: arvon täytyy olla ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Liian pieni: ${r.subject} täytyy olla ${n}${e.minimum.toString()} ${r.unit}`.trim() : `Liian pieni: arvon täytyy olla ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Virheellinen syöte: täytyy alkaa "${t.prefix}"` : t.format === "ends_with" ? `Virheellinen syöte: täytyy loppua "${t.suffix}"` : t.format === "includes" ? `Virheellinen syöte: täytyy sisältää "${t.includes}"` : t.format === "regex" ? `Virheellinen syöte: täytyy vastata säännöllistä lauseketta ${t.pattern}` : `Virheellinen ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Virheellinen luku: täytyy olla luvun ${e.divisor} monikerta`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Tuntemattomat avaimet" : "Tuntematon avain"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return "Virheellinen avain tietueessa";
			case "invalid_union": return "Virheellinen unioni";
			case "invalid_element": return "Virheellinen arvo joukossa";
			default: return "Virheellinen syöte";
		}
	};
};
function gl() {
	return { localeError: hl() };
}
//#endregion
//#region node_modules/zod/v4/locales/fr.js
var _l = () => {
	let e = {
		string: {
			unit: "caractères",
			verb: "avoir"
		},
		file: {
			unit: "octets",
			verb: "avoir"
		},
		array: {
			unit: "éléments",
			verb: "avoir"
		},
		set: {
			unit: "éléments",
			verb: "avoir"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "expression régulière",
		email: "adresse e-mail",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "date et heure ISO",
		date: "date ISO",
		time: "heure ISO",
		duration: "durée ISO",
		ipv4: "adresse IPv4",
		ipv6: "adresse IPv6",
		mac: "adresse MAC",
		cidrv4: "plage IPv4",
		cidrv6: "plage IPv6",
		base64: "chaîne de caractères encodée en base64",
		base64url: "chaîne de caractères encodée en base64url",
		json_string: "chaîne de caractères JSON",
		e164: "numéro au format E.164",
		credit_card: "numéro de carte de crédit",
		currency_code: "code de devise",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "entrée"
	}, r = {
		string: "chaîne de caractères",
		number: "nombre",
		int: "entier",
		boolean: "booléen",
		bigint: "grand entier",
		symbol: "symbole",
		undefined: "indéfini",
		null: "null",
		never: "jamais",
		void: "vide",
		date: "date",
		array: "tableau",
		object: "objet",
		tuple: "tuple",
		record: "record",
		map: "map",
		set: "ensemble",
		file: "fichier",
		nonoptional: "non optionnel",
		nan: "NaN",
		function: "fonction"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Entrée invalide : instance de ${e.expected} attendu, ${i} reçu` : `Entrée invalide : ${t} attendu, ${i} reçu`;
			}
			case "invalid_value": return e.values.length === 1 ? `Entrée invalide : ${D(e.values[0])} attendu` : `Option invalide : une valeur parmi ${j(e.values, "|")} attendue`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", i = t(e.origin);
				return i ? `Trop grand : ${r[e.origin] ?? "valeur"} doit ${i.verb} ${n}${e.maximum.toString()} ${i.unit ?? "élément(s)"}` : `Trop grand : ${r[e.origin] ?? "valeur"} doit être ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", i = t(e.origin);
				return i ? `Trop petit : ${r[e.origin] ?? "valeur"} doit ${i.verb} ${n}${e.minimum.toString()} ${i.unit}` : `Trop petit : ${r[e.origin] ?? "valeur"} doit être ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Chaîne de caractères invalide : doit commencer par "${t.prefix}"` : t.format === "ends_with" ? `Chaîne de caractères invalide : doit se terminer par "${t.suffix}"` : t.format === "includes" ? `Chaîne de caractères invalide : doit inclure "${t.includes}"` : t.format === "regex" ? `Chaîne de caractères invalide : doit correspondre au motif ${t.pattern}` : `${n[t.format] ?? e.format} invalide`;
			}
			case "not_multiple_of": return `Nombre invalide : doit être un multiple de ${e.divisor}`;
			case "unrecognized_keys": return `Clé${e.keys.length > 1 ? "s" : ""} non reconnue${e.keys.length > 1 ? "s" : ""} : ${j(e.keys, ", ")}`;
			case "invalid_key": return `Clé invalide dans ${e.origin}`;
			case "invalid_union": return "Entrée invalide";
			case "invalid_element": return `Valeur invalide dans ${e.origin}`;
			default: return "Entrée invalide";
		}
	};
};
function vl() {
	return { localeError: _l() };
}
//#endregion
//#region node_modules/zod/v4/locales/fr-CA.js
var yl = () => {
	let e = {
		string: {
			unit: "caractères",
			verb: "avoir"
		},
		file: {
			unit: "octets",
			verb: "avoir"
		},
		array: {
			unit: "éléments",
			verb: "avoir"
		},
		set: {
			unit: "éléments",
			verb: "avoir"
		},
		map: {
			unit: "éléments",
			verb: "avoir"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "entrée",
		email: "adresse courriel",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "date-heure ISO",
		date: "date ISO",
		time: "heure ISO",
		duration: "durée ISO",
		ipv4: "adresse IPv4",
		ipv6: "adresse IPv6",
		mac: "adresse MAC",
		cidrv4: "plage IPv4",
		cidrv6: "plage IPv6",
		base64: "chaîne encodée en base64",
		base64url: "chaîne encodée en base64url",
		json_string: "chaîne JSON",
		e164: "numéro E.164",
		credit_card: "numéro de carte de crédit",
		currency_code: "code de devise",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "entrée"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Entrée invalide : attendu instanceof ${e.expected}, reçu ${i}` : `Entrée invalide : attendu ${t}, reçu ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Entrée invalide : attendu ${D(e.values[0])}` : `Option invalide : attendu l'une des valeurs suivantes ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "≤" : "<", r = t(e.origin);
				return r ? `Trop grand : attendu que ${e.origin ?? "la valeur"} ait ${n}${e.maximum.toString()} ${r.unit}` : `Trop grand : attendu que ${e.origin ?? "la valeur"} soit ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? "≥" : ">", r = t(e.origin);
				return r ? `Trop petit : attendu que ${e.origin} ait ${n}${e.minimum.toString()} ${r.unit}` : `Trop petit : attendu que ${e.origin} soit ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Chaîne invalide : doit commencer par "${t.prefix}"` : t.format === "ends_with" ? `Chaîne invalide : doit se terminer par "${t.suffix}"` : t.format === "includes" ? `Chaîne invalide : doit inclure "${t.includes}"` : t.format === "regex" ? `Chaîne invalide : doit correspondre au motif ${t.pattern}` : `${n[t.format] ?? e.format} invalide`;
			}
			case "not_multiple_of": return `Nombre invalide : doit être un multiple de ${e.divisor}`;
			case "unrecognized_keys": return `Clé${e.keys.length > 1 ? "s" : ""} non reconnue${e.keys.length > 1 ? "s" : ""} : ${j(e.keys, ", ")}`;
			case "invalid_key": return `Clé invalide dans ${e.origin}`;
			case "invalid_union": return "Entrée invalide";
			case "invalid_element": return `Valeur invalide dans ${e.origin}`;
			default: return "Entrée invalide";
		}
	};
};
function bl() {
	return { localeError: yl() };
}
//#endregion
//#region node_modules/zod/v4/locales/gu.js
var xl = () => {
	let e = {
		string: {
			unit: "અક્ષર",
			verb: "હોવા જોઈએ"
		},
		file: {
			unit: "બાયટ",
			verb: "હોવા જોઈએ"
		},
		array: {
			unit: "આઇટમ",
			verb: "હોવા જોઈએ"
		},
		set: {
			unit: "આઇટમ",
			verb: "હોવા જોઈએ"
		},
		map: {
			unit: "એન્ટ્રી",
			verb: "હોવા જોઈએ"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ઇનપુટ",
		email: "ઈમેઇલ એડ્રેસ",
		url: "URL",
		emoji: "ઇમોજી",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO તારીખ અને સમય",
		date: "ISO તારીખ",
		time: "ISO સમય",
		duration: "ISO અવધિ",
		ipv4: "IPv4 એડ્રેસ",
		ipv6: "IPv6 એડ્રેસ",
		mac: "MAC એડ્રેસ",
		cidrv4: "IPv4 શ્રેણી",
		cidrv6: "IPv6 શ્રેણી",
		base64: "base64-એન્કોડેડ સ્ટ્રિંગ",
		base64url: "base64url-એન્કોડેડ સ્ટ્રિંગ",
		json_string: "JSON સ્ટ્રિંગ",
		e164: "E.164 નંબર",
		credit_card: "ક્રેડિટ કાર્ડ નંબર",
		currency_code: "ચલણ કોડ",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ઇનપુટ"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `અમાન્ય ઇનપુટ: અપેક્ષિત ${t}, પ્રાપ્ત ${r[n] ?? n}`;
			}
			case "invalid_value": return e.values.length === 1 ? `અમાન્ય ઇનપુટ: અપેક્ષિત ${D(e.values[0])}` : `અમાન્ય વિકલ્પ: ${j(e.values, " | ")} માધ્યમથી એક અપેક્ષિત`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `ખૂબ મોટું: ${e.origin ?? "મૂલ્ય"} ${n}${e.maximum.toString()} ${r.unit ?? "એલિમેન્ટ"} હોવા જોઈએ` : `ખૂબ મોટું: ${e.origin ?? "મૂલ્ય"} ${n}${e.maximum.toString()} હોવું જોઈએ`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `ખૂબ નાનું: ${e.origin} ${n}${e.minimum.toString()} ${r.unit} હોવા જોઈએ` : `ખૂબ નાનું: ${e.origin} ${n}${e.minimum.toString()} હોવું જોઈએ`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `અમાન્ય સ્ટ્રિંગ: "${t.prefix}" થી શરૂ થવું જોઈએ` : t.format === "ends_with" ? `અમાન્ય સ્ટ્રિંગ: "${t.suffix}" પર સમાપ્ત થવું જોઈએ` : t.format === "includes" ? `અમાન્ય સ્ટ્રિંગ: "${t.includes}" શામેલ હોવું જોઈએ` : t.format === "regex" ? `અમાન્ય સ્ટ્રિંગ: પેટર્ન ${t.pattern} સાથે મેળ ખાવું જોઈએ` : `અમાન્ય ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `અમાન્ય નંબર: ${e.divisor} નો ગુણાંક હોવો જોઈએ`;
			case "unrecognized_keys": return `ઓળખી શકાતા નહીં તે કી${e.keys.length > 1 ? "ઓ" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} માં અમાન્ય કી`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `અમાન્ય ડિસ્ક્રિમિનેટર મૂલ્ય. અપેક્ષિત ${e.options.map((e) => `'${e}'`).join(" | ")}` : "અમાન્ય ઇનપુટ";
			case "invalid_element": return `${e.origin} માં અમાન્ય મૂલ્ય`;
			default: return "અમાન્ય ઇનપુટ";
		}
	};
};
function Sl() {
	return { localeError: xl() };
}
//#endregion
//#region node_modules/zod/v4/locales/he.js
var Cl = () => {
	let e = {
		string: {
			label: "מחרוזת",
			gender: "f"
		},
		number: {
			label: "מספר",
			gender: "m"
		},
		boolean: {
			label: "ערך בוליאני",
			gender: "m"
		},
		bigint: {
			label: "BigInt",
			gender: "m"
		},
		date: {
			label: "תאריך",
			gender: "m"
		},
		array: {
			label: "מערך",
			gender: "m"
		},
		object: {
			label: "אובייקט",
			gender: "m"
		},
		null: {
			label: "ערך ריק (null)",
			gender: "m"
		},
		undefined: {
			label: "ערך לא מוגדר (undefined)",
			gender: "m"
		},
		symbol: {
			label: "סימבול (Symbol)",
			gender: "m"
		},
		function: {
			label: "פונקציה",
			gender: "f"
		},
		map: {
			label: "מפה (Map)",
			gender: "f"
		},
		set: {
			label: "קבוצה (Set)",
			gender: "f"
		},
		file: {
			label: "קובץ",
			gender: "m"
		},
		promise: {
			label: "Promise",
			gender: "m"
		},
		NaN: {
			label: "NaN",
			gender: "m"
		},
		unknown: {
			label: "ערך לא ידוע",
			gender: "m"
		},
		value: {
			label: "ערך",
			gender: "m"
		}
	}, t = {
		string: {
			unit: "תווים",
			shortLabel: "קצר",
			longLabel: "ארוך"
		},
		file: {
			unit: "בייטים",
			shortLabel: "קטן",
			longLabel: "גדול"
		},
		array: {
			unit: "פריטים",
			shortLabel: "קטן",
			longLabel: "גדול"
		},
		set: {
			unit: "פריטים",
			shortLabel: "קטן",
			longLabel: "גדול"
		},
		number: {
			unit: "",
			shortLabel: "קטן",
			longLabel: "גדול"
		}
	}, n = (t) => t ? e[t] : void 0, r = (t) => {
		let r = n(t);
		return r ? r.label : t ?? e.unknown.label;
	}, i = (e) => `ה${r(e)}`, a = (e) => (n(e)?.gender ?? "m") === "f" ? "צריכה להיות" : "צריך להיות", o = (e) => e ? t[e] ?? null : null, s = {
		regex: {
			label: "קלט",
			gender: "m"
		},
		email: {
			label: "כתובת אימייל",
			gender: "f"
		},
		url: {
			label: "כתובת רשת",
			gender: "f"
		},
		emoji: {
			label: "אימוג'י",
			gender: "m"
		},
		uuid: {
			label: "UUID",
			gender: "m"
		},
		uuidv4: {
			label: "UUIDv4",
			gender: "m"
		},
		uuidv6: {
			label: "UUIDv6",
			gender: "m"
		},
		nanoid: {
			label: "nanoid",
			gender: "m"
		},
		guid: {
			label: "GUID",
			gender: "m"
		},
		cuid: {
			label: "cuid",
			gender: "m"
		},
		cuid2: {
			label: "cuid2",
			gender: "m"
		},
		ulid: {
			label: "ULID",
			gender: "m"
		},
		xid: {
			label: "XID",
			gender: "m"
		},
		ksuid: {
			label: "KSUID",
			gender: "m"
		},
		datetime: {
			label: "תאריך וזמן ISO",
			gender: "m"
		},
		date: {
			label: "תאריך ISO",
			gender: "m"
		},
		time: {
			label: "זמן ISO",
			gender: "m"
		},
		duration: {
			label: "משך זמן ISO",
			gender: "m"
		},
		ipv4: {
			label: "כתובת IPv4",
			gender: "f"
		},
		ipv6: {
			label: "כתובת IPv6",
			gender: "f"
		},
		mac: {
			label: "כתובת MAC",
			gender: "f"
		},
		cidrv4: {
			label: "טווח IPv4",
			gender: "m"
		},
		cidrv6: {
			label: "טווח IPv6",
			gender: "m"
		},
		base64: {
			label: "מחרוזת בבסיס 64",
			gender: "f"
		},
		base64url: {
			label: "מחרוזת בבסיס 64 לכתובות רשת",
			gender: "f"
		},
		json_string: {
			label: "מחרוזת JSON",
			gender: "f"
		},
		e164: {
			label: "מספר E.164",
			gender: "m"
		},
		credit_card: {
			label: "מספר כרטיס אשראי",
			gender: "m"
		},
		currency_code: {
			label: "קוד מטבע",
			gender: "m"
		},
		iban: {
			label: "IBAN",
			gender: "m"
		},
		jwt: {
			label: "JWT",
			gender: "m"
		},
		template_literal: {
			label: "קלט",
			gender: "m"
		},
		ends_with: {
			label: "קלט",
			gender: "m"
		},
		includes: {
			label: "קלט",
			gender: "m"
		},
		lowercase: {
			label: "קלט",
			gender: "m"
		},
		starts_with: {
			label: "קלט",
			gender: "m"
		},
		uppercase: {
			label: "קלט",
			gender: "m"
		}
	}, c = { nan: "NaN" };
	return (t) => {
		switch (t.code) {
			case "invalid_type": {
				let n = t.expected, i = c[n ?? ""] ?? r(n), a = E(t.input), o = c[a] ?? e[a]?.label ?? a;
				return /^[A-Z]/.test(t.expected) ? `קלט לא תקין: צריך להיות instanceof ${t.expected}, התקבל ${o}` : `קלט לא תקין: צריך להיות ${i}, התקבל ${o}`;
			}
			case "invalid_value": {
				if (t.values.length === 1) return `ערך לא תקין: הערך חייב להיות ${D(t.values[0])}`;
				let e = t.values.map((e) => D(e));
				if (t.values.length === 2) return `ערך לא תקין: האפשרויות המתאימות הן ${e[0]} או ${e[1]}`;
				let n = e[e.length - 1];
				return `ערך לא תקין: האפשרויות המתאימות הן ${e.slice(0, -1).join(", ")} או ${n}`;
			}
			case "too_big": {
				let e = o(t.origin), n = i(t.origin ?? "value");
				if (t.origin === "string") return `${e?.longLabel ?? "ארוך"} מדי: ${n} צריכה להכיל ${t.maximum.toString()} ${e?.unit ?? ""} ${t.inclusive ? "או פחות" : "לכל היותר"}`.trim();
				if (t.origin === "number") return `גדול מדי: ${n} צריך להיות ${t.inclusive ? `קטן או שווה ל-${t.maximum}` : `קטן מ-${t.maximum}`}`;
				if (t.origin === "array" || t.origin === "set") return `גדול מדי: ${n} ${t.origin === "set" ? "צריכה" : "צריך"} להכיל ${t.inclusive ? `${t.maximum} ${e?.unit ?? ""} או פחות` : `פחות מ-${t.maximum} ${e?.unit ?? ""}`}`.trim();
				let r = t.inclusive ? "<=" : "<", s = a(t.origin ?? "value");
				return e?.unit ? `${e.longLabel} מדי: ${n} ${s} ${r}${t.maximum.toString()} ${e.unit}` : `${e?.longLabel ?? "גדול"} מדי: ${n} ${s} ${r}${t.maximum.toString()}`;
			}
			case "too_small": {
				let e = o(t.origin), n = i(t.origin ?? "value");
				if (t.origin === "string") return `${e?.shortLabel ?? "קצר"} מדי: ${n} צריכה להכיל ${t.minimum.toString()} ${e?.unit ?? ""} ${t.inclusive ? "או יותר" : "לפחות"}`.trim();
				if (t.origin === "number") return `קטן מדי: ${n} צריך להיות ${t.inclusive ? `גדול או שווה ל-${t.minimum}` : `גדול מ-${t.minimum}`}`;
				if (t.origin === "array" || t.origin === "set") {
					let r = t.origin === "set" ? "צריכה" : "צריך";
					return t.minimum === 1 && t.inclusive ? `קטן מדי: ${n} ${r} להכיל ${t.origin, "לפחות פריט אחד"}` : `קטן מדי: ${n} ${r} להכיל ${t.inclusive ? `${t.minimum} ${e?.unit ?? ""} או יותר` : `יותר מ-${t.minimum} ${e?.unit ?? ""}`}`.trim();
				}
				let r = t.inclusive ? ">=" : ">", s = a(t.origin ?? "value");
				return e?.unit ? `${e.shortLabel} מדי: ${n} ${s} ${r}${t.minimum.toString()} ${e.unit}` : `${e?.shortLabel ?? "קטן"} מדי: ${n} ${s} ${r}${t.minimum.toString()}`;
			}
			case "invalid_format": {
				let e = t;
				if (e.format === "starts_with") return `המחרוזת חייבת להתחיל ב "${e.prefix}"`;
				if (e.format === "ends_with") return `המחרוזת חייבת להסתיים ב "${e.suffix}"`;
				if (e.format === "includes") return `המחרוזת חייבת לכלול "${e.includes}"`;
				if (e.format === "regex") return `המחרוזת חייבת להתאים לתבנית ${e.pattern}`;
				let n = s[e.format];
				return `${n?.label ?? e.format} לא ${(n?.gender ?? "m") === "f" ? "תקינה" : "תקין"}`;
			}
			case "not_multiple_of": return `מספר לא תקין: חייב להיות מכפלה של ${t.divisor}`;
			case "unrecognized_keys": return `מפתח${t.keys.length > 1 ? "ות" : ""} לא מזוה${t.keys.length > 1 ? "ים" : "ה"}: ${j(t.keys, ", ")}`;
			case "invalid_key": return "שדה לא תקין באובייקט";
			case "invalid_union": return "קלט לא תקין";
			case "invalid_element": return `ערך לא תקין ב${i(t.origin ?? "array")}`;
			default: return "קלט לא תקין";
		}
	};
};
function wl() {
	return { localeError: Cl() };
}
//#endregion
//#region node_modules/zod/v4/locales/hi.js
var Tl = () => {
	let e = {
		string: {
			unit: "अक्षर",
			verb: "रखने के लिए"
		},
		file: {
			unit: "बाइट्स",
			verb: "रखने के लिए"
		},
		array: {
			unit: "तत्व",
			verb: "रखने के लिए"
		},
		set: {
			unit: "तत्व",
			verb: "रखने के लिए"
		},
		map: {
			unit: "प्रविष्टियाँ",
			verb: "रखने के लिए"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "इनपुट",
		email: "ईमेल पता",
		url: "URL",
		emoji: "इमोजी",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO तिथि और समय",
		date: "ISO तिथि",
		time: "ISO समय",
		duration: "ISO अवधि",
		ipv4: "IPv4 पता",
		ipv6: "IPv6 पता",
		mac: "MAC पता",
		cidrv4: "IPv4 श्रेणी",
		cidrv6: "IPv6 श्रेणी",
		base64: "Base64-एन्कोडेड स्ट्रिंग",
		base64url: "Base64URL-एन्कोडेड स्ट्रिंग",
		json_string: "JSON स्ट्रिंग",
		e164: "E.164 संख्या",
		credit_card: "क्रेडिट कार्ड संख्या",
		currency_code: "मुद्रा कोड",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "इनपुट"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `अमान्य इनपुट: अपेक्षित ${t}, प्राप्त ${r[n] ?? n}`;
			}
			case "invalid_value": return e.values.length === 1 ? `अमान्य इनपुट: अपेक्षित ${D(e.values[0])}` : `अमान्य विकल्प: अपेक्षित मानों में से एक ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `बहुत बड़ा: अपेक्षित था कि ${e.origin ?? "मान"} में ${n}${e.maximum} ${r.unit} हों` : `बहुत बड़ा: अपेक्षित था कि ${e.origin ?? "मान"} ${n}${e.maximum} हो`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `बहुत छोटा: अपेक्षित था कि ${e.origin} में ${n}${e.minimum} ${r.unit} हों` : `बहुत छोटा: अपेक्षित था कि ${e.origin} ${n}${e.minimum} हो`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `अमान्य स्ट्रिंग: "${t.prefix}" से शुरू होना चाहिए` : t.format === "ends_with" ? `अमान्य स्ट्रिंग: "${t.suffix}" पर समाप्त होना चाहिए` : t.format === "includes" ? `अमान्य स्ट्रिंग: इसमें "${t.includes}" शामिल होना चाहिए` : t.format === "regex" ? `अमान्य स्ट्रिंग: पैटर्न ${t.pattern} से मेल खाना चाहिए` : `अमान्य ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `अमान्य संख्या: यह ${e.divisor} का गुणज होना चाहिए`;
			case "unrecognized_keys": return `अपरिचित कुंजी${e.keys.length > 1 ? "याँ" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `अमान्य कुंजी: ${e.origin} में`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `अमान्य डिस्क्रिमिनेटर मान: अपेक्षित ${e.options.map((e) => `'${e}'`).join(" | ")}` : "अमान्य इनपुट";
			case "invalid_element": return `अमान्य मान: ${e.origin} में`;
			default: return "अमान्य इनपुट";
		}
	};
};
function El() {
	return { localeError: Tl() };
}
//#endregion
//#region node_modules/zod/v4/locales/hr.js
var Dl = () => {
	let e = {
		string: {
			unit: "znakova",
			verb: "imati"
		},
		file: {
			unit: "bajtova",
			verb: "imati"
		},
		array: {
			unit: "stavki",
			verb: "imati"
		},
		set: {
			unit: "stavki",
			verb: "imati"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "unos",
		email: "email adresa",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datum i vrijeme",
		date: "ISO datum",
		time: "ISO vrijeme",
		duration: "ISO trajanje",
		ipv4: "IPv4 adresa",
		ipv6: "IPv6 adresa",
		mac: "MAC adresa",
		cidrv4: "IPv4 raspon",
		cidrv6: "IPv6 raspon",
		base64: "base64 kodirani tekst",
		base64url: "base64url kodirani tekst",
		json_string: "JSON tekst",
		e164: "E.164 broj",
		credit_card: "broj kreditne kartice",
		currency_code: "kod valute",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "unos"
	}, r = {
		nan: "NaN",
		string: "tekst",
		number: "broj",
		boolean: "boolean",
		array: "niz",
		object: "objekt",
		set: "skup",
		file: "datoteka",
		date: "datum",
		bigint: "bigint",
		symbol: "simbol",
		undefined: "undefined",
		null: "null",
		function: "funkcija",
		map: "mapa"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Neispravan unos: očekuje se instanceof ${e.expected}, a primljeno je ${i}` : `Neispravan unos: očekuje se ${t}, a primljeno je ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Neispravna vrijednost: očekivano ${D(e.values[0])}` : `Neispravna opcija: očekivano jedno od ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", i = t(e.origin), a = r[e.origin] ?? e.origin;
				return i ? `Preveliko: očekivano da ${a ?? "vrijednost"} ima ${n}${e.maximum.toString()} ${i.unit ?? "elemenata"}` : `Preveliko: očekivano da ${a ?? "vrijednost"} bude ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", i = t(e.origin), a = r[e.origin] ?? e.origin;
				return i ? `Premalo: očekivano da ${a} ima ${n}${e.minimum.toString()} ${i.unit}` : `Premalo: očekivano da ${a} bude ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Neispravan tekst: mora započinjati s "${t.prefix}"` : t.format === "ends_with" ? `Neispravan tekst: mora završavati s "${t.suffix}"` : t.format === "includes" ? `Neispravan tekst: mora sadržavati "${t.includes}"` : t.format === "regex" ? `Neispravan tekst: mora odgovarati uzorku ${t.pattern}` : `Neispravna ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Neispravan broj: mora biti višekratnik od ${e.divisor}`;
			case "unrecognized_keys": return `Neprepoznat${e.keys.length > 1 ? "i ključevi" : " ključ"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Neispravan ključ u ${r[e.origin] ?? e.origin}`;
			case "invalid_union": return "Neispravan unos";
			case "invalid_element": return `Neispravna vrijednost u ${r[e.origin] ?? e.origin}`;
			default: return "Neispravan unos";
		}
	};
};
function Ol() {
	return { localeError: Dl() };
}
//#endregion
//#region node_modules/zod/v4/locales/hu.js
var kl = () => {
	let e = {
		string: {
			unit: "karakter",
			verb: "legyen"
		},
		file: {
			unit: "byte",
			verb: "legyen"
		},
		array: {
			unit: "elem",
			verb: "legyen"
		},
		set: {
			unit: "elem",
			verb: "legyen"
		},
		map: {
			unit: "elem",
			verb: "legyen"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "bemenet",
		email: "email cím",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO időbélyeg",
		date: "ISO dátum",
		time: "ISO idő",
		duration: "ISO időintervallum",
		ipv4: "IPv4 cím",
		ipv6: "IPv6 cím",
		mac: "MAC cím",
		cidrv4: "IPv4 tartomány",
		cidrv6: "IPv6 tartomány",
		base64: "base64-kódolt string",
		base64url: "base64url-kódolt string",
		json_string: "JSON string",
		e164: "E.164 szám",
		credit_card: "hitelkártyaszám",
		currency_code: "pénznemkód",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "bemenet"
	}, r = {
		nan: "NaN",
		number: "szám",
		array: "tömb"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Érvénytelen bemenet: a várt érték instanceof ${e.expected}, a kapott érték ${i}` : `Érvénytelen bemenet: a várt érték ${t}, a kapott érték ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Érvénytelen bemenet: a várt érték ${D(e.values[0])}` : `Érvénytelen opció: valamelyik érték várt ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Túl nagy: ${e.origin ?? "érték"} mérete túl nagy ${n}${e.maximum.toString()} ${r.unit ?? "elem"}` : `Túl nagy: a bemeneti érték ${e.origin ?? "érték"} túl nagy: ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Túl kicsi: a bemeneti érték ${e.origin} mérete túl kicsi ${n}${e.minimum.toString()} ${r.unit}` : `Túl kicsi: a bemeneti érték ${e.origin} túl kicsi ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Érvénytelen string: "${t.prefix}" értékkel kell kezdődnie` : t.format === "ends_with" ? `Érvénytelen string: "${t.suffix}" értékkel kell végződnie` : t.format === "includes" ? `Érvénytelen string: "${t.includes}" értéket kell tartalmaznia` : t.format === "regex" ? `Érvénytelen string: ${t.pattern} mintának kell megfelelnie` : `Érvénytelen ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Érvénytelen szám: ${e.divisor} többszörösének kell lennie`;
			case "unrecognized_keys": return `Ismeretlen kulcs${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Érvénytelen kulcs ${e.origin}`;
			case "invalid_union": return "Érvénytelen bemenet";
			case "invalid_element": return `Érvénytelen érték: ${e.origin}`;
			default: return "Érvénytelen bemenet";
		}
	};
};
function Al() {
	return { localeError: kl() };
}
//#endregion
//#region node_modules/zod/v4/locales/hy.js
function jl(e, t, n) {
	return Math.abs(e) === 1 ? t : n;
}
function Ml(e) {
	if (!e) return "";
	let t = [
		"ա",
		"ե",
		"ը",
		"ի",
		"ո",
		"ու",
		"օ"
	], n = e[e.length - 1];
	return e + (t.includes(n) ? "ն" : "ը");
}
var Nl = () => {
	let e = {
		string: {
			unit: {
				one: "նշան",
				many: "նշաններ"
			},
			verb: "ունենալ"
		},
		file: {
			unit: {
				one: "բայթ",
				many: "բայթեր"
			},
			verb: "ունենալ"
		},
		array: {
			unit: {
				one: "տարր",
				many: "տարրեր"
			},
			verb: "ունենալ"
		},
		set: {
			unit: {
				one: "տարր",
				many: "տարրեր"
			},
			verb: "ունենալ"
		},
		map: {
			unit: {
				one: "տարր",
				many: "տարրեր"
			},
			verb: "ունենալ"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "մուտք",
		email: "էլ. հասցե",
		url: "URL",
		emoji: "էմոջի",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO ամսաթիվ և ժամ",
		date: "ISO ամսաթիվ",
		time: "ISO ժամ",
		duration: "ISO տևողություն",
		ipv4: "IPv4 հասցե",
		ipv6: "IPv6 հասցե",
		mac: "MAC հասցե",
		cidrv4: "IPv4 միջակայք",
		cidrv6: "IPv6 միջակայք",
		base64: "base64 ձևաչափով տող",
		base64url: "base64url ձևաչափով տող",
		json_string: "JSON տող",
		e164: "E.164 համար",
		credit_card: "կրեդիտ քարտի համար",
		currency_code: "արժույթի կոդ",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "մուտք"
	}, r = {
		nan: "NaN",
		number: "թիվ",
		array: "զանգված"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Սխալ մուտքագրում․ սպասվում էր instanceof ${e.expected}, ստացվել է ${i}` : `Սխալ մուտքագրում․ սպասվում էր ${t}, ստացվել է ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Սխալ մուտքագրում․ սպասվում էր ${D(e.values[1])}` : `Սխալ տարբերակ․ սպասվում էր հետևյալներից մեկը՝ ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				if (r) {
					let t = jl(Number(e.maximum), r.unit.one, r.unit.many);
					return `Չափազանց մեծ արժեք․ սպասվում է, որ ${Ml(e.origin ?? "արժեք")} կունենա ${n}${e.maximum.toString()} ${t}`;
				}
				return `Չափազանց մեծ արժեք․ սպասվում է, որ ${Ml(e.origin ?? "արժեք")} լինի ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				if (r) {
					let t = jl(Number(e.minimum), r.unit.one, r.unit.many);
					return `Չափազանց փոքր արժեք․ սպասվում է, որ ${Ml(e.origin)} կունենա ${n}${e.minimum.toString()} ${t}`;
				}
				return `Չափազանց փոքր արժեք․ սպասվում է, որ ${Ml(e.origin)} լինի ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Սխալ տող․ պետք է սկսվի "${t.prefix}"-ով` : t.format === "ends_with" ? `Սխալ տող․ պետք է ավարտվի "${t.suffix}"-ով` : t.format === "includes" ? `Սխալ տող․ պետք է պարունակի "${t.includes}"` : t.format === "regex" ? `Սխալ տող․ պետք է համապատասխանի ${t.pattern} ձևաչափին` : `Սխալ ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Սխալ թիվ․ պետք է բազմապատիկ լինի ${e.divisor}-ի`;
			case "unrecognized_keys": return `Չճանաչված բանալի${e.keys.length > 1 ? "ներ" : ""}. ${j(e.keys, ", ")}`;
			case "invalid_key": return `Սխալ բանալի ${Ml(e.origin)}-ում`;
			case "invalid_union": return "Սխալ մուտքագրում";
			case "invalid_element": return `Սխալ արժեք ${Ml(e.origin)}-ում`;
			default: return "Սխալ մուտքագրում";
		}
	};
};
function Pl() {
	return { localeError: Nl() };
}
//#endregion
//#region node_modules/zod/v4/locales/id.js
var Fl = () => {
	let e = {
		string: {
			unit: "karakter",
			verb: "memiliki"
		},
		file: {
			unit: "byte",
			verb: "memiliki"
		},
		array: {
			unit: "item",
			verb: "memiliki"
		},
		set: {
			unit: "item",
			verb: "memiliki"
		},
		map: {
			unit: "item",
			verb: "memiliki"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "alamat email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "tanggal dan waktu format ISO",
		date: "tanggal format ISO",
		time: "jam format ISO",
		duration: "durasi format ISO",
		ipv4: "alamat IPv4",
		ipv6: "alamat IPv6",
		mac: "alamat MAC",
		cidrv4: "rentang alamat IPv4",
		cidrv6: "rentang alamat IPv6",
		base64: "string dengan enkode base64",
		base64url: "string dengan enkode base64url",
		json_string: "string JSON",
		e164: "angka E.164",
		credit_card: "nomor kartu kredit",
		currency_code: "kode mata uang",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Input tidak valid: diharapkan instanceof ${e.expected}, diterima ${i}` : `Input tidak valid: diharapkan ${t}, diterima ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Input tidak valid: diharapkan ${D(e.values[0])}` : `Pilihan tidak valid: diharapkan salah satu dari ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Terlalu besar: diharapkan ${e.origin ?? "value"} memiliki ${n}${e.maximum.toString()} ${r.unit ?? "elemen"}` : `Terlalu besar: diharapkan ${e.origin ?? "value"} menjadi ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Terlalu kecil: diharapkan ${e.origin} memiliki ${n}${e.minimum.toString()} ${r.unit}` : `Terlalu kecil: diharapkan ${e.origin} menjadi ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `String tidak valid: harus dimulai dengan "${t.prefix}"` : t.format === "ends_with" ? `String tidak valid: harus berakhir dengan "${t.suffix}"` : t.format === "includes" ? `String tidak valid: harus menyertakan "${t.includes}"` : t.format === "regex" ? `String tidak valid: harus sesuai pola ${t.pattern}` : `${n[t.format] ?? e.format} tidak valid`;
			}
			case "not_multiple_of": return `Angka tidak valid: harus kelipatan dari ${e.divisor}`;
			case "unrecognized_keys": return `Kunci tidak dikenali ${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Kunci tidak valid di ${e.origin}`;
			case "invalid_union": return "Input tidak valid";
			case "invalid_element": return `Nilai tidak valid di ${e.origin}`;
			default: return "Input tidak valid";
		}
	};
};
function Il() {
	return { localeError: Fl() };
}
//#endregion
//#region node_modules/zod/v4/locales/is.js
var Ll = () => {
	let e = {
		string: {
			unit: "stafi",
			verb: "að hafa"
		},
		file: {
			unit: "bæti",
			verb: "að hafa"
		},
		array: {
			unit: "hluti",
			verb: "að hafa"
		},
		set: {
			unit: "hluti",
			verb: "að hafa"
		},
		map: {
			unit: "hluti",
			verb: "að hafa"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "gildi",
		email: "netfang",
		url: "vefslóð",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dagsetning og tími",
		date: "ISO dagsetning",
		time: "ISO tími",
		duration: "ISO tímalengd",
		ipv4: "IPv4 address",
		ipv6: "IPv6 address",
		mac: "MAC address",
		cidrv4: "IPv4 range",
		cidrv6: "IPv6 range",
		base64: "base64-encoded strengur",
		base64url: "base64url-encoded strengur",
		json_string: "JSON strengur",
		e164: "E.164 tölugildi",
		credit_card: "kreditkortanúmer",
		currency_code: "gjaldmiðilskóði",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "gildi"
	}, r = {
		nan: "NaN",
		number: "númer",
		array: "fylki"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Rangt gildi: Þú slóst inn ${i} þar sem á að vera instanceof ${e.expected}` : `Rangt gildi: Þú slóst inn ${i} þar sem á að vera ${t}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Rangt gildi: gert ráð fyrir ${D(e.values[0])}` : `Ógilt val: má vera eitt af eftirfarandi ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Of stórt: gert er ráð fyrir að ${e.origin ?? "gildi"} hafi ${n}${e.maximum.toString()} ${r.unit ?? "hluti"}` : `Of stórt: gert er ráð fyrir að ${e.origin ?? "gildi"} sé ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Of lítið: gert er ráð fyrir að ${e.origin} hafi ${n}${e.minimum.toString()} ${r.unit}` : `Of lítið: gert er ráð fyrir að ${e.origin} sé ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ógildur strengur: verður að byrja á "${t.prefix}"` : t.format === "ends_with" ? `Ógildur strengur: verður að enda á "${t.suffix}"` : t.format === "includes" ? `Ógildur strengur: verður að innihalda "${t.includes}"` : t.format === "regex" ? `Ógildur strengur: verður að fylgja mynstri ${t.pattern}` : `Rangt ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Röng tala: verður að vera margfeldi af ${e.divisor}`;
			case "unrecognized_keys": return `Óþekkt ${e.keys.length > 1 ? "ir lyklar" : "ur lykill"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Rangur lykill í ${e.origin}`;
			case "invalid_union": return "Rangt gildi";
			case "invalid_element": return `Rangt gildi í ${e.origin}`;
			default: return "Rangt gildi";
		}
	};
};
function Rl() {
	return { localeError: Ll() };
}
//#endregion
//#region node_modules/zod/v4/locales/it.js
var zl = () => {
	let e = {
		string: {
			unit: "caratteri",
			verb: "avere"
		},
		file: {
			unit: "byte",
			verb: "avere"
		},
		array: {
			unit: "elementi",
			verb: "avere"
		},
		set: {
			unit: "elementi",
			verb: "avere"
		},
		map: {
			unit: "elementi",
			verb: "avere"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "indirizzo email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data e ora ISO",
		date: "data ISO",
		time: "ora ISO",
		duration: "durata ISO",
		ipv4: "indirizzo IPv4",
		ipv6: "indirizzo IPv6",
		mac: "indirizzo MAC",
		cidrv4: "intervallo IPv4",
		cidrv6: "intervallo IPv6",
		base64: "stringa codificata in base64",
		base64url: "URL codificata in base64",
		json_string: "stringa JSON",
		e164: "numero E.164",
		credit_card: "numero di carta di credito",
		currency_code: "codice valuta",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = {
		nan: "NaN",
		number: "numero",
		array: "vettore"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Input non valido: atteso instanceof ${e.expected}, ricevuto ${i}` : `Input non valido: atteso ${t}, ricevuto ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Input non valido: atteso ${D(e.values[0])}` : `Opzione non valida: atteso uno tra ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Troppo grande: ${e.origin ?? "valore"} deve avere ${n}${e.maximum.toString()} ${r.unit ?? "elementi"}` : `Troppo grande: ${e.origin ?? "valore"} deve essere ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Troppo piccolo: ${e.origin} deve avere ${n}${e.minimum.toString()} ${r.unit}` : `Troppo piccolo: ${e.origin} deve essere ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Stringa non valida: deve iniziare con "${t.prefix}"` : t.format === "ends_with" ? `Stringa non valida: deve terminare con "${t.suffix}"` : t.format === "includes" ? `Stringa non valida: deve includere "${t.includes}"` : t.format === "regex" ? `Stringa non valida: deve corrispondere al pattern ${t.pattern}` : `Input non valido: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Numero non valido: deve essere un multiplo di ${e.divisor}`;
			case "unrecognized_keys": return `Chiav${e.keys.length > 1 ? "i" : "e"} non riconosciut${e.keys.length > 1 ? "e" : "a"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Chiave non valida in ${e.origin}`;
			case "invalid_union": return "Input non valido";
			case "invalid_element": return `Valore non valido in ${e.origin}`;
			default: return "Input non valido";
		}
	};
};
function Bl() {
	return { localeError: zl() };
}
//#endregion
//#region node_modules/zod/v4/locales/ja.js
var Vl = () => {
	let e = {
		string: {
			unit: "文字",
			verb: "である"
		},
		file: {
			unit: "バイト",
			verb: "である"
		},
		array: {
			unit: "要素",
			verb: "である"
		},
		set: {
			unit: "要素",
			verb: "である"
		},
		map: {
			unit: "要素",
			verb: "である"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "入力値",
		email: "メールアドレス",
		url: "URL",
		emoji: "絵文字",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO日時",
		date: "ISO日付",
		time: "ISO時刻",
		duration: "ISO期間",
		ipv4: "IPv4アドレス",
		ipv6: "IPv6アドレス",
		mac: "MACアドレス",
		cidrv4: "IPv4範囲",
		cidrv6: "IPv6範囲",
		base64: "base64エンコード文字列",
		base64url: "base64urlエンコード文字列",
		json_string: "JSON文字列",
		e164: "E.164番号",
		credit_card: "クレジットカード番号",
		currency_code: "通貨コード",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "入力値"
	}, r = {
		nan: "NaN",
		number: "数値",
		array: "配列"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `無効な入力: instanceof ${e.expected}が期待されましたが、${i}が入力されました` : `無効な入力: ${t}が期待されましたが、${i}が入力されました`;
			}
			case "invalid_value": return e.values.length === 1 ? `無効な入力: ${D(e.values[0])}が期待されました` : `無効な選択: ${j(e.values, "、")}のいずれかである必要があります`;
			case "too_big": {
				let n = e.inclusive ? "以下である" : "より小さい", r = t(e.origin);
				return r ? `大きすぎる値: ${e.origin ?? "値"}は${e.maximum.toString()}${r.unit ?? "要素"}${n}必要があります` : `大きすぎる値: ${e.origin ?? "値"}は${e.maximum.toString()}${n}必要があります`;
			}
			case "too_small": {
				let n = e.inclusive ? "以上である" : "より大きい", r = t(e.origin);
				return r ? `小さすぎる値: ${e.origin}は${e.minimum.toString()}${r.unit}${n}必要があります` : `小さすぎる値: ${e.origin}は${e.minimum.toString()}${n}必要があります`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `無効な文字列: "${t.prefix}"で始まる必要があります` : t.format === "ends_with" ? `無効な文字列: "${t.suffix}"で終わる必要があります` : t.format === "includes" ? `無効な文字列: "${t.includes}"を含む必要があります` : t.format === "regex" ? `無効な文字列: パターン${t.pattern}に一致する必要があります` : `無効な${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `無効な数値: ${e.divisor}の倍数である必要があります`;
			case "unrecognized_keys": return `認識されていないキー${e.keys.length > 1 ? "群" : ""}: ${j(e.keys, "、")}`;
			case "invalid_key": return `${e.origin}内の無効なキー`;
			case "invalid_union": return "無効な入力";
			case "invalid_element": return `${e.origin}内の無効な値`;
			default: return "無効な入力";
		}
	};
};
function Hl() {
	return { localeError: Vl() };
}
//#endregion
//#region node_modules/zod/v4/locales/ka.js
var Ul = () => {
	let e = {
		string: {
			unit: "სიმბოლო",
			verb: "უნდა შეიცავდეს"
		},
		file: {
			unit: "ბაიტი",
			verb: "უნდა შეიცავდეს"
		},
		array: {
			unit: "ელემენტი",
			verb: "უნდა შეიცავდეს"
		},
		set: {
			unit: "ელემენტი",
			verb: "უნდა შეიცავდეს"
		},
		map: {
			unit: "ელემენტი",
			verb: "უნდა შეიცავდეს"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "შეყვანა",
		email: "ელ-ფოსტის მისამართი",
		url: "URL",
		emoji: "ემოჯი",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "თარიღი-დრო",
		date: "თარიღი",
		time: "დრო",
		duration: "ხანგრძლივობა",
		ipv4: "IPv4 მისამართი",
		ipv6: "IPv6 მისამართი",
		mac: "MAC მისამართი",
		cidrv4: "IPv4 დიაპაზონი",
		cidrv6: "IPv6 დიაპაზონი",
		base64: "base64-კოდირებული ველი",
		base64url: "base64url-კოდირებული ველი",
		json_string: "JSON ველი",
		e164: "E.164 ნომერი",
		credit_card: "საკრედიტო ბარათის ნომერი",
		currency_code: "ვალუტის კოდი",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "შეყვანა"
	}, r = {
		nan: "NaN",
		number: "რიცხვი",
		string: "ველი",
		boolean: "ბულეანი",
		function: "ფუნქცია",
		array: "მასივი"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `არასწორი შეყვანა: მოსალოდნელი instanceof ${e.expected}, მიღებული ${i}` : `არასწორი შეყვანა: მოსალოდნელი ${t}, მიღებული ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `არასწორი შეყვანა: მოსალოდნელი ${D(e.values[0])}` : `არასწორი ვარიანტი: მოსალოდნელია ერთ-ერთი ${j(e.values, "|")}-დან`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `ზედმეტად დიდი: მოსალოდნელი ${e.origin ?? "მნიშვნელობა"} ${r.verb} ${n}${e.maximum.toString()} ${r.unit}` : `ზედმეტად დიდი: მოსალოდნელი ${e.origin ?? "მნიშვნელობა"} იყოს ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `ზედმეტად პატარა: მოსალოდნელი ${e.origin} ${r.verb} ${n}${e.minimum.toString()} ${r.unit}` : `ზედმეტად პატარა: მოსალოდნელი ${e.origin} იყოს ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `არასწორი ველი: უნდა იწყებოდეს "${t.prefix}"-ით` : t.format === "ends_with" ? `არასწორი ველი: უნდა მთავრდებოდეს "${t.suffix}"-ით` : t.format === "includes" ? `არასწორი ველი: უნდა შეიცავდეს "${t.includes}"-ს` : t.format === "regex" ? `არასწორი ველი: უნდა შეესაბამებოდეს შაბლონს ${t.pattern}` : `არასწორი ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `არასწორი რიცხვი: უნდა იყოს ${e.divisor}-ის ჯერადი`;
			case "unrecognized_keys": return `უცნობი გასაღებ${e.keys.length > 1 ? "ები" : "ი"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `არასწორი გასაღები ${e.origin}-ში`;
			case "invalid_union": return "არასწორი შეყვანა";
			case "invalid_element": return `არასწორი მნიშვნელობა ${e.origin}-ში`;
			default: return "არასწორი შეყვანა";
		}
	};
};
function Wl() {
	return { localeError: Ul() };
}
//#endregion
//#region node_modules/zod/v4/locales/km.js
var Gl = () => {
	let e = {
		string: {
			unit: "តួអក្សរ",
			verb: "គួរមាន"
		},
		file: {
			unit: "បៃ",
			verb: "គួរមាន"
		},
		array: {
			unit: "ធាតុ",
			verb: "គួរមាន"
		},
		set: {
			unit: "ធាតុ",
			verb: "គួរមាន"
		},
		map: {
			unit: "ធាតុ",
			verb: "គួរមាន"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ទិន្នន័យបញ្ចូល",
		email: "អាសយដ្ឋានអ៊ីមែល",
		url: "URL",
		emoji: "សញ្ញាអារម្មណ៍",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "កាលបរិច្ឆេទ និងម៉ោង ISO",
		date: "កាលបរិច្ឆេទ ISO",
		time: "ម៉ោង ISO",
		duration: "រយៈពេល ISO",
		ipv4: "អាសយដ្ឋាន IPv4",
		ipv6: "អាសយដ្ឋាន IPv6",
		mac: "អាសយដ្ឋាន MAC",
		cidrv4: "ដែនអាសយដ្ឋាន IPv4",
		cidrv6: "ដែនអាសយដ្ឋាន IPv6",
		base64: "ខ្សែអក្សរអ៊ិកូដ base64",
		base64url: "ខ្សែអក្សរអ៊ិកូដ base64url",
		json_string: "ខ្សែអក្សរ JSON",
		e164: "លេខ E.164",
		credit_card: "លេខប័ណ្ណឥណទាន",
		currency_code: "កូដរូបិយប័ណ្ណ",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ទិន្នន័យបញ្ចូល"
	}, r = {
		nan: "NaN",
		number: "លេខ",
		array: "អារេ (Array)",
		null: "គ្មានតម្លៃ (null)"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `ទិន្នន័យបញ្ចូលមិនត្រឹមត្រូវ៖ ត្រូវការ instanceof ${e.expected} ប៉ុន្តែទទួលបាន ${i}` : `ទិន្នន័យបញ្ចូលមិនត្រឹមត្រូវ៖ ត្រូវការ ${t} ប៉ុន្តែទទួលបាន ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `ទិន្នន័យបញ្ចូលមិនត្រឹមត្រូវ៖ ត្រូវការ ${D(e.values[0])}` : `ជម្រើសមិនត្រឹមត្រូវ៖ ត្រូវជាមួយក្នុងចំណោម ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `ធំពេក៖ ត្រូវការ ${e.origin ?? "តម្លៃ"} ${n} ${e.maximum.toString()} ${r.unit ?? "ធាតុ"}` : `ធំពេក៖ ត្រូវការ ${e.origin ?? "តម្លៃ"} ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `តូចពេក៖ ត្រូវការ ${e.origin} ${n} ${e.minimum.toString()} ${r.unit}` : `តូចពេក៖ ត្រូវការ ${e.origin} ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវចាប់ផ្តើមដោយ "${t.prefix}"` : t.format === "ends_with" ? `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវបញ្ចប់ដោយ "${t.suffix}"` : t.format === "includes" ? `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវមាន "${t.includes}"` : t.format === "regex" ? `ខ្សែអក្សរមិនត្រឹមត្រូវ៖ ត្រូវតែផ្គូផ្គងនឹងទម្រង់ដែលបានកំណត់ ${t.pattern}` : `មិនត្រឹមត្រូវ៖ ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `លេខមិនត្រឹមត្រូវ៖ ត្រូវតែជាពហុគុណនៃ ${e.divisor}`;
			case "unrecognized_keys": return `រកឃើញសោមិនស្គាល់៖ ${j(e.keys, ", ")}`;
			case "invalid_key": return `សោមិនត្រឹមត្រូវនៅក្នុង ${e.origin}`;
			case "invalid_union": return "ទិន្នន័យមិនត្រឹមត្រូវ";
			case "invalid_element": return `ទិន្នន័យមិនត្រឹមត្រូវនៅក្នុង ${e.origin}`;
			default: return "ទិន្នន័យមិនត្រឹមត្រូវ";
		}
	};
};
function Kl() {
	return { localeError: Gl() };
}
//#endregion
//#region node_modules/zod/v4/locales/kh.js
function ql() {
	return Kl();
}
//#endregion
//#region node_modules/zod/v4/locales/kn.js
var Jl = () => {
	let e = {
		string: {
			unit: "ಅಕ್ಷರಗಳು",
			verb: "ಹೊಂದಲು"
		},
		file: {
			unit: "ಬೈಟ್‌ಗಳು",
			verb: "ಹೊಂದಲು"
		},
		array: {
			unit: "ವಸ್ತುಗಳು",
			verb: "ಹೊಂದಲು"
		},
		set: {
			unit: "ವಸ್ತುಗಳು",
			verb: "ಹೊಂದಲು"
		},
		map: {
			unit: "entries",
			verb: "ಹೊಂದಲು"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ಇನ್ಪುಟ್",
		email: "email ವಿಳಾಸ",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO ದಿನಾಂಕದ ಸಮಯ",
		date: "ISO ದಿನಾಂಕ",
		time: "ISO ಸಮಯ",
		duration: "ISO ಅವಧಿ",
		ipv4: "IPv4 ವಿಳಾಸ",
		ipv6: "IPv6 ವಿಳಾಸ",
		mac: "MAC ವಿಳಾಸ",
		cidrv4: "IPv4 ವ್ಯಾಪ್ತಿಯ",
		cidrv6: "IPv6 ವ್ಯಾಪ್ತಿಯ",
		base64: "base64-encodedಸ್ಟ್ರಿಂಗ್",
		base64url: "base64url-encodedಸ್ಟ್ರಿಂಗ್",
		json_string: "JSONಸ್ಟ್ರಿಂಗ್",
		e164: "E.164 ಸಂಖ್ಯೆ",
		credit_card: "ಕ್ರೆಡಿಟ್ ಕಾರ್ಡ್ ಸಂಖ್ಯೆ",
		currency_code: "ಕರೆನ್ಸಿ ಕೋಡ್",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ಇನ್ಪುಟ್"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `ಅಮಾನ್ಯ ಇನ್‌ಪುಟ್: ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${t}, ಸ್ವೀಕರಿಸಿದನು ${r[n] ?? n}`;
			}
			case "invalid_value": return e.values.length === 1 ? `ಅಮಾನ್ಯ ಇನ್‌ಪುಟ್: ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${D(e.values[0])}` : `ಅಮಾನ್ಯ ಆಯ್ಕೆ: ಇವುಗಳಲ್ಲಿ ಒಂದನ್ನು ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `ತುಂಬಾ ದೊಡ್ಡದು: ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${e.origin ?? "value"} ಹೊಂದಲು ${n}${e.maximum.toString()} ${r.unit ?? "ಅಂಶಗಳು"}` : `ತುಂಬಾ ದೊಡ್ಡದು: ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${e.origin ?? "value"} ಎಂದು ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `ತುಂಬಾ ಚಿಕ್ಕದು: ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${e.origin} ಹೊಂದಲು ${n}${e.minimum.toString()} ${r.unit}` : `ತುಂಬಾ ಚಿಕ್ಕದು: ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${e.origin} ಎಂದು ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `ಅಮಾನ್ಯವಾದ ಸ್ಟ್ರಿಂಗ್: ಇದರೊಂದಿಗೆ ಪ್ರಾರಂಭಿಸಬೇಕು "${t.prefix}"` : t.format === "ends_with" ? `ಅಮಾನ್ಯವಾದ ಸ್ಟ್ರಿಂಗ್: ಇದರೊಂದಿಗೆ ಕೊನೆಗೊಳ್ಳಬೇಕು "${t.suffix}"` : t.format === "includes" ? `ಅಮಾನ್ಯ ಸ್ಟ್ರಿಂಗ್: ಒಳಗೊಂಡಿರಬೇಕು "${t.includes}"` : t.format === "regex" ? `ಅಮಾನ್ಯವಾದ ಸ್ಟ್ರಿಂಗ್: ಮಾದರಿಗೆ ಹೊಂದಿಕೆಯಾಗಬೇಕು ${t.pattern}` : `ಅಮಾನ್ಯ ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `ಅಮಾನ್ಯ ಸಂಖ್ಯೆ: ಬಹುಸಂಖ್ಯೆಯಾಗಿರಬೇಕು ${e.divisor}`;
			case "unrecognized_keys": return `ಗುರುತಿಸಲಾಗದ ಕೀ ${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `ಅಮಾನ್ಯವಾದ ಕೀ ಇನ್ ${e.origin}`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `ಅಮಾನ್ಯ ತಾರತಮ್ಯ ಮೌಲ್ಯ. ನಿರೀಕ್ಷಿಸಲಾಗಿದೆ ${e.options.map((e) => `'${e}'`).join(" | ")}` : "ಅಮಾನ್ಯ ಇನ್‌ಪುಟ್";
			case "invalid_element": return `ರಲ್ಲಿ ಅಮಾನ್ಯ ಮೌಲ್ಯ ${e.origin}`;
			default: return "ಅಮಾನ್ಯ ಇನ್‌ಪುಟ್";
		}
	};
};
function Yl() {
	return { localeError: Jl() };
}
//#endregion
//#region node_modules/zod/v4/locales/ko.js
var Xl = () => {
	let e = {
		string: {
			unit: "문자",
			verb: "to have"
		},
		file: {
			unit: "바이트",
			verb: "to have"
		},
		array: {
			unit: "개",
			verb: "to have"
		},
		set: {
			unit: "개",
			verb: "to have"
		},
		map: {
			unit: "개",
			verb: "to have"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "입력",
		email: "이메일 주소",
		url: "URL",
		emoji: "이모지",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO 날짜시간",
		date: "ISO 날짜",
		time: "ISO 시간",
		duration: "ISO 기간",
		ipv4: "IPv4 주소",
		ipv6: "IPv6 주소",
		mac: "MAC 주소",
		cidrv4: "IPv4 범위",
		cidrv6: "IPv6 범위",
		base64: "base64 인코딩 문자열",
		base64url: "base64url 인코딩 문자열",
		json_string: "JSON 문자열",
		e164: "E.164 번호",
		credit_card: "신용카드 번호",
		currency_code: "통화 코드",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "입력"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `잘못된 입력: 예상 타입은 instanceof ${e.expected}, 받은 타입은 ${i}입니다` : `잘못된 입력: 예상 타입은 ${t}, 받은 타입은 ${i}입니다`;
			}
			case "invalid_value": return e.values.length === 1 ? `잘못된 입력: 값은 ${D(e.values[0])} 이어야 합니다` : `잘못된 옵션: ${j(e.values, "또는 ")} 중 하나여야 합니다`;
			case "too_big": {
				let n = e.inclusive ? "이하" : "미만", r = n === "미만" ? "이어야 합니다" : "여야 합니다", i = t(e.origin), a = i?.unit ?? "요소";
				return i ? `${e.origin ?? "값"}이 너무 큽니다: ${e.maximum.toString()}${a} ${n}${r}` : `${e.origin ?? "값"}이 너무 큽니다: ${e.maximum.toString()} ${n}${r}`;
			}
			case "too_small": {
				let n = e.inclusive ? "이상" : "초과", r = n === "이상" ? "이어야 합니다" : "여야 합니다", i = t(e.origin), a = i?.unit ?? "요소";
				return i ? `${e.origin ?? "값"}이 너무 작습니다: ${e.minimum.toString()}${a} ${n}${r}` : `${e.origin ?? "값"}이 너무 작습니다: ${e.minimum.toString()} ${n}${r}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `잘못된 문자열: "${t.prefix}"(으)로 시작해야 합니다` : t.format === "ends_with" ? `잘못된 문자열: "${t.suffix}"(으)로 끝나야 합니다` : t.format === "includes" ? `잘못된 문자열: "${t.includes}"을(를) 포함해야 합니다` : t.format === "regex" ? `잘못된 문자열: 정규식 ${t.pattern} 패턴과 일치해야 합니다` : `잘못된 ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `잘못된 숫자: ${e.divisor}의 배수여야 합니다`;
			case "unrecognized_keys": return `인식할 수 없는 키: ${j(e.keys, ", ")}`;
			case "invalid_key": return `잘못된 키: ${e.origin}`;
			case "invalid_union": return "잘못된 입력";
			case "invalid_element": return `잘못된 값: ${e.origin}`;
			default: return "잘못된 입력";
		}
	};
};
function Zl() {
	return { localeError: Xl() };
}
//#endregion
//#region node_modules/zod/v4/locales/lt.js
var Ql = (e) => e.charAt(0).toUpperCase() + e.slice(1);
function $l(e) {
	let t = Math.abs(e), n = t % 10, r = t % 100;
	return r >= 11 && r <= 19 || n === 0 ? "many" : n === 1 ? "one" : "few";
}
var eu = () => {
	let e = {
		string: {
			unit: {
				one: "simbolis",
				few: "simboliai",
				many: "simbolių"
			},
			verb: {
				smaller: {
					inclusive: "turi būti ne ilgesnė kaip",
					notInclusive: "turi būti trumpesnė kaip"
				},
				bigger: {
					inclusive: "turi būti ne trumpesnė kaip",
					notInclusive: "turi būti ilgesnė kaip"
				}
			}
		},
		file: {
			unit: {
				one: "baitas",
				few: "baitai",
				many: "baitų"
			},
			verb: {
				smaller: {
					inclusive: "turi būti ne didesnis kaip",
					notInclusive: "turi būti mažesnis kaip"
				},
				bigger: {
					inclusive: "turi būti ne mažesnis kaip",
					notInclusive: "turi būti didesnis kaip"
				}
			}
		},
		array: {
			unit: {
				one: "elementą",
				few: "elementus",
				many: "elementų"
			},
			verb: {
				smaller: {
					inclusive: "turi turėti ne daugiau kaip",
					notInclusive: "turi turėti mažiau kaip"
				},
				bigger: {
					inclusive: "turi turėti ne mažiau kaip",
					notInclusive: "turi turėti daugiau kaip"
				}
			}
		},
		set: {
			unit: {
				one: "elementą",
				few: "elementus",
				many: "elementų"
			},
			verb: {
				smaller: {
					inclusive: "turi turėti ne daugiau kaip",
					notInclusive: "turi turėti mažiau kaip"
				},
				bigger: {
					inclusive: "turi turėti ne mažiau kaip",
					notInclusive: "turi turėti daugiau kaip"
				}
			}
		}
	};
	function t(t, n, r, i) {
		let a = e[t] ?? null;
		return a === null ? a : {
			unit: a.unit[n],
			verb: a.verb[i][r ? "inclusive" : "notInclusive"]
		};
	}
	let n = {
		regex: "įvestis",
		email: "el. pašto adresas",
		url: "URL",
		emoji: "jaustukas",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO data ir laikas",
		date: "ISO data",
		time: "ISO laikas",
		duration: "ISO trukmė",
		ipv4: "IPv4 adresas",
		ipv6: "IPv6 adresas",
		mac: "MAC adresas",
		cidrv4: "IPv4 tinklo prefiksas (CIDR)",
		cidrv6: "IPv6 tinklo prefiksas (CIDR)",
		base64: "base64 užkoduota eilutė",
		base64url: "base64url užkoduota eilutė",
		json_string: "JSON eilutė",
		e164: "E.164 numeris",
		credit_card: "kredito kortelės numeris",
		currency_code: "valiutos kodas",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "įvestis"
	}, r = {
		nan: "NaN",
		number: "skaičius",
		bigint: "sveikasis skaičius",
		string: "eilutė",
		boolean: "loginė reikšmė",
		undefined: "neapibrėžta reikšmė",
		function: "funkcija",
		symbol: "simbolis",
		array: "masyvas",
		object: "objektas",
		null: "nulinė reikšmė"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Gautas tipas ${i}, o tikėtasi - instanceof ${e.expected}` : `Gautas tipas ${i}, o tikėtasi - ${t}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Privalo būti ${D(e.values[0])}` : `Privalo būti vienas iš ${j(e.values, "|")} pasirinkimų`;
			case "too_big": {
				let n = r[e.origin] ?? e.origin, i = t(e.origin, $l(Number(e.maximum)), e.inclusive ?? !1, "smaller");
				if (i?.verb) return `${Ql(n ?? e.origin ?? "reikšmė")} ${i.verb} ${e.maximum.toString()} ${i.unit ?? "elementų"}`;
				let a = e.inclusive ? "ne didesnis kaip" : "mažesnis kaip";
				return `${Ql(n ?? e.origin ?? "reikšmė")} turi būti ${a} ${e.maximum.toString()} ${i?.unit}`;
			}
			case "too_small": {
				let n = r[e.origin] ?? e.origin, i = t(e.origin, $l(Number(e.minimum)), e.inclusive ?? !1, "bigger");
				if (i?.verb) return `${Ql(n ?? e.origin ?? "reikšmė")} ${i.verb} ${e.minimum.toString()} ${i.unit ?? "elementų"}`;
				let a = e.inclusive ? "ne mažesnis kaip" : "didesnis kaip";
				return `${Ql(n ?? e.origin ?? "reikšmė")} turi būti ${a} ${e.minimum.toString()} ${i?.unit}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Eilutė privalo prasidėti "${t.prefix}"` : t.format === "ends_with" ? `Eilutė privalo pasibaigti "${t.suffix}"` : t.format === "includes" ? `Eilutė privalo įtraukti "${t.includes}"` : t.format === "regex" ? `Eilutė privalo atitikti ${t.pattern}` : `Neteisingas ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Skaičius privalo būti ${e.divisor} kartotinis.`;
			case "unrecognized_keys": return `Neatpažint${e.keys.length > 1 ? "i" : "as"} rakt${e.keys.length > 1 ? "ai" : "as"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return "Rastas klaidingas raktas";
			case "invalid_union": return "Klaidinga įvestis";
			case "invalid_element": return `${Ql(r[e.origin] ?? e.origin ?? e.origin ?? "reikšmė")} turi klaidingą įvestį`;
			default: return "Klaidinga įvestis";
		}
	};
};
function tu() {
	return { localeError: eu() };
}
//#endregion
//#region node_modules/zod/v4/locales/mk.js
var nu = () => {
	let e = {
		string: {
			unit: "знаци",
			verb: "да имаат"
		},
		file: {
			unit: "бајти",
			verb: "да имаат"
		},
		array: {
			unit: "ставки",
			verb: "да имаат"
		},
		set: {
			unit: "ставки",
			verb: "да имаат"
		},
		map: {
			unit: "ставки",
			verb: "да имаат"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "внес",
		email: "адреса на е-пошта",
		url: "URL",
		emoji: "емоџи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO датум и време",
		date: "ISO датум",
		time: "ISO време",
		duration: "ISO времетраење",
		ipv4: "IPv4 адреса",
		ipv6: "IPv6 адреса",
		mac: "MAC адреса",
		cidrv4: "IPv4 опсег",
		cidrv6: "IPv6 опсег",
		base64: "base64-енкодирана низа",
		base64url: "base64url-енкодирана низа",
		json_string: "JSON низа",
		e164: "E.164 број",
		credit_card: "број на кредитна картичка",
		currency_code: "код на валута",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "внес"
	}, r = {
		nan: "NaN",
		number: "број",
		array: "низа"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Грешен внес: се очекува instanceof ${e.expected}, примено ${i}` : `Грешен внес: се очекува ${t}, примено ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Invalid input: expected ${D(e.values[0])}` : `Грешана опција: се очекува една ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Премногу голем: се очекува ${e.origin ?? "вредноста"} да има ${n}${e.maximum.toString()} ${r.unit ?? "елементи"}` : `Премногу голем: се очекува ${e.origin ?? "вредноста"} да биде ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Премногу мал: се очекува ${e.origin} да има ${n}${e.minimum.toString()} ${r.unit}` : `Премногу мал: се очекува ${e.origin} да биде ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Неважечка низа: мора да започнува со "${t.prefix}"` : t.format === "ends_with" ? `Неважечка низа: мора да завршува со "${t.suffix}"` : t.format === "includes" ? `Неважечка низа: мора да вклучува "${t.includes}"` : t.format === "regex" ? `Неважечка низа: мора да одгоара на патернот ${t.pattern}` : `Invalid ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Грешен број: мора да биде делив со ${e.divisor}`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Непрепознаени клучеви" : "Непрепознаен клуч"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Грешен клуч во ${e.origin}`;
			case "invalid_union": return "Грешен внес";
			case "invalid_element": return `Грешна вредност во ${e.origin}`;
			default: return "Грешен внес";
		}
	};
};
function ru() {
	return { localeError: nu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ms.js
var iu = () => {
	let e = {
		string: {
			unit: "aksara",
			verb: "mempunyai"
		},
		file: {
			unit: "bait",
			verb: "mempunyai"
		},
		array: {
			unit: "elemen",
			verb: "mempunyai"
		},
		set: {
			unit: "elemen",
			verb: "mempunyai"
		},
		map: {
			unit: "elemen",
			verb: "mempunyai"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "alamat e-mel",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "tarikh masa ISO",
		date: "tarikh ISO",
		time: "masa ISO",
		duration: "tempoh ISO",
		ipv4: "alamat IPv4",
		ipv6: "alamat IPv6",
		mac: "alamat MAC",
		cidrv4: "julat IPv4",
		cidrv6: "julat IPv6",
		base64: "string dikodkan base64",
		base64url: "string dikodkan base64url",
		json_string: "string JSON",
		e164: "nombor E.164",
		credit_card: "nombor kad kredit",
		currency_code: "kod mata wang",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = {
		nan: "NaN",
		number: "nombor"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Input tidak sah: dijangka instanceof ${e.expected}, diterima ${i}` : `Input tidak sah: dijangka ${t}, diterima ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Input tidak sah: dijangka ${D(e.values[0])}` : `Pilihan tidak sah: dijangka salah satu daripada ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Terlalu besar: dijangka ${e.origin ?? "nilai"} ${r.verb} ${n}${e.maximum.toString()} ${r.unit ?? "elemen"}` : `Terlalu besar: dijangka ${e.origin ?? "nilai"} adalah ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Terlalu kecil: dijangka ${e.origin} ${r.verb} ${n}${e.minimum.toString()} ${r.unit}` : `Terlalu kecil: dijangka ${e.origin} adalah ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `String tidak sah: mesti bermula dengan "${t.prefix}"` : t.format === "ends_with" ? `String tidak sah: mesti berakhir dengan "${t.suffix}"` : t.format === "includes" ? `String tidak sah: mesti mengandungi "${t.includes}"` : t.format === "regex" ? `String tidak sah: mesti sepadan dengan corak ${t.pattern}` : `${n[t.format] ?? e.format} tidak sah`;
			}
			case "not_multiple_of": return `Nombor tidak sah: perlu gandaan ${e.divisor}`;
			case "unrecognized_keys": return `Kunci tidak dikenali: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Kunci tidak sah dalam ${e.origin}`;
			case "invalid_union": return "Input tidak sah";
			case "invalid_element": return `Nilai tidak sah dalam ${e.origin}`;
			default: return "Input tidak sah";
		}
	};
};
function au() {
	return { localeError: iu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ne.js
var ou = () => {
	let e = {
		string: {
			unit: "अक्षर",
			verb: "हुनुपर्छ"
		},
		file: {
			unit: "बाइट",
			verb: "हुनुपर्छ"
		},
		array: {
			unit: "तत्व",
			verb: "हुनुपर्छ"
		},
		set: {
			unit: "तत्व",
			verb: "हुनुपर्छ"
		},
		map: {
			unit: "प्रविष्टि",
			verb: "हुनुपर्छ"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "इनपुट",
		email: "इमेल ठेगाना",
		url: "URL",
		emoji: "इमोजी",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO मिति र समय",
		date: "ISO मिति",
		time: "ISO समय",
		duration: "ISO अवधि",
		ipv4: "IPv4 ठेगाना",
		ipv6: "IPv6 ठेगाना",
		mac: "MAC ठेगाना",
		cidrv4: "IPv4 दायरा",
		cidrv6: "IPv6 दायरा",
		base64: "base64-इन्कोड गरिएको स्ट्रिङ",
		base64url: "base64url-इन्कोड गरिएको स्ट्रिङ",
		json_string: "JSON स्ट्रिङ",
		e164: "E.164 नम्बर",
		credit_card: "क्रेडिट कार्ड नम्बर",
		currency_code: "मुद्रा कोड",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "इनपुट"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `अमान्य इनपुट: अपेक्षित ${t}, प्राप्त ${r[n] ?? n}`;
			}
			case "invalid_value": return e.values.length === 1 ? `अमान्य इनपुट: अपेक्षित ${D(e.values[0])}` : `अमान्य विकल्प: अपेक्षित मानहरू मध्ये एक ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `धेरै ठूलो: ${e.origin ?? "मान"} मा ${n}${e.maximum.toString()} ${r.unit} ${r.verb}` : `धेरै ठूलो: ${e.origin ?? "मान"} ${n}${e.maximum.toString()} हुनुपर्छ`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `धेरै सानो: ${e.origin} मा ${n}${e.minimum.toString()} ${r.unit} ${r.verb}` : `धेरै सानो: ${e.origin} ${n}${e.minimum.toString()} हुनुपर्छ`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `अमान्य स्ट्रिङ: "${t.prefix}" बाट सुरु हुनुपर्छ` : t.format === "ends_with" ? `अमान्य स्ट्रिङ: "${t.suffix}" मा समाप्त हुनुपर्छ` : t.format === "includes" ? `अमान्य स्ट्रिङ: "${t.includes}" समावेश हुनुपर्छ` : t.format === "regex" ? `अमान्य स्ट्रिङ: ढाँचा ${t.pattern} सँग मेल खानुपर्छ` : `अमान्य ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `अमान्य संख्या: ${e.divisor} को गुणज हुनुपर्छ`;
			case "unrecognized_keys": return `अपरिचित कुञ्जी${e.keys.length > 1 ? "हरू" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `अमान्य कुञ्जी: ${e.origin} मा`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `अमान्य डिस्क्रिमिनेटर मान: अपेक्षित ${e.options.map((e) => `'${e}'`).join(" | ")}` : "अमान्य इनपुट";
			case "invalid_element": return `अमान्य मान: ${e.origin} मा`;
			default: return "अमान्य इनपुट";
		}
	};
};
function su() {
	return { localeError: ou() };
}
//#endregion
//#region node_modules/zod/v4/locales/nl.js
var cu = () => {
	let e = {
		string: {
			unit: "tekens",
			verb: "heeft"
		},
		file: {
			unit: "bytes",
			verb: "heeft"
		},
		array: {
			unit: "elementen",
			verb: "heeft"
		},
		set: {
			unit: "elementen",
			verb: "heeft"
		},
		map: {
			unit: "elementen",
			verb: "heeft"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "invoer",
		email: "emailadres",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datum en tijd",
		date: "ISO datum",
		time: "ISO tijd",
		duration: "ISO duur",
		ipv4: "IPv4-adres",
		ipv6: "IPv6-adres",
		mac: "MAC-adres",
		cidrv4: "IPv4-bereik",
		cidrv6: "IPv6-bereik",
		base64: "base64-gecodeerde tekst",
		base64url: "base64 URL-gecodeerde tekst",
		json_string: "JSON string",
		e164: "E.164-nummer",
		credit_card: "creditcardnummer",
		currency_code: "valutacode",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "invoer"
	}, r = {
		nan: "NaN",
		number: "getal"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ongeldige invoer: verwacht instanceof ${e.expected}, ontving ${i}` : `Ongeldige invoer: verwacht ${t}, ontving ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ongeldige invoer: verwacht ${D(e.values[0])}` : `Ongeldige optie: verwacht één van ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin), i = e.origin === "date" ? "laat" : e.origin === "string" ? "lang" : "groot";
				return r ? `Te ${i}: verwacht dat ${e.origin ?? "waarde"} ${n}${e.maximum.toString()} ${r.unit ?? "elementen"} ${r.verb}` : `Te ${i}: verwacht dat ${e.origin ?? "waarde"} ${n}${e.maximum.toString()} is`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin), i = e.origin === "date" ? "vroeg" : e.origin === "string" ? "kort" : "klein";
				return r ? `Te ${i}: verwacht dat ${e.origin} ${n}${e.minimum.toString()} ${r.unit} ${r.verb}` : `Te ${i}: verwacht dat ${e.origin} ${n}${e.minimum.toString()} is`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ongeldige tekst: moet met "${t.prefix}" beginnen` : t.format === "ends_with" ? `Ongeldige tekst: moet op "${t.suffix}" eindigen` : t.format === "includes" ? `Ongeldige tekst: moet "${t.includes}" bevatten` : t.format === "regex" ? `Ongeldige tekst: moet overeenkomen met patroon ${t.pattern}` : `Ongeldig: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Ongeldig getal: moet een veelvoud van ${e.divisor} zijn`;
			case "unrecognized_keys": return `Onbekende key${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Ongeldige key in ${e.origin}`;
			case "invalid_union": return "Ongeldige invoer";
			case "invalid_element": return `Ongeldige waarde in ${e.origin}`;
			default: return "Ongeldige invoer";
		}
	};
};
function lu() {
	return { localeError: cu() };
}
//#endregion
//#region node_modules/zod/v4/locales/nn.js
var uu = () => {
	let e = {
		string: {
			unit: "teikn",
			verb: "å ha"
		},
		file: {
			unit: "bytes",
			verb: "å ha"
		},
		array: {
			unit: "element",
			verb: "å innehalde"
		},
		set: {
			unit: "element",
			verb: "å innehalde"
		},
		map: {
			unit: "element",
			verb: "å innehalde"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "e-postadresse",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dato- og klokkeslett",
		date: "ISO-dato",
		time: "ISO-klokkeslett",
		duration: "ISO-varigheit",
		ipv4: "IPv4-adresse",
		ipv6: "IPv6-adresse",
		mac: "MAC-adresse",
		cidrv4: "IPv4-spekter",
		cidrv6: "IPv6-spekter",
		base64: "base64-enkoda streng",
		base64url: "base64url-enkoda streng",
		json_string: "JSON-streng",
		e164: "E.164-nummer",
		credit_card: "kredittkortnummer",
		currency_code: "valutakode",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = {
		nan: "NaN",
		number: "tal",
		array: "liste"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ugyldig input: forventa instanceof ${e.expected}, fekk ${i}` : `Ugyldig input: forventa ${t}, fekk ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ugyldig verdi: forventa ${D(e.values[0])}` : `Ugyldig val: forventa eitt av ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `For stor(t): forventa ${e.origin ?? "value"} til å ha ${n}${e.maximum.toString()} ${r.unit ?? "element"}` : `For stor(t): forventa ${e.origin ?? "value"} til å ha ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `For lite(n): forventa ${e.origin} til å ha ${n}${e.minimum.toString()} ${r.unit}` : `For lite(n): forventa ${e.origin} til å ha ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ugyldig streng: må starte med "${t.prefix}"` : t.format === "ends_with" ? `Ugyldig streng: må slutte med "${t.suffix}"` : t.format === "includes" ? `Ugyldig streng: må innehalde "${t.includes}"` : t.format === "regex" ? `Ugyldig streng: må matche mønsteret ${t.pattern}` : `Ugyldig ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Ugyldig tal: må vere eit multiplum av ${e.divisor}`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Ukjende nøklar" : "Ukjend nøkkel"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Ugyldig nøkkel i ${e.origin}`;
			case "invalid_union": return "Ugyldig input";
			case "invalid_element": return `Ugyldig verdi i ${e.origin}`;
			default: return "Ugyldig input";
		}
	};
};
function du() {
	return { localeError: uu() };
}
//#endregion
//#region node_modules/zod/v4/locales/no.js
var fu = () => {
	let e = {
		string: {
			unit: "tegn",
			verb: "å ha"
		},
		file: {
			unit: "bytes",
			verb: "å ha"
		},
		array: {
			unit: "elementer",
			verb: "å inneholde"
		},
		set: {
			unit: "elementer",
			verb: "å inneholde"
		},
		map: {
			unit: "elementer",
			verb: "å inneholde"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "input",
		email: "e-postadresse",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO dato- og klokkeslett",
		date: "ISO-dato",
		time: "ISO-klokkeslett",
		duration: "ISO-varighet",
		ipv4: "IPv4-adresse",
		ipv6: "IPv6-adresse",
		mac: "MAC-adresse",
		cidrv4: "IPv4-spekter",
		cidrv6: "IPv6-spekter",
		base64: "base64-enkodet streng",
		base64url: "base64url-enkodet streng",
		json_string: "JSON-streng",
		e164: "E.164-nummer",
		credit_card: "kredittkortnummer",
		currency_code: "valutakode",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = {
		nan: "NaN",
		number: "tall",
		array: "liste"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ugyldig input: forventet instanceof ${e.expected}, fikk ${i}` : `Ugyldig input: forventet ${t}, fikk ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ugyldig verdi: forventet ${D(e.values[0])}` : `Ugyldig valg: forventet en av ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `For stor(t): forventet ${e.origin ?? "value"} til å ha ${n}${e.maximum.toString()} ${r.unit ?? "elementer"}` : `For stor(t): forventet ${e.origin ?? "value"} til å ha ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `For lite(n): forventet ${e.origin} til å ha ${n}${e.minimum.toString()} ${r.unit}` : `For lite(n): forventet ${e.origin} til å ha ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ugyldig streng: må starte med "${t.prefix}"` : t.format === "ends_with" ? `Ugyldig streng: må ende med "${t.suffix}"` : t.format === "includes" ? `Ugyldig streng: må inneholde "${t.includes}"` : t.format === "regex" ? `Ugyldig streng: må matche mønsteret ${t.pattern}` : `Ugyldig ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Ugyldig tall: må være et multiplum av ${e.divisor}`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Ukjente nøkler" : "Ukjent nøkkel"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Ugyldig nøkkel i ${e.origin}`;
			case "invalid_union": return "Ugyldig input";
			case "invalid_element": return `Ugyldig verdi i ${e.origin}`;
			default: return "Ugyldig input";
		}
	};
};
function pu() {
	return { localeError: fu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ota.js
var mu = () => {
	let e = {
		string: {
			unit: "harf",
			verb: "olmalıdır"
		},
		file: {
			unit: "bayt",
			verb: "olmalıdır"
		},
		array: {
			unit: "unsur",
			verb: "olmalıdır"
		},
		set: {
			unit: "unsur",
			verb: "olmalıdır"
		},
		map: {
			unit: "unsur",
			verb: "olmalıdır"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "giren",
		email: "epostagâh",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO hengâmı",
		date: "ISO tarihi",
		time: "ISO zamanı",
		duration: "ISO müddeti",
		ipv4: "IPv4 nişânı",
		ipv6: "IPv6 nişânı",
		mac: "MAC nişânı",
		cidrv4: "IPv4 menzili",
		cidrv6: "IPv6 menzili",
		base64: "base64-şifreli metin",
		base64url: "base64url-şifreli metin",
		json_string: "JSON metin",
		e164: "E.164 sayısı",
		credit_card: "i'tibâr kartı numarası",
		currency_code: "para birimi kodu",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "giren"
	}, r = {
		nan: "NaN",
		number: "numara",
		array: "saf",
		null: "gayb"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Fâsit giren: umulan instanceof ${e.expected}, alınan ${i}` : `Fâsit giren: umulan ${t}, alınan ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Fâsit giren: umulan ${D(e.values[0])}` : `Fâsit tercih: mûteberler ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Fazla büyük: ${e.origin ?? "value"}, ${n}${e.maximum.toString()} ${r.unit ?? "elements"} sahip olmalıydı.` : `Fazla büyük: ${e.origin ?? "value"}, ${n}${e.maximum.toString()} olmalıydı.`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Fazla küçük: ${e.origin}, ${n}${e.minimum.toString()} ${r.unit} sahip olmalıydı.` : `Fazla küçük: ${e.origin}, ${n}${e.minimum.toString()} olmalıydı.`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Fâsit metin: "${t.prefix}" ile başlamalı.` : t.format === "ends_with" ? `Fâsit metin: "${t.suffix}" ile bitmeli.` : t.format === "includes" ? `Fâsit metin: "${t.includes}" ihtivâ etmeli.` : t.format === "regex" ? `Fâsit metin: ${t.pattern} nakşına uymalı.` : `Fâsit ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Fâsit sayı: ${e.divisor} katı olmalıydı.`;
			case "unrecognized_keys": return `Tanınmayan anahtar ${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} için tanınmayan anahtar var.`;
			case "invalid_union": return "Giren tanınamadı.";
			case "invalid_element": return `${e.origin} için tanınmayan kıymet var.`;
			default: return "Kıymet tanınamadı.";
		}
	};
};
function hu() {
	return { localeError: mu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ps.js
var gu = () => {
	let e = {
		string: {
			unit: "توکي",
			verb: "ولري"
		},
		file: {
			unit: "بایټس",
			verb: "ولري"
		},
		array: {
			unit: "توکي",
			verb: "ولري"
		},
		set: {
			unit: "توکي",
			verb: "ولري"
		},
		map: {
			unit: "توکي",
			verb: "ولري"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ورودي",
		email: "بریښنالیک",
		url: "یو آر ال",
		emoji: "ایموجي",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "نیټه او وخت",
		date: "نېټه",
		time: "وخت",
		duration: "موده",
		ipv4: "د IPv4 پته",
		ipv6: "د IPv6 پته",
		mac: "د MAC پته",
		cidrv4: "د IPv4 ساحه",
		cidrv6: "د IPv6 ساحه",
		base64: "base64-encoded متن",
		base64url: "base64url-encoded متن",
		json_string: "JSON متن",
		e164: "د E.164 شمېره",
		credit_card: "د کریډیټ کارت شمیره",
		currency_code: "د اسعارو کوډ",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ورودي"
	}, r = {
		nan: "NaN",
		number: "عدد",
		array: "ارې"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `ناسم ورودي: باید instanceof ${e.expected} وای, مګر ${i} ترلاسه شو` : `ناسم ورودي: باید ${t} وای, مګر ${i} ترلاسه شو`;
			}
			case "invalid_value": return e.values.length === 1 ? `ناسم ورودي: باید ${D(e.values[0])} وای` : `ناسم انتخاب: باید یو له ${j(e.values, "|")} څخه وای`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `ډیر لوی: ${e.origin ?? "ارزښت"} باید ${n}${e.maximum.toString()} ${r.unit ?? "عنصرونه"} ولري` : `ډیر لوی: ${e.origin ?? "ارزښت"} باید ${n}${e.maximum.toString()} وي`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `ډیر کوچنی: ${e.origin} باید ${n}${e.minimum.toString()} ${r.unit} ولري` : `ډیر کوچنی: ${e.origin} باید ${n}${e.minimum.toString()} وي`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `ناسم متن: باید د "${t.prefix}" سره پیل شي` : t.format === "ends_with" ? `ناسم متن: باید د "${t.suffix}" سره پای ته ورسيږي` : t.format === "includes" ? `ناسم متن: باید "${t.includes}" ولري` : t.format === "regex" ? `ناسم متن: باید د ${t.pattern} سره مطابقت ولري` : `${n[t.format] ?? e.format} ناسم دی`;
			}
			case "not_multiple_of": return `ناسم عدد: باید د ${e.divisor} مضرب وي`;
			case "unrecognized_keys": return `ناسم ${e.keys.length > 1 ? "کلیډونه" : "کلیډ"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `ناسم کلیډ په ${e.origin} کې`;
			case "invalid_union": return "ناسمه ورودي";
			case "invalid_element": return `ناسم عنصر په ${e.origin} کې`;
			default: return "ناسمه ورودي";
		}
	};
};
function _u() {
	return { localeError: gu() };
}
//#endregion
//#region node_modules/zod/v4/locales/pl.js
var vu = () => {
	let e = {
		string: {
			unit: "znaków",
			verb: "mieć"
		},
		file: {
			unit: "bajtów",
			verb: "mieć"
		},
		array: {
			unit: "elementów",
			verb: "mieć"
		},
		set: {
			unit: "elementów",
			verb: "mieć"
		},
		map: {
			unit: "elementów",
			verb: "mieć"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "wyrażenie",
		email: "adres email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "data i godzina w formacie ISO",
		date: "data w formacie ISO",
		time: "godzina w formacie ISO",
		duration: "czas trwania ISO",
		ipv4: "adres IPv4",
		ipv6: "adres IPv6",
		mac: "adres MAC",
		cidrv4: "zakres IPv4",
		cidrv6: "zakres IPv6",
		base64: "ciąg znaków zakodowany w formacie base64",
		base64url: "ciąg znaków zakodowany w formacie base64url",
		json_string: "ciąg znaków w formacie JSON",
		e164: "liczba E.164",
		credit_card: "numer karty kredytowej",
		currency_code: "kod waluty",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "wejście"
	}, r = {
		nan: "NaN",
		number: "liczba",
		array: "tablica"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Nieprawidłowe dane wejściowe: oczekiwano instanceof ${e.expected}, otrzymano ${i}` : `Nieprawidłowe dane wejściowe: oczekiwano ${t}, otrzymano ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Nieprawidłowe dane wejściowe: oczekiwano ${D(e.values[0])}` : `Nieprawidłowa opcja: oczekiwano jednej z wartości ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Za duża wartość: oczekiwano, że ${e.origin ?? "wartość"} będzie mieć ${n}${e.maximum.toString()} ${r.unit ?? "elementów"}` : `Zbyt duż(y/a/e): oczekiwano, że ${e.origin ?? "wartość"} będzie wynosić ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Za mała wartość: oczekiwano, że ${e.origin ?? "wartość"} będzie mieć ${n}${e.minimum.toString()} ${r.unit ?? "elementów"}` : `Zbyt mał(y/a/e): oczekiwano, że ${e.origin ?? "wartość"} będzie wynosić ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Nieprawidłowy ciąg znaków: musi zaczynać się od "${t.prefix}"` : t.format === "ends_with" ? `Nieprawidłowy ciąg znaków: musi kończyć się na "${t.suffix}"` : t.format === "includes" ? `Nieprawidłowy ciąg znaków: musi zawierać "${t.includes}"` : t.format === "regex" ? `Nieprawidłowy ciąg znaków: musi odpowiadać wzorcowi ${t.pattern}` : `Nieprawidłow(y/a/e) ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Nieprawidłowa liczba: musi być wielokrotnością ${e.divisor}`;
			case "unrecognized_keys": return `Nierozpoznane klucze${e.keys.length > 1 ? "s" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Nieprawidłowy klucz w ${e.origin}`;
			case "invalid_union": return "Nieprawidłowe dane wejściowe";
			case "invalid_element": return `Nieprawidłowa wartość w ${e.origin}`;
			default: return "Nieprawidłowe dane wejściowe";
		}
	};
};
function yu() {
	return { localeError: vu() };
}
//#endregion
//#region node_modules/zod/v4/locales/pt.js
var bu = () => {
	let e = {
		string: { unit: "caracteres" },
		file: { unit: "bytes" },
		array: { unit: "elementos" },
		set: { unit: "elementos" },
		map: { unit: "entradas" }
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "a entrada",
		email: "o endereço de e-mail",
		url: "o URL",
		emoji: "o emoji",
		uuid: "o UUID",
		uuidv4: "o UUIDv4",
		uuidv6: "o UUIDv6",
		nanoid: "o nanoid",
		guid: "o GUID",
		cuid: "o cuid",
		cuid2: "o cuid2",
		ulid: "o ULID",
		xid: "o XID",
		ksuid: "o KSUID",
		datetime: "a data e hora ISO",
		date: "a data ISO",
		time: "a hora ISO",
		duration: "a duração ISO",
		ipv4: "o endereço IPv4",
		ipv6: "o endereço IPv6",
		mac: "o endereço MAC",
		cidrv4: "o intervalo de endereços IPv4",
		cidrv6: "o intervalo de endereços IPv6",
		base64: "o texto codificado em base64",
		base64url: "o texto codificado em base64url",
		json_string: "o texto JSON",
		e164: "o número E.164",
		credit_card: "o número de cartão de crédito",
		currency_code: "o código de moeda",
		iban: "o IBAN",
		jwt: "o JWT",
		template_literal: "a entrada"
	}, r = {
		masculine: {
			definite: "o",
			indefinite: "um"
		},
		feminine: {
			definite: "a",
			indefinite: "uma"
		}
	}, i = {
		string: {
			name: "texto",
			articles: r.masculine
		},
		number: {
			name: "número",
			articles: r.masculine
		},
		int: {
			name: "número inteiro",
			articles: r.masculine
		},
		boolean: {
			name: "valor booleano",
			articles: r.masculine
		},
		bigint: {
			name: "número bigint",
			articles: r.masculine
		},
		symbol: {
			name: "símbolo",
			articles: r.masculine
		},
		undefined: {
			name: "valor \"undefined\"",
			articles: r.masculine
		},
		null: {
			name: "valor \"nulo\"",
			articles: r.masculine
		},
		never: {
			name: "valor \"never\"",
			articles: r.masculine
		},
		void: {
			name: "valor \"void\"",
			articles: r.masculine
		},
		date: {
			name: "data",
			articles: r.feminine
		},
		array: {
			name: "vetor",
			articles: r.masculine
		},
		object: {
			name: "objeto",
			articles: r.masculine
		},
		tuple: {
			name: "tuplo",
			articles: r.masculine
		},
		record: {
			name: "registo",
			articles: r.masculine
		},
		map: {
			name: "mapa",
			articles: r.masculine
		},
		set: {
			name: "conjunto",
			articles: r.masculine
		},
		file: {
			name: "ficheiro",
			articles: r.masculine
		},
		nonoptional: {
			name: "valor não opcional",
			articles: r.masculine
		},
		nan: {
			name: "valor \"NaN\"",
			articles: r.masculine
		},
		function: {
			name: "função",
			articles: r.feminine
		}
	};
	function a(e, t) {
		let n = i[e] ?? {
			name: `valor "${e}"`,
			articles: r.masculine
		};
		return `${n.articles[t]} ${n.name}`;
	}
	return (e) => {
		switch (e.code) {
			case "invalid_type": return `Entrada inválida: esperava ${a(e.expected, "indefinite")}, recebeu ${a(E(e.input), "indefinite")}`;
			case "invalid_value": return e.values.length === 1 ? `Entrada inválida: esperava ${D(e.values[0])}` : `Opção inválida: esperava uma das seguintes opções: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Demasiado grande: esperava que ${a(e.origin, "definite")} tivesse ${n} ${e.maximum.toString()} ${r.unit ?? "elementos"}` : `Demasiado grande: esperava que ${a(e.origin, "definite")} fosse ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Demasiado pequeno: esperava que ${a(e.origin, "definite")} tivesse ${n} ${e.minimum.toString()} ${r.unit ?? "elementos"}` : `Demasiado pequeno: esperava que ${a(e.origin, "definite")} fosse ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Texto inválido: deve começar por "${t.prefix}"` : t.format === "ends_with" ? `Texto inválido: deve terminar em "${t.suffix}"` : t.format === "includes" ? `Texto inválido: deve incluir "${t.includes}"` : t.format === "regex" ? `Texto inválido: deve corresponder ao padrão ${t.pattern}` : `Formato d${n[t.format] ?? e.format} inválido`;
			}
			case "not_multiple_of": return `Número inválido: deve ser múltiplo de ${e.divisor}`;
			case "unrecognized_keys": {
				let t = e.keys.length > 1 ? "s" : "";
				return `Chave${t} inválida${t}: ${j(e.keys, ", ")}`;
			}
			case "invalid_key": return `Entrada inválida n${a(e.origin, "definite")}`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `Valor de discriminação inválido. Esperava ${e.options.map((e) => `'${e}'`).join(" | ")}` : "Entrada inválida";
			case "invalid_element": return `Entrada inválida n${a(e.origin, "definite")}`;
			default: return "Entrada inválida";
		}
	};
};
function xu() {
	return { localeError: bu() };
}
//#endregion
//#region node_modules/zod/v4/locales/pt-BR.js
var Su = () => {
	let e = {
		string: { unit: "caracteres" },
		file: { unit: "bytes" },
		array: { unit: "elementos" },
		set: { unit: "elementos" },
		map: { unit: "entradas" }
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "a entrada",
		email: "o endereço de e-mail",
		url: "o URL",
		emoji: "o emoji",
		uuid: "o UUID",
		uuidv4: "o UUIDv4",
		uuidv6: "o UUIDv6",
		nanoid: "o nanoid",
		guid: "o GUID",
		cuid: "o cuid",
		cuid2: "o cuid2",
		ulid: "o ULID",
		xid: "o XID",
		ksuid: "o KSUID",
		datetime: "a data e hora ISO",
		date: "a data ISO",
		time: "a hora ISO",
		duration: "a duração ISO",
		ipv4: "o endereço IPv4",
		ipv6: "o endereço IPv6",
		mac: "o endereço MAC",
		cidrv4: "a faixa de endereços IPv4",
		cidrv6: "a faixa de endereços IPv6",
		base64: "o texto codificado em base64",
		base64url: "o texto codificado em base64url",
		json_string: "o texto JSON",
		e164: "o número E.164",
		credit_card: "o número de cartão de crédito",
		currency_code: "o código de moeda",
		iban: "o IBAN",
		jwt: "o JWT",
		template_literal: "a entrada"
	}, r = {
		masculine: {
			definite: "o",
			indefinite: "um"
		},
		feminine: {
			definite: "a",
			indefinite: "uma"
		}
	}, i = {
		string: {
			name: "texto",
			articles: r.masculine
		},
		number: {
			name: "número",
			articles: r.masculine
		},
		int: {
			name: "número inteiro",
			articles: r.masculine
		},
		boolean: {
			name: "valor booleano",
			articles: r.masculine
		},
		bigint: {
			name: "número bigint",
			articles: r.masculine
		},
		symbol: {
			name: "símbolo",
			articles: r.masculine
		},
		undefined: {
			name: "valor \"undefined\"",
			articles: r.masculine
		},
		null: {
			name: "valor \"nulo\"",
			articles: r.masculine
		},
		never: {
			name: "valor \"never\"",
			articles: r.masculine
		},
		void: {
			name: "valor \"void\"",
			articles: r.masculine
		},
		date: {
			name: "data",
			articles: r.feminine
		},
		array: {
			name: "vetor",
			articles: r.masculine
		},
		object: {
			name: "objeto",
			articles: r.masculine
		},
		tuple: {
			name: "tupla",
			articles: r.feminine
		},
		record: {
			name: "registro",
			articles: r.masculine
		},
		map: {
			name: "mapa",
			articles: r.masculine
		},
		set: {
			name: "conjunto",
			articles: r.masculine
		},
		file: {
			name: "arquivo",
			articles: r.masculine
		},
		nonoptional: {
			name: "valor não opcional",
			articles: r.masculine
		},
		nan: {
			name: "valor \"NaN\"",
			articles: r.masculine
		},
		function: {
			name: "função",
			articles: r.feminine
		}
	};
	function a(e, t) {
		let n = i[e] ?? {
			name: `valor "${e}"`,
			articles: r.masculine
		};
		return `${n.articles[t]} ${n.name}`;
	}
	return (e) => {
		switch (e.code) {
			case "invalid_type": return `Entrada inválida: esperava ${a(e.expected, "indefinite")}, recebeu ${a(E(e.input), "indefinite")}`;
			case "invalid_value": return e.values.length === 1 ? `Entrada inválida: esperava ${D(e.values[0])}` : `Opção inválida: esperava uma das seguintes opções: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Grande demais: esperava que ${a(e.origin, "definite")} tivesse ${n} ${e.maximum.toString()} ${r.unit ?? "elementos"}` : `Grande demais: esperava que ${a(e.origin, "definite")} fosse ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Pequeno demais: esperava que ${a(e.origin, "definite")} tivesse ${n} ${e.minimum.toString()} ${r.unit ?? "elementos"}` : `Pequeno demais: esperava que ${a(e.origin, "definite")} fosse ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Texto inválido: deve começar com "${t.prefix}"` : t.format === "ends_with" ? `Texto inválido: deve terminar com "${t.suffix}"` : t.format === "includes" ? `Texto inválido: deve incluir "${t.includes}"` : t.format === "regex" ? `Texto inválido: deve corresponder ao padrão ${t.pattern}` : `Formato d${n[t.format] ?? e.format} inválido`;
			}
			case "not_multiple_of": return `Número inválido: deve ser múltiplo de ${e.divisor}`;
			case "unrecognized_keys": {
				let t = e.keys.length > 1 ? "s" : "";
				return `Chave${t} inválida${t}: ${j(e.keys, ", ")}`;
			}
			case "invalid_key": return `Entrada inválida n${a(e.origin, "definite")}`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `Valor de discriminação inválido. Esperava ${e.options.map((e) => `'${e}'`).join(" | ")}` : "Entrada inválida";
			case "invalid_element": return `Entrada inválida n${a(e.origin, "definite")}`;
			default: return "Entrada inválida";
		}
	};
};
function Cu() {
	return { localeError: Su() };
}
//#endregion
//#region node_modules/zod/v4/locales/ro.js
var wu = () => {
	let e = {
		string: {
			unit: "caractere",
			verb: "să aibă"
		},
		file: {
			unit: "octeți",
			verb: "să aibă"
		},
		array: {
			unit: "elemente",
			verb: "să aibă"
		},
		set: {
			unit: "elemente",
			verb: "să aibă"
		},
		map: {
			unit: "intrări",
			verb: "să aibă"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "intrare",
		email: "adresă de email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "dată și oră ISO",
		date: "dată ISO",
		time: "oră ISO",
		duration: "durată ISO",
		ipv4: "adresă IPv4",
		ipv6: "adresă IPv6",
		mac: "adresă MAC",
		cidrv4: "interval IPv4",
		cidrv6: "interval IPv6",
		base64: "șir codat base64",
		base64url: "șir codat base64url",
		json_string: "șir JSON",
		e164: "număr E.164",
		credit_card: "număr de card de credit",
		currency_code: "cod valutar",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "intrare"
	}, r = {
		nan: "NaN",
		string: "șir",
		number: "număr",
		boolean: "boolean",
		function: "funcție",
		array: "matrice",
		object: "obiect",
		undefined: "nedefinit",
		symbol: "simbol",
		bigint: "număr mare",
		void: "void",
		never: "never",
		map: "hartă",
		set: "set"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `Intrare invalidă: așteptat ${t}, primit ${r[n] ?? n}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Intrare invalidă: așteptat ${D(e.values[0])}` : `Opțiune invalidă: așteptat una dintre ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Prea mare: așteptat ca ${e.origin ?? "valoarea"} ${r.verb} ${n}${e.maximum.toString()} ${r.unit ?? "elemente"}` : `Prea mare: așteptat ca ${e.origin ?? "valoarea"} să fie ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Prea mic: așteptat ca ${e.origin} ${r.verb} ${n}${e.minimum.toString()} ${r.unit}` : `Prea mic: așteptat ca ${e.origin} să fie ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Șir invalid: trebuie să înceapă cu "${t.prefix}"` : t.format === "ends_with" ? `Șir invalid: trebuie să se termine cu "${t.suffix}"` : t.format === "includes" ? `Șir invalid: trebuie să includă "${t.includes}"` : t.format === "regex" ? `Șir invalid: trebuie să se potrivească cu modelul ${t.pattern}` : `Format invalid: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Număr invalid: trebuie să fie multiplu de ${e.divisor}`;
			case "unrecognized_keys": return `Chei nerecunoscute: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Cheie invalidă în ${e.origin}`;
			case "invalid_union": return "Intrare invalidă";
			case "invalid_element": return `Valoare invalidă în ${e.origin}`;
			default: return "Intrare invalidă";
		}
	};
};
function Tu() {
	return { localeError: wu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ru.js
function Eu(e, t, n, r) {
	let i = Math.abs(e), a = i % 10, o = i % 100;
	return o >= 11 && o <= 19 ? r : a === 1 ? t : a >= 2 && a <= 4 ? n : r;
}
var Du = () => {
	let e = {
		string: {
			unit: {
				one: "символ",
				few: "символа",
				many: "символов"
			},
			verb: "иметь"
		},
		file: {
			unit: {
				one: "байт",
				few: "байта",
				many: "байт"
			},
			verb: "иметь"
		},
		array: {
			unit: {
				one: "элемент",
				few: "элемента",
				many: "элементов"
			},
			verb: "иметь"
		},
		set: {
			unit: {
				one: "элемент",
				few: "элемента",
				many: "элементов"
			},
			verb: "иметь"
		},
		map: {
			unit: {
				one: "элемент",
				few: "элемента",
				many: "элементов"
			},
			verb: "иметь"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ввод",
		email: "email адрес",
		url: "URL",
		emoji: "эмодзи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO дата и время",
		date: "ISO дата",
		time: "ISO время",
		duration: "ISO длительность",
		ipv4: "IPv4 адрес",
		ipv6: "IPv6 адрес",
		mac: "MAC адрес",
		cidrv4: "IPv4 диапазон",
		cidrv6: "IPv6 диапазон",
		base64: "строка в формате base64",
		base64url: "строка в формате base64url",
		json_string: "JSON строка",
		e164: "номер E.164",
		credit_card: "номер кредитной карты",
		currency_code: "код валюты",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ввод"
	}, r = {
		nan: "NaN",
		number: "число",
		array: "массив"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Неверный ввод: ожидалось instanceof ${e.expected}, получено ${i}` : `Неверный ввод: ожидалось ${t}, получено ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Неверный ввод: ожидалось ${D(e.values[0])}` : `Неверный вариант: ожидалось одно из ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				if (r) {
					let t = Eu(Number(e.maximum), r.unit.one, r.unit.few, r.unit.many);
					return `Слишком большое значение: ожидалось, что ${e.origin ?? "значение"} будет иметь ${n}${e.maximum.toString()} ${t}`;
				}
				return `Слишком большое значение: ожидалось, что ${e.origin ?? "значение"} будет ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				if (r) {
					let t = Eu(Number(e.minimum), r.unit.one, r.unit.few, r.unit.many);
					return `Слишком маленькое значение: ожидалось, что ${e.origin} будет иметь ${n}${e.minimum.toString()} ${t}`;
				}
				return `Слишком маленькое значение: ожидалось, что ${e.origin} будет ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Неверная строка: должна начинаться с "${t.prefix}"` : t.format === "ends_with" ? `Неверная строка: должна заканчиваться на "${t.suffix}"` : t.format === "includes" ? `Неверная строка: должна содержать "${t.includes}"` : t.format === "regex" ? `Неверная строка: должна соответствовать шаблону ${t.pattern}` : `Неверный ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Неверное число: должно быть кратным ${e.divisor}`;
			case "unrecognized_keys": return `Нераспознанн${e.keys.length > 1 ? "ые" : "ый"} ключ${e.keys.length > 1 ? "и" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Неверный ключ в ${e.origin}`;
			case "invalid_union": return "Неверные входные данные";
			case "invalid_element": return `Неверное значение в ${e.origin}`;
			default: return "Неверные входные данные";
		}
	};
};
function Ou() {
	return { localeError: Du() };
}
//#endregion
//#region node_modules/zod/v4/locales/sk.js
var ku = () => {
	let e = {
		string: {
			unit: "znakov",
			verb: "mať"
		},
		file: {
			unit: "bajtov",
			verb: "mať"
		},
		array: {
			unit: "prvkov",
			verb: "mať"
		},
		set: {
			unit: "prvkov",
			verb: "mať"
		},
		map: {
			unit: "položiek",
			verb: "mať"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "regulárny výraz",
		email: "e-mailová adresa",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "dátum a čas vo formáte ISO",
		date: "dátum vo formáte ISO",
		time: "čas vo formáte ISO",
		duration: "doba trvania ISO",
		ipv4: "IPv4 adresa",
		ipv6: "IPv6 adresa",
		mac: "MAC adresa",
		cidrv4: "rozsah IPv4",
		cidrv6: "rozsah IPv6",
		base64: "reťazec zakódovaný vo formáte base64",
		base64url: "reťazec zakódovaný vo formáte base64url",
		json_string: "reťazec vo formáte JSON",
		e164: "číslo E.164",
		credit_card: "číslo kreditnej karty",
		currency_code: "kód meny",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "vstup"
	}, r = {
		nan: "NaN",
		number: "číslo",
		string: "reťazec",
		function: "funkcia",
		array: "pole"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Neplatný vstup: očakávané instanceof ${e.expected}, obdržané ${i}` : `Neplatný vstup: očakávané ${t}, obdržané ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Neplatný vstup: očakávané ${D(e.values[0])}` : `Neplatný vstup: očakávaná jedna z hodnôt ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Hodnota je príliš veľká: ${e.origin ?? "hodnota"} musí mať ${n}${e.maximum.toString()} ${r.unit ?? "prvkov"}` : `Hodnota je príliš veľká: ${e.origin ?? "hodnota"} musí byť ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Hodnota je príliš malá: ${e.origin ?? "hodnota"} musí mať ${n}${e.minimum.toString()} ${r.unit ?? "prvkov"}` : `Hodnota je príliš malá: ${e.origin ?? "hodnota"} musí byť ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Neplatný reťazec: musí začínať na "${t.prefix}"` : t.format === "ends_with" ? `Neplatný reťazec: musí končiť na "${t.suffix}"` : t.format === "includes" ? `Neplatný reťazec: musí obsahovať "${t.includes}"` : t.format === "regex" ? `Neplatný reťazec: musí zodpovedať vzoru ${t.pattern}` : `Neplatný formát ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Neplatné číslo: musí byť násobkom ${e.divisor}`;
			case "unrecognized_keys": return `Neznáme klúče: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Neplatný klúč v ${e.origin}`;
			case "invalid_union": return "Neplatný vstup";
			case "invalid_element": return `Neplatná hodnota v ${e.origin}`;
			default: return "Neplatný vstup";
		}
	};
};
function Au() {
	return { localeError: ku() };
}
//#endregion
//#region node_modules/zod/v4/locales/sl.js
var ju = () => {
	let e = {
		string: {
			unit: "znakov",
			verb: "imeti"
		},
		file: {
			unit: "bajtov",
			verb: "imeti"
		},
		array: {
			unit: "elementov",
			verb: "imeti"
		},
		set: {
			unit: "elementov",
			verb: "imeti"
		},
		map: {
			unit: "elementov",
			verb: "imeti"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "vnos",
		email: "e-poštni naslov",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO datum in čas",
		date: "ISO datum",
		time: "ISO čas",
		duration: "ISO trajanje",
		ipv4: "IPv4 naslov",
		ipv6: "IPv6 naslov",
		mac: "MAC naslov",
		cidrv4: "obseg IPv4",
		cidrv6: "obseg IPv6",
		base64: "base64 kodiran niz",
		base64url: "base64url kodiran niz",
		json_string: "JSON niz",
		e164: "E.164 številka",
		credit_card: "številka kreditne kartice",
		currency_code: "koda valute",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "vnos"
	}, r = {
		nan: "NaN",
		number: "število",
		array: "tabela"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Neveljaven vnos: pričakovano instanceof ${e.expected}, prejeto ${i}` : `Neveljaven vnos: pričakovano ${t}, prejeto ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Neveljaven vnos: pričakovano ${D(e.values[0])}` : `Neveljavna možnost: pričakovano eno izmed ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Preveliko: pričakovano, da bo ${e.origin ?? "vrednost"} imelo ${n}${e.maximum.toString()} ${r.unit ?? "elementov"}` : `Preveliko: pričakovano, da bo ${e.origin ?? "vrednost"} ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Premajhno: pričakovano, da bo ${e.origin} imelo ${n}${e.minimum.toString()} ${r.unit}` : `Premajhno: pričakovano, da bo ${e.origin} ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Neveljaven niz: mora se začeti z "${t.prefix}"` : t.format === "ends_with" ? `Neveljaven niz: mora se končati z "${t.suffix}"` : t.format === "includes" ? `Neveljaven niz: mora vsebovati "${t.includes}"` : t.format === "regex" ? `Neveljaven niz: mora ustrezati vzorcu ${t.pattern}` : `Neveljaven ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Neveljavno število: mora biti večkratnik ${e.divisor}`;
			case "unrecognized_keys": return `Neprepoznan${e.keys.length > 1 ? "i ključi" : " ključ"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Neveljaven ključ v ${e.origin}`;
			case "invalid_union": return "Neveljaven vnos";
			case "invalid_element": return `Neveljavna vrednost v ${e.origin}`;
			default: return "Neveljaven vnos";
		}
	};
};
function Mu() {
	return { localeError: ju() };
}
//#endregion
//#region node_modules/zod/v4/locales/sv.js
var Nu = () => {
	let e = {
		string: {
			unit: "tecken",
			verb: "att ha"
		},
		file: {
			unit: "bytes",
			verb: "att ha"
		},
		array: {
			unit: "objekt",
			verb: "att innehålla"
		},
		set: {
			unit: "objekt",
			verb: "att innehålla"
		},
		map: {
			unit: "objekt",
			verb: "att innehålla"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "reguljärt uttryck",
		email: "e-postadress",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO-datum och tid",
		date: "ISO-datum",
		time: "ISO-tid",
		duration: "ISO-varaktighet",
		ipv4: "IPv4-adress",
		ipv6: "IPv6-adress",
		mac: "MAC-adress",
		cidrv4: "IPv4-spektrum",
		cidrv6: "IPv6-spektrum",
		base64: "base64-kodad sträng",
		base64url: "base64url-kodad sträng",
		json_string: "JSON-sträng",
		e164: "E.164-nummer",
		credit_card: "kreditkortsnummer",
		currency_code: "valutakod",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "mall-literal"
	}, r = {
		nan: "NaN",
		number: "antal",
		array: "lista"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ogiltig inmatning: förväntat instanceof ${e.expected}, fick ${i}` : `Ogiltig inmatning: förväntat ${t}, fick ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ogiltig inmatning: förväntat ${D(e.values[0])}` : `Ogiltigt val: förväntade en av ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `För stor(t): förväntade ${e.origin ?? "värdet"} att ha ${n}${e.maximum.toString()} ${r.unit ?? "element"}` : `För stor(t): förväntat ${e.origin ?? "värdet"} att ha ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `För lite(t): förväntade ${e.origin ?? "värdet"} att ha ${n}${e.minimum.toString()} ${r.unit}` : `För lite(t): förväntade ${e.origin ?? "värdet"} att ha ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ogiltig sträng: måste börja med "${t.prefix}"` : t.format === "ends_with" ? `Ogiltig sträng: måste sluta med "${t.suffix}"` : t.format === "includes" ? `Ogiltig sträng: måste innehålla "${t.includes}"` : t.format === "regex" ? `Ogiltig sträng: måste matcha mönstret "${t.pattern}"` : `Ogiltig(t) ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Ogiltigt tal: måste vara en multipel av ${e.divisor}`;
			case "unrecognized_keys": return `${e.keys.length > 1 ? "Okända nycklar" : "Okänd nyckel"}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Ogiltig nyckel i ${e.origin ?? "värdet"}`;
			case "invalid_union": return "Ogiltig input";
			case "invalid_element": return `Ogiltigt värde i ${e.origin ?? "värdet"}`;
			default: return "Ogiltig input";
		}
	};
};
function Pu() {
	return { localeError: Nu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ta.js
var Fu = () => {
	let e = {
		string: {
			unit: "எழுத்துக்கள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		file: {
			unit: "பைட்டுகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		array: {
			unit: "உறுப்புகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		set: {
			unit: "உறுப்புகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		},
		map: {
			unit: "உறுப்புகள்",
			verb: "கொண்டிருக்க வேண்டும்"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "உள்ளீடு",
		email: "மின்னஞ்சல் முகவரி",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO தேதி நேரம்",
		date: "ISO தேதி",
		time: "ISO நேரம்",
		duration: "ISO கால அளவு",
		ipv4: "IPv4 முகவரி",
		ipv6: "IPv6 முகவரி",
		mac: "MAC முகவரி",
		cidrv4: "IPv4 வரம்பு",
		cidrv6: "IPv6 வரம்பு",
		base64: "base64-encoded சரம்",
		base64url: "base64url-encoded சரம்",
		json_string: "JSON சரம்",
		e164: "E.164 எண்",
		credit_card: "கடன் அட்டை எண்",
		currency_code: "நாணயக் குறியீடு",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "input"
	}, r = {
		nan: "NaN",
		number: "எண்",
		array: "அணி",
		null: "வெறுமை"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `தவறான உள்ளீடு: எதிர்பார்க்கப்பட்டது instanceof ${e.expected}, பெறப்பட்டது ${i}` : `தவறான உள்ளீடு: எதிர்பார்க்கப்பட்டது ${t}, பெறப்பட்டது ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `தவறான உள்ளீடு: எதிர்பார்க்கப்பட்டது ${D(e.values[0])}` : `தவறான விருப்பம்: எதிர்பார்க்கப்பட்டது ${j(e.values, "|")} இல் ஒன்று`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `மிக பெரியது: எதிர்பார்க்கப்பட்டது ${e.origin ?? "மதிப்பு"} ${n}${e.maximum.toString()} ${r.unit ?? "உறுப்புகள்"} ஆக இருக்க வேண்டும்` : `மிக பெரியது: எதிர்பார்க்கப்பட்டது ${e.origin ?? "மதிப்பு"} ${n}${e.maximum.toString()} ஆக இருக்க வேண்டும்`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `மிகச் சிறியது: எதிர்பார்க்கப்பட்டது ${e.origin} ${n}${e.minimum.toString()} ${r.unit} ஆக இருக்க வேண்டும்` : `மிகச் சிறியது: எதிர்பார்க்கப்பட்டது ${e.origin} ${n}${e.minimum.toString()} ஆக இருக்க வேண்டும்`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `தவறான சரம்: "${t.prefix}" இல் தொடங்க வேண்டும்` : t.format === "ends_with" ? `தவறான சரம்: "${t.suffix}" இல் முடிவடைய வேண்டும்` : t.format === "includes" ? `தவறான சரம்: "${t.includes}" ஐ உள்ளடக்க வேண்டும்` : t.format === "regex" ? `தவறான சரம்: ${t.pattern} முறைபாட்டுடன் பொருந்த வேண்டும்` : `தவறான ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `தவறான எண்: ${e.divisor} இன் பலமாக இருக்க வேண்டும்`;
			case "unrecognized_keys": return `அடையாளம் தெரியாத விசை${e.keys.length > 1 ? "கள்" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} இல் தவறான விசை`;
			case "invalid_union": return "தவறான உள்ளீடு";
			case "invalid_element": return `${e.origin} இல் தவறான மதிப்பு`;
			default: return "தவறான உள்ளீடு";
		}
	};
};
function Iu() {
	return { localeError: Fu() };
}
//#endregion
//#region node_modules/zod/v4/locales/tg.js
var Lu = () => {
	let e = {
		string: {
			unit: "аломат",
			verb: "дошта бошад"
		},
		file: {
			unit: "байт",
			verb: "дошта бошад"
		},
		array: {
			unit: "унсур",
			verb: "дошта бошад"
		},
		set: {
			unit: "унсур",
			verb: "дошта бошад"
		},
		map: {
			unit: "сабт",
			verb: "дошта бошад"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "вуруд",
		email: "суроғаи email",
		url: "URL",
		emoji: "эмоҷи",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "санаву вақти ISO",
		date: "санаи ISO",
		time: "вақти ISO",
		duration: "давомнокии ISO",
		ipv4: "суроғаи IPv4",
		ipv6: "суроғаи IPv6",
		mac: "суроғаи MAC",
		cidrv4: "маҳдудаи IPv4",
		cidrv6: "маҳдудаи IPv6",
		base64: "сатри дар формати base64",
		base64url: "сатри дар формати base64url",
		json_string: "сатри JSON",
		e164: "рақами E.164",
		credit_card: "рақами корти кредитӣ",
		currency_code: "рамзи асъор",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "вуруд"
	}, r = {
		nan: "NaN",
		number: "рақам",
		string: "сатр",
		array: "массив",
		object: "объект",
		date: "сана"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `Вуруди нодуруст: ${t} интизор мерафт, ${r[n] ?? n} гирифта шуд`;
			}
			case "invalid_value": return e.values.length === 1 ? `Вуруди нодуруст: ${D(e.values[0])} интизор мерафт` : `Интихоби нодуруст: яке аз ${j(e.values, "|")} интизор мерафт`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Хеле калон: ${e.origin ?? "қимат"} бояд ${n}${e.maximum.toString()} ${r.unit} ${r.verb}` : `Хеле калон: ${e.origin ?? "қимат"} бояд ${n}${e.maximum.toString()} бошад`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Хеле хурд: ${e.origin} бояд ${n}${e.minimum.toString()} ${r.unit} ${r.verb}` : `Хеле хурд: ${e.origin} бояд ${n}${e.minimum.toString()} бошад`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Сатри нодуруст: бояд бо "${t.prefix}" оғоз шавад` : t.format === "ends_with" ? `Сатри нодуруст: бояд бо "${t.suffix}" анҷом ёбад` : t.format === "includes" ? `Сатри нодуруст: бояд "${t.includes}"-ро дар бар гирад` : t.format === "regex" ? `Сатри нодуруст: бояд ба намунаи ${t.pattern} мувофиқат кунад` : `${n[t.format] ?? e.format}-и нодуруст`;
			}
			case "not_multiple_of": return `Рақами нодуруст: бояд ба ${e.divisor} бе бақия тақсим шавад`;
			case "unrecognized_keys": return `Калид${e.keys.length > 1 ? "ҳои" : "и"} номаълум: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Калиди нодуруст дар ${e.origin}`;
			case "invalid_union": return e.options && Array.isArray(e.options) && e.options.length > 0 ? `Қимати нодурусти дискриминатор: ${e.options.map((e) => `'${e}'`).join(" | ")} интизор мерафт` : "Вуруди нодуруст";
			case "invalid_element": return `Қимати нодуруст дар ${e.origin}`;
			default: return "Вуруди нодуруст";
		}
	};
};
function Ru() {
	return { localeError: Lu() };
}
//#endregion
//#region node_modules/zod/v4/locales/th.js
var zu = () => {
	let e = {
		string: {
			unit: "ตัวอักษร",
			verb: "ควรมี"
		},
		file: {
			unit: "ไบต์",
			verb: "ควรมี"
		},
		array: {
			unit: "รายการ",
			verb: "ควรมี"
		},
		set: {
			unit: "รายการ",
			verb: "ควรมี"
		},
		map: {
			unit: "รายการ",
			verb: "ควรมี"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ข้อมูลที่ป้อน",
		email: "ที่อยู่อีเมล",
		url: "URL",
		emoji: "อิโมจิ",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "วันที่เวลาแบบ ISO",
		date: "วันที่แบบ ISO",
		time: "เวลาแบบ ISO",
		duration: "ช่วงเวลาแบบ ISO",
		ipv4: "ที่อยู่ IPv4",
		ipv6: "ที่อยู่ IPv6",
		mac: "ที่อยู่ MAC",
		cidrv4: "ช่วง IP แบบ IPv4",
		cidrv6: "ช่วง IP แบบ IPv6",
		base64: "ข้อความแบบ Base64",
		base64url: "ข้อความแบบ Base64 สำหรับ URL",
		json_string: "ข้อความแบบ JSON",
		e164: "เบอร์โทรศัพท์ระหว่างประเทศ (E.164)",
		credit_card: "หมายเลขบัตรเครดิต",
		currency_code: "รหัสสกุลเงิน",
		iban: "IBAN",
		jwt: "โทเคน JWT",
		template_literal: "ข้อมูลที่ป้อน"
	}, r = {
		nan: "NaN",
		number: "ตัวเลข",
		array: "อาร์เรย์ (Array)",
		null: "ไม่มีค่า (null)"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `ประเภทข้อมูลไม่ถูกต้อง: ควรเป็น instanceof ${e.expected} แต่ได้รับ ${i}` : `ประเภทข้อมูลไม่ถูกต้อง: ควรเป็น ${t} แต่ได้รับ ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `ค่าไม่ถูกต้อง: ควรเป็น ${D(e.values[0])}` : `ตัวเลือกไม่ถูกต้อง: ควรเป็นหนึ่งใน ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "ไม่เกิน" : "น้อยกว่า", r = t(e.origin);
				return r ? `เกินกำหนด: ${e.origin ?? "ค่า"} ควรมี${n} ${e.maximum.toString()} ${r.unit ?? "รายการ"}` : `เกินกำหนด: ${e.origin ?? "ค่า"} ควรมี${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? "อย่างน้อย" : "มากกว่า", r = t(e.origin);
				return r ? `น้อยกว่ากำหนด: ${e.origin} ควรมี${n} ${e.minimum.toString()} ${r.unit}` : `น้อยกว่ากำหนด: ${e.origin} ควรมี${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `รูปแบบไม่ถูกต้อง: ข้อความต้องขึ้นต้นด้วย "${t.prefix}"` : t.format === "ends_with" ? `รูปแบบไม่ถูกต้อง: ข้อความต้องลงท้ายด้วย "${t.suffix}"` : t.format === "includes" ? `รูปแบบไม่ถูกต้อง: ข้อความต้องมี "${t.includes}" อยู่ในข้อความ` : t.format === "regex" ? `รูปแบบไม่ถูกต้อง: ต้องตรงกับรูปแบบที่กำหนด ${t.pattern}` : `รูปแบบไม่ถูกต้อง: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `ตัวเลขไม่ถูกต้อง: ต้องเป็นจำนวนที่หารด้วย ${e.divisor} ได้ลงตัว`;
			case "unrecognized_keys": return `พบคีย์ที่ไม่รู้จัก: ${j(e.keys, ", ")}`;
			case "invalid_key": return `คีย์ไม่ถูกต้องใน ${e.origin}`;
			case "invalid_union": return "ข้อมูลไม่ถูกต้อง: ไม่ตรงกับรูปแบบยูเนียนที่กำหนดไว้";
			case "invalid_element": return `ข้อมูลไม่ถูกต้องใน ${e.origin}`;
			default: return "ข้อมูลไม่ถูกต้อง";
		}
	};
};
function Bu() {
	return { localeError: zu() };
}
//#endregion
//#region node_modules/zod/v4/locales/tk.js
var Vu = () => {
	let e = {
		string: {
			unit: "simwol",
			verb: "bolmaly"
		},
		file: {
			unit: "baýt",
			verb: "bolmaly"
		},
		array: {
			unit: "elementler",
			verb: "bolmaly"
		},
		set: {
			unit: "elementler",
			verb: "bolmaly"
		},
		map: {
			unit: "elementler",
			verb: "bolmaly"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "giriş",
		email: "e-poçta salgysy",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO sene we wagt",
		date: "ISO sene",
		time: "ISO wagt",
		duration: "ISO wagt aralygy",
		ipv4: "IPv4 salgysy",
		ipv6: "IPv6 salgysy",
		mac: "MAC salgysy",
		cidrv4: "IPv4 aralygy",
		cidrv6: "IPv6 aralygy",
		base64: "base64 bilen şifrlenen setir",
		base64url: "base64url bilen şifrlenen setir",
		json_string: "JSON setiri",
		e164: "E.164 nomeri",
		credit_card: "kredit kartynyň nomeri",
		currency_code: "walýuta kody",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "şablon"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input);
				return `Nädogry baha: garaşylan ${t} ýerine ${r[n] ?? n} alyndy`;
			}
			case "invalid_value": return e.values.length === 1 ? `Nädogry baha: ${D(e.values[0])} bolmaly` : `Nädogry saýlaw: aşakdakylardan biri bolmaly: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Has uly: garaşylýan ${e.origin ?? "baha"} ${n} ${e.maximum.toString()} ${r.unit ?? "element"}` : `Has uly: garaşylýan ${e.origin ?? "baha"} ${n} ${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Has kiçi: garaşylýan ${e.origin} ${n} ${e.minimum.toString()} ${r.unit}` : `Has kiçi: garaşylýan ${e.origin} ${n} ${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Nädogry setir: "${t.prefix}" bilen başlamaly` : t.format === "ends_with" ? `Nädogry setir: "${t.suffix}" bilen gutarmaly` : t.format === "includes" ? `Nädogry setir: "${t.includes}" saklamaly` : t.format === "regex" ? `Nädogry setir: ${t.pattern} nusga laýyk bolmaly` : `Nädogry ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Nädogry san: ${e.divisor} bilen galyndysyz bölünmeli`;
			case "unrecognized_keys": return `Tanalmaýan açar${e.keys.length > 1 ? "lar" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} içinde nädogry açar`;
			case "invalid_union": return "Nädogry baha";
			case "invalid_element": return `${e.origin} içinde nädogry baha`;
			default: return "Nädogry baha";
		}
	};
};
function Hu() {
	return { localeError: Vu() };
}
//#endregion
//#region node_modules/zod/v4/locales/tr.js
var Uu = () => {
	let e = {
		string: {
			unit: "karakter",
			verb: "olmalı"
		},
		file: {
			unit: "bayt",
			verb: "olmalı"
		},
		array: {
			unit: "öğe",
			verb: "olmalı"
		},
		set: {
			unit: "öğe",
			verb: "olmalı"
		},
		map: {
			unit: "öğe",
			verb: "olmalı"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "girdi",
		email: "e-posta adresi",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO tarih ve saat",
		date: "ISO tarih",
		time: "ISO saat",
		duration: "ISO süre",
		ipv4: "IPv4 adresi",
		ipv6: "IPv6 adresi",
		mac: "MAC adresi",
		cidrv4: "IPv4 aralığı",
		cidrv6: "IPv6 aralığı",
		base64: "base64 ile şifrelenmiş metin",
		base64url: "base64url ile şifrelenmiş metin",
		json_string: "JSON dizesi",
		e164: "E.164 sayısı",
		credit_card: "kredi kartı numarası",
		currency_code: "para birimi kodu",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "Şablon dizesi"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Geçersiz değer: beklenen instanceof ${e.expected}, alınan ${i}` : `Geçersiz değer: beklenen ${t}, alınan ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Geçersiz değer: beklenen ${D(e.values[0])}` : `Geçersiz seçenek: aşağıdakilerden biri olmalı: ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Çok büyük: beklenen ${e.origin ?? "değer"} ${n}${e.maximum.toString()} ${r.unit ?? "öğe"}` : `Çok büyük: beklenen ${e.origin ?? "değer"} ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Çok küçük: beklenen ${e.origin} ${n}${e.minimum.toString()} ${r.unit}` : `Çok küçük: beklenen ${e.origin} ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Geçersiz metin: "${t.prefix}" ile başlamalı` : t.format === "ends_with" ? `Geçersiz metin: "${t.suffix}" ile bitmeli` : t.format === "includes" ? `Geçersiz metin: "${t.includes}" içermeli` : t.format === "regex" ? `Geçersiz metin: ${t.pattern} desenine uymalı` : `Geçersiz ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Geçersiz sayı: ${e.divisor} ile tam bölünebilmeli`;
			case "unrecognized_keys": return `Tanınmayan anahtar${e.keys.length > 1 ? "lar" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} içinde geçersiz anahtar`;
			case "invalid_union": return "Geçersiz değer";
			case "invalid_element": return `${e.origin} içinde geçersiz değer`;
			default: return "Geçersiz değer";
		}
	};
};
function Wu() {
	return { localeError: Uu() };
}
//#endregion
//#region node_modules/zod/v4/locales/uk.js
var Gu = () => {
	let e = {
		string: {
			unit: "символів",
			verb: "матиме"
		},
		file: {
			unit: "байтів",
			verb: "матиме"
		},
		array: {
			unit: "елементів",
			verb: "матиме"
		},
		set: {
			unit: "елементів",
			verb: "матиме"
		},
		map: {
			unit: "елементів",
			verb: "матиме"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "вхідні дані",
		email: "адреса електронної пошти",
		url: "URL",
		emoji: "емодзі",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "дата та час ISO",
		date: "дата ISO",
		time: "час ISO",
		duration: "тривалість ISO",
		ipv4: "адреса IPv4",
		ipv6: "адреса IPv6",
		mac: "адреса MAC",
		cidrv4: "діапазон IPv4",
		cidrv6: "діапазон IPv6",
		base64: "рядок у кодуванні base64",
		base64url: "рядок у кодуванні base64url",
		json_string: "рядок JSON",
		e164: "номер E.164",
		credit_card: "номер кредитної картки",
		currency_code: "код валюти",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "вхідні дані"
	}, r = {
		nan: "NaN",
		number: "число",
		array: "масив"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Неправильні вхідні дані: очікується instanceof ${e.expected}, отримано ${i}` : `Неправильні вхідні дані: очікується ${t}, отримано ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Неправильні вхідні дані: очікується ${D(e.values[0])}` : `Неправильна опція: очікується одне з ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Занадто велике: очікується, що ${e.origin ?? "значення"} ${r.verb} ${n}${e.maximum.toString()} ${r.unit ?? "елементів"}` : `Занадто велике: очікується, що ${e.origin ?? "значення"} буде ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Занадто мале: очікується, що ${e.origin} ${r.verb} ${n}${e.minimum.toString()} ${r.unit}` : `Занадто мале: очікується, що ${e.origin} буде ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Неправильний рядок: повинен починатися з "${t.prefix}"` : t.format === "ends_with" ? `Неправильний рядок: повинен закінчуватися на "${t.suffix}"` : t.format === "includes" ? `Неправильний рядок: повинен містити "${t.includes}"` : t.format === "regex" ? `Неправильний рядок: повинен відповідати шаблону ${t.pattern}` : `Неправильний ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Неправильне число: повинно бути кратним ${e.divisor}`;
			case "unrecognized_keys": return `Нерозпізнаний ключ${e.keys.length > 1 ? "і" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Неправильний ключ у ${e.origin}`;
			case "invalid_union": return "Неправильні вхідні дані";
			case "invalid_element": return `Неправильне значення у ${e.origin}`;
			default: return "Неправильні вхідні дані";
		}
	};
};
function Ku() {
	return { localeError: Gu() };
}
//#endregion
//#region node_modules/zod/v4/locales/ua.js
function qu() {
	return Ku();
}
//#endregion
//#region node_modules/zod/v4/locales/ur.js
var Ju = () => {
	let e = {
		string: {
			unit: "حروف",
			verb: "ہونا"
		},
		file: {
			unit: "بائٹس",
			verb: "ہونا"
		},
		array: {
			unit: "آئٹمز",
			verb: "ہونا"
		},
		set: {
			unit: "آئٹمز",
			verb: "ہونا"
		},
		map: {
			unit: "آئٹمز",
			verb: "ہونا"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ان پٹ",
		email: "ای میل ایڈریس",
		url: "یو آر ایل",
		emoji: "ایموجی",
		uuid: "یو یو آئی ڈی",
		uuidv4: "یو یو آئی ڈی وی 4",
		uuidv6: "یو یو آئی ڈی وی 6",
		nanoid: "نینو آئی ڈی",
		guid: "جی یو آئی ڈی",
		cuid: "سی یو آئی ڈی",
		cuid2: "سی یو آئی ڈی 2",
		ulid: "یو ایل آئی ڈی",
		xid: "ایکس آئی ڈی",
		ksuid: "کے ایس یو آئی ڈی",
		datetime: "آئی ایس او ڈیٹ ٹائم",
		date: "آئی ایس او تاریخ",
		time: "آئی ایس او وقت",
		duration: "آئی ایس او مدت",
		ipv4: "آئی پی وی 4 ایڈریس",
		ipv6: "آئی پی وی 6 ایڈریس",
		mac: "ایم اے سی ایڈریس",
		cidrv4: "آئی پی وی 4 رینج",
		cidrv6: "آئی پی وی 6 رینج",
		base64: "بیس 64 ان کوڈڈ سٹرنگ",
		base64url: "بیس 64 یو آر ایل ان کوڈڈ سٹرنگ",
		json_string: "جے ایس او این سٹرنگ",
		e164: "ای 164 نمبر",
		credit_card: "کریڈٹ کارڈ نمبر",
		currency_code: "کرنسی کوڈ",
		iban: "IBAN",
		jwt: "جے ڈبلیو ٹی",
		template_literal: "ان پٹ"
	}, r = {
		nan: "NaN",
		number: "نمبر",
		array: "آرے",
		null: "نل"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `غلط ان پٹ: instanceof ${e.expected} متوقع تھا، ${i} موصول ہوا` : `غلط ان پٹ: ${t} متوقع تھا، ${i} موصول ہوا`;
			}
			case "invalid_value": return e.values.length === 1 ? `غلط ان پٹ: ${D(e.values[0])} متوقع تھا` : `غلط آپشن: ${j(e.values, "|")} میں سے ایک متوقع تھا`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `بہت بڑا: ${e.origin ?? "ویلیو"} کے ${n}${e.maximum.toString()} ${r.unit ?? "عناصر"} ہونے متوقع تھے` : `بہت بڑا: ${e.origin ?? "ویلیو"} کا ${n}${e.maximum.toString()} ہونا متوقع تھا`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `بہت چھوٹا: ${e.origin} کے ${n}${e.minimum.toString()} ${r.unit} ہونے متوقع تھے` : `بہت چھوٹا: ${e.origin} کا ${n}${e.minimum.toString()} ہونا متوقع تھا`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `غلط سٹرنگ: "${t.prefix}" سے شروع ہونا چاہیے` : t.format === "ends_with" ? `غلط سٹرنگ: "${t.suffix}" پر ختم ہونا چاہیے` : t.format === "includes" ? `غلط سٹرنگ: "${t.includes}" شامل ہونا چاہیے` : t.format === "regex" ? `غلط سٹرنگ: پیٹرن ${t.pattern} سے میچ ہونا چاہیے` : `غلط ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `غلط نمبر: ${e.divisor} کا مضاعف ہونا چاہیے`;
			case "unrecognized_keys": return `غیر تسلیم شدہ کی${e.keys.length > 1 ? "ز" : ""}: ${j(e.keys, "، ")}`;
			case "invalid_key": return `${e.origin} میں غلط کی`;
			case "invalid_union": return "غلط ان پٹ";
			case "invalid_element": return `${e.origin} میں غلط ویلیو`;
			default: return "غلط ان پٹ";
		}
	};
};
function Yu() {
	return { localeError: Ju() };
}
//#endregion
//#region node_modules/zod/v4/locales/uz.js
var Xu = () => {
	let e = {
		string: {
			unit: "belgi",
			verb: "bo‘lishi kerak"
		},
		file: {
			unit: "bayt",
			verb: "bo‘lishi kerak"
		},
		array: {
			unit: "element",
			verb: "bo‘lishi kerak"
		},
		set: {
			unit: "element",
			verb: "bo‘lishi kerak"
		},
		map: {
			unit: "yozuv",
			verb: "bo‘lishi kerak"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "kirish",
		email: "elektron pochta manzili",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO sana va vaqti",
		date: "ISO sana",
		time: "ISO vaqt",
		duration: "ISO davomiylik",
		ipv4: "IPv4 manzil",
		ipv6: "IPv6 manzil",
		mac: "MAC manzil",
		cidrv4: "IPv4 diapazon",
		cidrv6: "IPv6 diapazon",
		base64: "base64 kodlangan satr",
		base64url: "base64url kodlangan satr",
		json_string: "JSON satr",
		e164: "E.164 raqam",
		credit_card: "kredit karta raqami",
		currency_code: "valyuta kodi",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "kirish"
	}, r = {
		nan: "NaN",
		number: "raqam",
		array: "massiv"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Noto‘g‘ri kirish: kutilgan instanceof ${e.expected}, qabul qilingan ${i}` : `Noto‘g‘ri kirish: kutilgan ${t}, qabul qilingan ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Noto‘g‘ri kirish: kutilgan ${D(e.values[0])}` : `Noto‘g‘ri variant: quyidagilardan biri kutilgan ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Juda katta: kutilgan ${e.origin ?? "qiymat"} ${n}${e.maximum.toString()} ${r.unit} ${r.verb}` : `Juda katta: kutilgan ${e.origin ?? "qiymat"} ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Juda kichik: kutilgan ${e.origin} ${n}${e.minimum.toString()} ${r.unit} ${r.verb}` : `Juda kichik: kutilgan ${e.origin} ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Noto‘g‘ri satr: "${t.prefix}" bilan boshlanishi kerak` : t.format === "ends_with" ? `Noto‘g‘ri satr: "${t.suffix}" bilan tugashi kerak` : t.format === "includes" ? `Noto‘g‘ri satr: "${t.includes}" ni o‘z ichiga olishi kerak` : t.format === "regex" ? `Noto‘g‘ri satr: ${t.pattern} shabloniga mos kelishi kerak` : `Noto‘g‘ri ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Noto‘g‘ri raqam: ${e.divisor} ning karralisi bo‘lishi kerak`;
			case "unrecognized_keys": return `Noma’lum kalit${e.keys.length > 1 ? "lar" : ""}: ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} dagi kalit noto‘g‘ri`;
			case "invalid_union": return "Noto‘g‘ri kirish";
			case "invalid_element": return `${e.origin} da noto‘g‘ri qiymat`;
			default: return "Noto‘g‘ri kirish";
		}
	};
};
function Zu() {
	return { localeError: Xu() };
}
//#endregion
//#region node_modules/zod/v4/locales/vi.js
var Qu = () => {
	let e = {
		string: {
			unit: "ký tự",
			verb: "có"
		},
		file: {
			unit: "byte",
			verb: "có"
		},
		array: {
			unit: "phần tử",
			verb: "có"
		},
		set: {
			unit: "phần tử",
			verb: "có"
		},
		map: {
			unit: "phần tử",
			verb: "có"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "đầu vào",
		email: "địa chỉ email",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ngày giờ ISO",
		date: "ngày ISO",
		time: "giờ ISO",
		duration: "khoảng thời gian ISO",
		ipv4: "địa chỉ IPv4",
		ipv6: "địa chỉ IPv6",
		mac: "địa chỉ MAC",
		cidrv4: "dải IPv4",
		cidrv6: "dải IPv6",
		base64: "chuỗi mã hóa base64",
		base64url: "chuỗi mã hóa base64url",
		json_string: "chuỗi JSON",
		e164: "số E.164",
		credit_card: "số thẻ tín dụng",
		currency_code: "mã tiền tệ",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "đầu vào"
	}, r = {
		nan: "NaN",
		number: "số",
		array: "mảng"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Đầu vào không hợp lệ: mong đợi instanceof ${e.expected}, nhận được ${i}` : `Đầu vào không hợp lệ: mong đợi ${t}, nhận được ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Đầu vào không hợp lệ: mong đợi ${D(e.values[0])}` : `Tùy chọn không hợp lệ: mong đợi một trong các giá trị ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Quá lớn: mong đợi ${e.origin ?? "giá trị"} ${r.verb} ${n}${e.maximum.toString()} ${r.unit ?? "phần tử"}` : `Quá lớn: mong đợi ${e.origin ?? "giá trị"} ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Quá nhỏ: mong đợi ${e.origin} ${r.verb} ${n}${e.minimum.toString()} ${r.unit}` : `Quá nhỏ: mong đợi ${e.origin} ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Chuỗi không hợp lệ: phải bắt đầu bằng "${t.prefix}"` : t.format === "ends_with" ? `Chuỗi không hợp lệ: phải kết thúc bằng "${t.suffix}"` : t.format === "includes" ? `Chuỗi không hợp lệ: phải bao gồm "${t.includes}"` : t.format === "regex" ? `Chuỗi không hợp lệ: phải khớp với mẫu ${t.pattern}` : `${n[t.format] ?? e.format} không hợp lệ`;
			}
			case "not_multiple_of": return `Số không hợp lệ: phải là bội số của ${e.divisor}`;
			case "unrecognized_keys": return `Khóa không được nhận dạng: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Khóa không hợp lệ trong ${e.origin}`;
			case "invalid_union": return "Đầu vào không hợp lệ";
			case "invalid_element": return `Giá trị không hợp lệ trong ${e.origin}`;
			default: return "Đầu vào không hợp lệ";
		}
	};
};
function $u() {
	return { localeError: Qu() };
}
//#endregion
//#region node_modules/zod/v4/locales/zh-CN.js
var ed = () => {
	let e = {
		string: {
			unit: "字符",
			verb: "包含"
		},
		file: {
			unit: "字节",
			verb: "包含"
		},
		array: {
			unit: "项",
			verb: "包含"
		},
		set: {
			unit: "项",
			verb: "包含"
		},
		map: {
			unit: "项",
			verb: "包含"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "输入",
		email: "电子邮件",
		url: "URL",
		emoji: "表情符号",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO日期时间",
		date: "ISO日期",
		time: "ISO时间",
		duration: "ISO时长",
		ipv4: "IPv4地址",
		ipv6: "IPv6地址",
		mac: "MAC地址",
		cidrv4: "IPv4网段",
		cidrv6: "IPv6网段",
		base64: "base64编码字符串",
		base64url: "base64url编码字符串",
		json_string: "JSON字符串",
		e164: "E.164号码",
		credit_card: "信用卡号",
		currency_code: "货币代码",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "输入"
	}, r = {
		nan: "NaN",
		number: "数字",
		array: "数组",
		null: "空值(null)"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `无效输入：期望 instanceof ${e.expected}，实际接收 ${i}` : `无效输入：期望 ${t}，实际接收 ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `无效输入：期望 ${D(e.values[0])}` : `无效选项：期望以下之一 ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `数值过大：期望 ${e.origin ?? "值"} ${n}${e.maximum.toString()} ${r.unit ?? "个元素"}` : `数值过大：期望 ${e.origin ?? "值"} ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `数值过小：期望 ${e.origin} ${n}${e.minimum.toString()} ${r.unit}` : `数值过小：期望 ${e.origin} ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `无效字符串：必须以 "${t.prefix}" 开头` : t.format === "ends_with" ? `无效字符串：必须以 "${t.suffix}" 结尾` : t.format === "includes" ? `无效字符串：必须包含 "${t.includes}"` : t.format === "regex" ? `无效字符串：必须满足正则表达式 ${t.pattern}` : `无效${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `无效数字：必须是 ${e.divisor} 的倍数`;
			case "unrecognized_keys": return `出现未知的键(key): ${j(e.keys, ", ")}`;
			case "invalid_key": return `${e.origin} 中的键(key)无效`;
			case "invalid_union": return "无效输入";
			case "invalid_element": return `${e.origin} 中包含无效值(value)`;
			default: return "无效输入";
		}
	};
};
function td() {
	return { localeError: ed() };
}
//#endregion
//#region node_modules/zod/v4/locales/zh-TW.js
var nd = () => {
	let e = {
		string: {
			unit: "字元",
			verb: "擁有"
		},
		file: {
			unit: "位元組",
			verb: "擁有"
		},
		array: {
			unit: "項目",
			verb: "擁有"
		},
		set: {
			unit: "項目",
			verb: "擁有"
		},
		map: {
			unit: "項目",
			verb: "擁有"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "輸入",
		email: "郵件地址",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "ISO 日期時間",
		date: "ISO 日期",
		time: "ISO 時間",
		duration: "ISO 期間",
		ipv4: "IPv4 位址",
		ipv6: "IPv6 位址",
		mac: "MAC 位址",
		cidrv4: "IPv4 範圍",
		cidrv6: "IPv6 範圍",
		base64: "base64 編碼字串",
		base64url: "base64url 編碼字串",
		json_string: "JSON 字串",
		e164: "E.164 數值",
		credit_card: "信用卡號",
		currency_code: "貨幣代碼",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "輸入"
	}, r = { nan: "NaN" };
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `無效的輸入值：預期為 instanceof ${e.expected}，但收到 ${i}` : `無效的輸入值：預期為 ${t}，但收到 ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `無效的輸入值：預期為 ${D(e.values[0])}` : `無效的選項：預期為以下其中之一 ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `數值過大：預期 ${e.origin ?? "值"} 應為 ${n}${e.maximum.toString()} ${r.unit ?? "個元素"}` : `數值過大：預期 ${e.origin ?? "值"} 應為 ${n}${e.maximum.toString()}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `數值過小：預期 ${e.origin} 應為 ${n}${e.minimum.toString()} ${r.unit}` : `數值過小：預期 ${e.origin} 應為 ${n}${e.minimum.toString()}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `無效的字串：必須以 "${t.prefix}" 開頭` : t.format === "ends_with" ? `無效的字串：必須以 "${t.suffix}" 結尾` : t.format === "includes" ? `無效的字串：必須包含 "${t.includes}"` : t.format === "regex" ? `無效的字串：必須符合格式 ${t.pattern}` : `無效的 ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `無效的數字：必須為 ${e.divisor} 的倍數`;
			case "unrecognized_keys": return `無法識別的鍵值${e.keys.length > 1 ? "們" : ""}：${j(e.keys, "、")}`;
			case "invalid_key": return `${e.origin} 中有無效的鍵值`;
			case "invalid_union": return "無效的輸入值";
			case "invalid_element": return `${e.origin} 中有無效的值`;
			default: return "無效的輸入值";
		}
	};
};
function rd() {
	return { localeError: nd() };
}
//#endregion
//#region node_modules/zod/v4/locales/yo.js
var id = () => {
	let e = {
		string: {
			unit: "àmi",
			verb: "ní"
		},
		file: {
			unit: "bytes",
			verb: "ní"
		},
		array: {
			unit: "nkan",
			verb: "ní"
		},
		set: {
			unit: "nkan",
			verb: "ní"
		},
		map: {
			unit: "nkan",
			verb: "ní"
		}
	};
	function t(t) {
		return e[t] ?? null;
	}
	let n = {
		regex: "ẹ̀rọ ìbáwọlé",
		email: "àdírẹ́sì ìmẹ́lì",
		url: "URL",
		emoji: "emoji",
		uuid: "UUID",
		uuidv4: "UUIDv4",
		uuidv6: "UUIDv6",
		nanoid: "nanoid",
		guid: "GUID",
		cuid: "cuid",
		cuid2: "cuid2",
		ulid: "ULID",
		xid: "XID",
		ksuid: "KSUID",
		datetime: "àkókò ISO",
		date: "ọjọ́ ISO",
		time: "àkókò ISO",
		duration: "àkókò tó pé ISO",
		ipv4: "àdírẹ́sì IPv4",
		ipv6: "àdírẹ́sì IPv6",
		mac: "àdírẹ́sì MAC",
		cidrv4: "àgbègbè IPv4",
		cidrv6: "àgbègbè IPv6",
		base64: "ọ̀rọ̀ tí a kọ́ ní base64",
		base64url: "ọ̀rọ̀ base64url",
		json_string: "ọ̀rọ̀ JSON",
		e164: "nọ́mbà E.164",
		credit_card: "nọmba kaadi gbese",
		currency_code: "koodu owó",
		iban: "IBAN",
		jwt: "JWT",
		template_literal: "ẹ̀rọ ìbáwọlé"
	}, r = {
		nan: "NaN",
		number: "nọ́mbà",
		array: "akopọ"
	};
	return (e) => {
		switch (e.code) {
			case "invalid_type": {
				let t = r[e.expected] ?? e.expected, n = E(e.input), i = r[n] ?? n;
				return /^[A-Z]/.test(e.expected) ? `Ìbáwọlé aṣìṣe: a ní láti fi instanceof ${e.expected}, àmọ̀ a rí ${i}` : `Ìbáwọlé aṣìṣe: a ní láti fi ${t}, àmọ̀ a rí ${i}`;
			}
			case "invalid_value": return e.values.length === 1 ? `Ìbáwọlé aṣìṣe: a ní láti fi ${D(e.values[0])}` : `Àṣàyàn aṣìṣe: yan ọ̀kan lára ${j(e.values, "|")}`;
			case "too_big": {
				let n = e.inclusive ? "<=" : "<", r = t(e.origin);
				return r ? `Tó pọ̀ jù: a ní láti jẹ́ pé ${e.origin ?? "iye"} ${r.verb} ${n}${e.maximum} ${r.unit}` : `Tó pọ̀ jù: a ní láti jẹ́ ${n}${e.maximum}`;
			}
			case "too_small": {
				let n = e.inclusive ? ">=" : ">", r = t(e.origin);
				return r ? `Kéré ju: a ní láti jẹ́ pé ${e.origin} ${r.verb} ${n}${e.minimum} ${r.unit}` : `Kéré ju: a ní láti jẹ́ ${n}${e.minimum}`;
			}
			case "invalid_format": {
				let t = e;
				return t.format === "starts_with" ? `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ bẹ̀rẹ̀ pẹ̀lú "${t.prefix}"` : t.format === "ends_with" ? `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ parí pẹ̀lú "${t.suffix}"` : t.format === "includes" ? `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ ní "${t.includes}"` : t.format === "regex" ? `Ọ̀rọ̀ aṣìṣe: gbọ́dọ̀ bá àpẹẹrẹ mu ${t.pattern}` : `Aṣìṣe: ${n[t.format] ?? e.format}`;
			}
			case "not_multiple_of": return `Nọ́mbà aṣìṣe: gbọ́dọ̀ jẹ́ èyà pípín ti ${e.divisor}`;
			case "unrecognized_keys": return `Bọtìnì àìmọ̀: ${j(e.keys, ", ")}`;
			case "invalid_key": return `Bọtìnì aṣìṣe nínú ${e.origin}`;
			case "invalid_union": return "Ìbáwọlé aṣìṣe";
			case "invalid_element": return `Iye aṣìṣe nínú ${e.origin}`;
			default: return "Ìbáwọlé aṣìṣe";
		}
	};
};
function ad() {
	return { localeError: id() };
}
//#endregion
//#region node_modules/zod/v4/locales/index.js
var od = /* @__PURE__ */ e({
	ar: () => Vc,
	az: () => Uc,
	be: () => Kc,
	bg: () => Jc,
	bn: () => Xc,
	ca: () => Qc,
	ckb: () => el,
	cs: () => nl,
	da: () => il,
	de: () => ol,
	el: () => cl,
	en: () => r,
	eo: () => ul,
	es: () => fl,
	fa: () => ml,
	fi: () => gl,
	fr: () => vl,
	frCA: () => bl,
	gu: () => Sl,
	he: () => wl,
	hi: () => El,
	hr: () => Ol,
	hu: () => Al,
	hy: () => Pl,
	id: () => Il,
	is: () => Rl,
	it: () => Bl,
	ja: () => Hl,
	ka: () => Wl,
	kh: () => ql,
	km: () => Kl,
	kn: () => Yl,
	ko: () => Zl,
	lt: () => tu,
	mk: () => ru,
	ms: () => au,
	ne: () => su,
	nl: () => lu,
	nn: () => du,
	no: () => pu,
	ota: () => hu,
	pl: () => yu,
	ps: () => _u,
	pt: () => xu,
	ptBR: () => Cu,
	ro: () => Tu,
	ru: () => Ou,
	sk: () => Au,
	sl: () => Mu,
	sv: () => Pu,
	ta: () => Iu,
	tg: () => Ru,
	th: () => Bu,
	tk: () => Hu,
	tr: () => Wu,
	ua: () => qu,
	uk: () => Ku,
	ur: () => Yu,
	uz: () => Zu,
	vi: () => $u,
	yo: () => ad,
	zhCN: () => td,
	zhTW: () => rd
}), P = Symbol.for("zod.compile.invalid"), sd = Symbol.for("zod.compile.fallback"), F = class extends Error {
	constructor(e = "z.compile does not support async refinements, transforms, or checks") {
		super(e), this.name = "ZodCompileAsyncError";
	}
}, I = class extends Error {
	constructor(e, t = !0) {
		super(`z.compile does not support ${e}; this schema must use the runtime parser`), this.name = "ZodCompileUnsupportedError", this.islandable = t;
	}
};
function cd(e, t) {
	try {
		return fd(e, { assertOnly: !0 });
	} catch {
		return t;
	}
}
function ld(e, t) {
	try {
		let t = fd(e), n = ud(e, t);
		return n._zod.bag.validator = cd(e, t), n;
	} catch (n) {
		if (t?.strict) throw n;
		return e;
	}
}
function ud(e, t) {
	if (Io(e)) throw new I("a schema whose subtree contains a reference cycle");
	let n = T(e), r = e._zod.run, i = r.__originalRun ?? r, a = (e, n) => {
		if (n?.async || n?.direction === "backward" || n?.skipChecks || n?.[sd] || n && Ls(n, e.value)) return i(e, n);
		let r = t(e.value);
		return r === P ? (n && (n[sd] = !0), i(e, n)) : (e.value = r, e);
	};
	return a.__originalRun = i, n._zod.bag.fallbackRun = i, n._zod.bag.validator = t, n._zod.run = a, r.__originalRun || dd(n, e, t), n;
}
function dd(e, t, n) {
	let r = e, i = t;
	if (typeof i.safeParse == "function") {
		let e = i.safeParse;
		r.safeParse = (t, r) => {
			let i = n(t);
			return i === P ? e(t, r) : {
				success: !0,
				data: i
			};
		};
	}
	if (typeof i.parse == "function") {
		let e = i.parse;
		r.parse = (t, r) => {
			let i = n(t);
			return i === P ? e(t, r) : i;
		};
	}
}
function fd(e, t) {
	let n = !0;
	try {
		n = Io(e);
	} catch {}
	if (n) throw new I("a schema whose subtree contains a reference cycle");
	let r = {
		constants: /* @__PURE__ */ new Map(),
		constantCounter: 0,
		varCounter: 0,
		definite: !0
	}, i = new et(["input"]), a = H(i, r, e, "input", !t?.assertOnly);
	i.write(a === null ? "return true;" : `return ${a};`);
	let o = ["INVALID", ...r.constants.keys()], s = [P, ...r.constants.values()], c = i.content.join("\n"), l = t?.debug ? o.length > 0 ? `// Constants: ${o.join(", ")}\n${c}` : c : "", u = Function, d = `return (input) => {\n${c}\n}`, f;
	try {
		f = new u(...o, d)(...s);
	} catch (e) {
		throw new I(`this schema (generated code failed to evaluate: ${e.message})`);
	}
	return t?.debug && (f.code = l), f.definite = r.definite, f;
}
function L(e, t) {
	for (let [n, r] of e.constants) if (r === t) return n;
	let n = `c${e.constantCounter++}`;
	return e.constants.set(n, t), n;
}
function R(e, t) {
	return e.definite = !1, L(e, t);
}
function z(e) {
	return `v${e.varCounter++}`;
}
function pd(e, t) {
	let n = e._zod.run({
		value: t,
		issues: []
	}, {});
	if (n && typeof n.then == "function") return P;
	let r = n;
	return r.issues.length === 0 ? r.value : P;
}
function B(e, t, n, r, i = !0) {
	let a = e.content.length, o = t.constants.size, s = t.constantCounter, c = t.varCounter;
	try {
		return H(e, t, n, r, i);
	} catch (i) {
		if (!(i instanceof I) || !i.islandable) throw i;
		if (e.content.length = a, t.constants.size > o) {
			let e = Array.from(t.constants.keys()).slice(o);
			for (let n of e) t.constants.delete(n);
		}
		return t.constantCounter = s, t.varCounter = c, md(e, t, n, r);
	}
}
function md(e, t, n, r) {
	t.definite = !1;
	let i = L(t, n), a = L(t, pd), o = z(t);
	return e.write(`const ${o} = ${a}(${i}, ${r});`), e.write(`if (${o} === INVALID) return INVALID;`), o;
}
var hd = /* @__PURE__ */ new Set([
	"max_size",
	"min_size",
	"size_equals",
	"max_length",
	"min_length",
	"length_equals"
]);
function gd(e, t, n, r) {
	let i = n._zod.def.checks;
	if (!i || i.length === 0) return r;
	let a = r;
	for (let n of i) {
		let r = n._zod.def;
		if (r.when && !hd.has(r.check)) throw new I("check with a custom \"when\" condition");
		switch (r.check) {
			case "greater_than":
				yd(e, t, r, a);
				break;
			case "less_than":
				bd(e, t, r, a);
				break;
			case "multiple_of":
				xd(e, t, r, a);
				break;
			case "number_format":
				Sd(e, r, a);
				break;
			case "min_length": {
				let n = V(r.minimum, "min_length"), i = _d(e, t, a, `${a}.length >= ${n} && ${a}.length < ${r.minimum * 2}`);
				e.write(`if (${i} < ${n}) return INVALID;`);
				break;
			}
			case "max_length": {
				let n = V(r.maximum, "max_length"), i = _d(e, t, a, `${a}.length > ${n}`);
				e.write(`if (${i} > ${n}) return INVALID;`);
				break;
			}
			case "length_equals": {
				let n = V(r.length, "length_equals"), i = _d(e, t, a, `${a}.length >= ${n} && ${a}.length <= ${r.length * 2}`);
				e.write(`if (${i} !== ${n}) return INVALID;`);
				break;
			}
			case "min_size":
				e.write(`if (${a}.size < ${V(r.minimum, "min_size")}) return INVALID;`);
				break;
			case "max_size":
				e.write(`if (${a}.size > ${V(r.maximum, "max_size")}) return INVALID;`);
				break;
			case "size_equals":
				e.write(`if (${a}.size !== ${V(r.size, "size_equals")}) return INVALID;`);
				break;
			case "string_format":
				a = Md(e, t, r, a);
				break;
			case "custom":
				a = Ad(e, t, n, a);
				break;
			case "bigint_format":
				Cd(e, r, a);
				break;
			case "mime_type":
				wd(e, t, r, a);
				break;
			case "property":
				Ed(e, t, r, a);
				break;
			case "properties":
				Td(e, t, r, a);
				break;
			case "overwrite": {
				let r = z(t);
				Dd(e, t, n, a, r), a = r;
				break;
			}
			default: throw new I(`check type ${r.check}`);
		}
	}
	return a;
}
function _d(e, t, n, r) {
	let i = L(t, Wo), a = z(t);
	return e.write(`const ${a} = typeof ${n} === "string" && ${r} ? ${i}(${n}) : ${n}.length;`), a;
}
function V(e, t) {
	if (typeof e != "number" || !Number.isFinite(e)) throw new I(`${t} bound of type ${typeof e}`);
	return `${e}`;
}
function vd(e, t) {
	if (typeof t == "bigint") return `${t}n`;
	if (typeof t == "number") {
		if (Number.isNaN(t)) throw new I("comparison check with NaN bound");
		return `${t}`;
	}
	if (t instanceof Date) {
		if (Number.isNaN(t.getTime())) throw new I("comparison check with Invalid Date bound");
		return L(e, t);
	}
	throw new I(`comparison check bound of type ${typeof t}`);
}
function yd(e, t, n, r) {
	let i = n.inclusive ? "<" : "<=";
	e.write(`if (${r} ${i} ${vd(t, n.value)}) return INVALID;`);
}
function bd(e, t, n, r) {
	let i = n.inclusive ? ">" : ">=";
	e.write(`if (${r} ${i} ${vd(t, n.value)}) return INVALID;`);
}
function xd(e, t, n, r) {
	if (typeof n.value == "bigint") {
		if (n.value === BigInt(0)) throw new I("multiple_of check with a zero divisor");
		e.write(`if (${r} % ${n.value}n !== 0n) return INVALID;`);
	} else {
		let i = L(t, Ii);
		e.write(`if (${i}(${r}, ${V(n.value, "multiple_of")}) !== 0) return INVALID;`);
	}
}
function Sd(e, t, n) {
	let r = t.format;
	switch (r) {
		case "safeint":
			e.write(`if (!Number.isSafeInteger(${n})) return INVALID;`);
			break;
		case "int32":
			e.write(`if (!Number.isInteger(${n}) || ${n} < -2147483648 || ${n} > 2147483647) return INVALID;`);
			break;
		case "uint32":
			e.write(`if (!Number.isInteger(${n}) || ${n} < 0 || ${n} > 4294967295) return INVALID;`);
			break;
		case "float32":
			e.write(`if (!Number.isFinite(${n}) || ${n} < -3.4028234663852886e38 || ${n} > 3.4028234663852886e38) return INVALID;`);
			break;
		case "float64":
			e.write(`if (!Number.isFinite(${n})) return INVALID;`);
			break;
		default: throw new I(`number format ${r}`);
	}
}
function Cd(e, t, n) {
	let r = t.format;
	if (r) switch (r) {
		case "int64":
			e.write(`if (${n} < -9223372036854775808n || ${n} > 9223372036854775807n) return INVALID;`);
			break;
		case "uint64":
			e.write(`if (${n} < 0n || ${n} > 18446744073709551615n) return INVALID;`);
			break;
		default: throw new I(`bigint format ${r}`);
	}
}
function wd(e, t, n, r) {
	let i = n.mime;
	if (i && i.length > 0) {
		let n = L(t, new Set(i));
		e.write(`if (!${n}.has(${r}.type)) return INVALID;`);
	}
}
function Td(e, t, n, r) {
	if (n.when) throw new I("check with a custom \"when\" condition");
	e.write(`if (${r} == null) return INVALID;`);
	let i = n.shape;
	for (let n of Reflect.ownKeys(i)) {
		let a = typeof n == "symbol" ? L(t, n) : k(n), o = z(t);
		e.write(`const ${o} = ${r}[${a}];`), B(e, t, i[n], o, !1);
	}
}
function Ed(e, t, n, r) {
	let i = `${r}[${JSON.stringify(n.property)}]`;
	H(e, t, n.schema, i);
}
function Dd(e, t, n, r, i) {
	let a = n._zod.def.tx;
	if (!a) throw new I("overwrite check without a transform function");
	if (bf(a)) throw new F("z.compile: async overwrite transforms are not supported");
	let o = L(t, a);
	e.write(`const ${i} = ${o}(${r});`);
}
function Od() {
	throw new Pr();
}
function kd(e) {
	this.issues.push(e);
}
function Ad(e, t, n, r) {
	let i = n._zod.def;
	if (i.fn) {
		if (bf(i.fn)) throw new F("z.compile: async .refine() predicates are not supported");
		let n = R(t, i.fn), a = L(t, Od), o = z(t);
		return e.write(`const ${o} = ${n}(${r});`), e.write(`if (${o} instanceof Promise) ${a}();`), e.write(`if (!${o}) return INVALID;`), r;
	}
	if (n._zod.check) {
		if (bf(n._zod.check)) throw new F("z.compile: async .superRefine() / check functions are not supported");
		let i = n._zod.check, a = R(t, (e) => {
			let t = {
				value: e,
				issues: [],
				addIssue: kd
			};
			return i(t) instanceof Promise && Od(), t.issues.length === 0 ? t.value : P;
		}), o = z(t);
		return e.write(`const ${o} = ${a}(${r});`), e.write(`if (${o} === INVALID) return INVALID;`), o;
	}
	throw new I("custom check without a predicate or check function");
}
var jd = /* @__PURE__ */ new Set([
	"cidrv4",
	"cuid",
	"cuid2",
	"date",
	"datetime",
	"duration",
	"e164",
	"email",
	"emoji",
	"ends_with",
	"guid",
	"includes",
	"ipv4",
	"ksuid",
	"lowercase",
	"mac",
	"nanoid",
	"regex",
	"starts_with",
	"time",
	"ulid",
	"uppercase",
	"uuid",
	"xid"
]);
function Md(e, t, n, r, i = !0) {
	let a = n.format;
	if (a === "base64") {
		let n = L(t, ne);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	if (a === "base64url") {
		let n = L(t, $a);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	if (a === "jwt") {
		let i = L(t, _t), a = L(t, n.alg ?? null);
		return e.write(`if (!${i}(${r}, ${a})) return INVALID;`), r;
	}
	if (a === "ipv6") {
		let n = L(t, Ge);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	if (a === "cidrv6") {
		let n = L(t, Zt);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	if (a === "credit_card") {
		let n = L(t, sn);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	if (a === "iban") {
		let n = L(t, Cn);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	let o = n;
	if (a === "url" || a === "httpurl" || o.normalize || o.hostname !== void 0 || o.protocol !== void 0) {
		let a = L(t, fr), s = L(t, n), c = z(t), l = z(t);
		if (e.write(`const ${c} = ${r}.trim();`), e.write(`const ${l} = ${a}(${c}, ${s});`), e.write(`if (typeof ${l} === "number") return INVALID;`), o.hostname !== void 0) {
			let n = L(t, xr);
			e.write(`if (!${n}(${l}, ${s}.hostname)) return INVALID;`);
		}
		if (o.protocol !== void 0) {
			let n = L(t, lt);
			e.write(`if (!${n}(${l}, ${s}.protocol)) return INVALID;`);
		}
		if (!i) return null;
		let u = z(t), d = o.normalize ? `${l}.href` : `${L(t, de)}(${c})`;
		return e.write(`const ${u} = ${d};`), u;
	}
	let s = n.fn;
	if (s) {
		if (bf(s)) throw new I(`async string format ${a}`);
		let n = L(t, s);
		return e.write(`if (!${n}(${r})) return INVALID;`), r;
	}
	if (jd.has(a) && n.pattern) {
		let i = L(t, n.pattern);
		return e.write(`${i}.lastIndex = 0;`), e.write(`if (!${i}.test(${r})) return INVALID;`), r;
	}
	let c = n.format;
	switch (c) {
		case "regex": throw new I("regex format without a pattern");
		case "lowercase":
			e.write(`if (${r} !== ${r}.toLowerCase()) return INVALID;`);
			break;
		case "uppercase":
			e.write(`if (${r} !== ${r}.toUpperCase()) return INVALID;`);
			break;
		case "includes":
			e.write(`if (!${r}.includes(${k(n.includes)})) return INVALID;`);
			break;
		case "starts_with": {
			let t = n.prefix;
			e.write(`if (${r}.slice(0, ${t.length}) !== ${k(t)}) return INVALID;`);
			break;
		}
		case "ends_with": {
			let t = n.suffix;
			e.write(`if (${r}.slice(-${t.length}) !== ${k(t)}) return INVALID;`);
			break;
		}
		default: throw new I(`string format ${c}`);
	}
	return r;
}
function H(e, t, n, r, i = !0) {
	let a = n._zod.def, o = a.type;
	if (a.coerce) throw new I(`coercion (z.coerce.${o}())`);
	let s = i || !!a.checks?.length, c;
	switch (o) {
		case "string":
			c = Nd(e, t, n, r, s);
			break;
		case "number":
			c = Pd(e, n, r);
			break;
		case "boolean":
			c = Fd(e, r);
			break;
		case "bigint":
			c = Id(e, n, r);
			break;
		case "symbol":
			c = Ld(e, r);
			break;
		case "undefined":
			c = Rd(e, r);
			break;
		case "null":
			c = zd(e, r);
			break;
		case "any":
		case "unknown":
			c = r;
			break;
		case "never":
			e.write("return INVALID;"), c = r;
			break;
		case "void":
			c = Bd(e, r);
			break;
		case "nan":
			c = Vd(e, r);
			break;
		case "date":
			c = Hd(e, r);
			break;
		case "object":
			c = Ud(e, t, n, r, s);
			break;
		case "optional":
			c = Wd(e, t, n, r, s);
			break;
		case "nullable":
			c = Zd(e, t, n, r, s);
			break;
		case "array":
			c = Qd(e, t, n, r, s);
			break;
		case "literal":
			c = $d(e, t, n, r);
			break;
		case "enum":
			c = ef(e, t, n, r);
			break;
		case "readonly": {
			let i = tf(e, t, n, r), a = z(t);
			e.write(`const ${a} = Object.freeze(${i});`), c = a;
			break;
		}
		case "success":
			tf(e, t, n, r), c = "true";
			break;
		case "default":
		case "prefault":
			c = nf(e, t, n, r);
			break;
		case "nonoptional":
			c = rf(e, t, n, r);
			break;
		case "tuple":
			c = af(e, t, n, r);
			break;
		case "union":
			c = sf(e, t, n, r);
			break;
		case "intersection":
			c = uf(e, t, n, r);
			break;
		case "record":
			c = df(e, t, n, r);
			break;
		case "map":
			c = mf(e, t, n, r);
			break;
		case "set":
			c = hf(e, t, n, r);
			break;
		case "file":
			c = gf(e, r);
			break;
		case "template_literal":
			c = _f(e, t, n, r);
			break;
		case "lazy":
			c = vf(e, t, n, r);
			break;
		case "pipe":
			c = yf(e, t, n, r);
			break;
		case "custom":
			c = xf(e, t, n, r);
			break;
		case "transform":
			c = wf(e, t, n, r);
			break;
		case "catch":
			c = Cf(e, t, n, r);
			break;
		default: throw new I(`schema type ${o}`);
	}
	return c === null ? null : gd(e, t, n, c);
}
function Nd(e, t, n, r, i = !0) {
	e.write(`if (typeof ${r} !== "string") return INVALID;`);
	let a = n._zod.def;
	return a.format === void 0 ? r : Md(e, t, a, r, i);
}
function Pd(e, t, n) {
	e.write(`if (typeof ${n} !== "number" || !Number.isFinite(${n})) return INVALID;`);
	let r = t._zod.def;
	return r.check === "number_format" && r.format && Sd(e, { format: r.format }, n), n;
}
function Fd(e, t) {
	return e.write(`if (typeof ${t} !== "boolean") return INVALID;`), t;
}
function Id(e, t, n) {
	e.write(`if (typeof ${n} !== "bigint") return INVALID;`);
	let r = t._zod.def;
	if (r.format) switch (r.format) {
		case "int64":
			e.write(`if (${n} < -9223372036854775808n || ${n} > 9223372036854775807n) return INVALID;`);
			break;
		case "uint64": e.write(`if (${n} < 0n || ${n} > 18446744073709551615n) return INVALID;`);
	}
	return n;
}
function Ld(e, t) {
	return e.write(`if (typeof ${t} !== "symbol") return INVALID;`), t;
}
function Rd(e, t) {
	return e.write(`if (${t} !== undefined) return INVALID;`), t;
}
function zd(e, t) {
	return e.write(`if (${t} !== null) return INVALID;`), t;
}
function Bd(e, t) {
	return e.write(`if (${t} !== undefined) return INVALID;`), t;
}
function Vd(e, t) {
	return e.write(`if (typeof ${t} !== "number" || !Number.isNaN(${t})) return INVALID;`), t;
}
function Hd(e, t) {
	return e.write(`if (!(${t} instanceof Date) || Number.isNaN(${t}.getTime())) return INVALID;`), t;
}
function Ud(e, t, n, r, i = !0) {
	let a = n._zod.def;
	e.write(`if (typeof ${r} !== "object" || ${r} === null || Array.isArray(${r})) return INVALID;`);
	let o = a.shape, s = Object.keys(o), c = Object.getOwnPropertySymbols(o), l = c.length ? [...s, ...c] : s, u = (e) => typeof e == "symbol" ? L(t, e) : k(e), d = (e) => typeof e == "symbol" ? `[${u(e)}]` : k(e), f = o;
	if (s.includes("__proto__")) throw new I("object shape key \"__proto__\"");
	let ee = /* @__PURE__ */ new Map();
	for (let n of l) {
		let a = f[n], o = u(n), s = z(t);
		if (e.write(`const ${s} = ${r}[${o}];`), a._zod.optin !== void 0) {
			let i = z(t);
			e.write(`let ${i} = (() => {`), e.indented((e) => {
				let n = B(e, t, a, s);
				e.write(`return ${n};`);
			}), e.write("})();"), a._zod.optout === "optional" ? (e.write(`if (${i} === INVALID) {`), e.indented((e) => {
				e.write(`if (${o} in ${r}) return INVALID;`), e.write(`${i} = undefined;`);
			}), e.write("}")) : e.write(`if (${i} === INVALID) return INVALID;`), ee.set(n, i);
		} else {
			Kd(a) && e.write(`if (!(${o} in ${r})) return INVALID;`);
			let c = B(e, t, a, s, i);
			c !== null && ee.set(n, c);
		}
	}
	let p = a.catchall, m = "none";
	if (p) {
		let t = p._zod.def.type;
		if (t === "never") {
			let t = s.map((e) => `k !== ${k(e)}`).join(" && ") || "true";
			e.write(`for (const k in ${r}) {`), e.indented((e) => {
				e.write(`if (${t}) return INVALID;`);
			}), e.write("}");
		} else m = (t === "unknown" || t === "any") && !p._zod.def.checks?.length ? "passthrough" : "schema";
	}
	let h = z(t), te = l.some((e) => Yd(f[e]) || Jd(f[e]));
	if (!i) {
		if (m === "schema") {
			let n = s.length > 0 ? L(t, new Set(s)) : null;
			e.write(`for (const k in ${r}) {`), e.indented((e) => {
				e.write("if (k === \"__proto__\") continue;"), n && e.write(`if (${n}.has(k)) continue;`);
				let i = z(t);
				e.write(`const ${i} = ${r}[k];`), B(e, t, p, i, !1);
			}), e.write("}");
		}
		return null;
	}
	if (te) {
		e.write(`const ${h} = {};`);
		for (let t of l) {
			let n = u(t), i = ee.get(t);
			Jd(f[t]) ? e.write(`if (${n} in ${r}) ${h}[${n}] = ${i};`) : Yd(f[t]) ? e.write(`if (${i} !== undefined || ${n} in ${r}) ${h}[${n}] = ${i};`) : e.write(`${h}[${n}] = ${i};`);
		}
	} else {
		let t = l.map((e) => `${d(e)}: ${ee.get(e)}`).join(", ");
		e.write(`const ${h} = { ${t} };`);
	}
	if (m !== "none") {
		let n = s.length > 0 ? L(t, new Set(s)) : null;
		e.write(`for (const k in ${r}) {`), e.indented((e) => {
			if (e.write("if (k === \"__proto__\") continue;"), n && e.write(`if (${n}.has(k)) continue;`), m === "passthrough") e.write(`${h}[k] = ${r}[k];`);
			else {
				let n = z(t);
				e.write(`const ${n} = ${r}[k];`);
				let i = B(e, t, p, n);
				e.write(`${h}[k] = ${i};`);
			}
		}), e.write("}");
	}
	return h;
}
function Wd(e, t, n, r, i = !0) {
	let a = n._zod.def;
	if (Gd(n)) return H(e, t, a.innerType, r, i);
	if (a.innerType._zod.optin === "defaulted") {
		let n = z(t), i = z(t);
		return e.write(`let ${n};`), e.write(`if (${r} === undefined) {`), e.indented((e) => {
			e.write(`const ${i} = (() => {`), e.indented((e) => {
				let n = H(e, t, a.innerType, r);
				e.write(`return ${n};`);
			}), e.write("})();"), e.write(`if (${i} !== INVALID) ${n} = ${i};`);
		}), e.write("} else {"), e.indented((e) => {
			let i = H(e, t, a.innerType, r);
			e.write(`${n} = ${i};`);
		}), e.write("}"), n;
	}
	let o = i ? z(t) : null;
	return o && e.write(`let ${o};`), e.write(`if (${r} !== undefined) {`), e.indented((e) => {
		let n = H(e, t, a.innerType, r, i);
		o && n !== null && e.write(`${o} = ${n};`);
	}), e.write("}"), o;
}
function Gd(e) {
	return e._zod.traits?.has("$ZodExactOptional") === !0;
}
function Kd(e) {
	return e._zod.optin === void 0 && qd(e);
}
function qd(e) {
	if (e._zod.def.coerce) return !0;
	let t = e._zod.def;
	switch (t.type) {
		case "any":
		case "unknown":
		case "undefined":
		case "void":
		case "default":
		case "prefault":
		case "transform":
		case "custom":
		case "lazy": return !0;
		case "string":
		case "number":
		case "boolean":
		case "bigint":
		case "symbol":
		case "null":
		case "never":
		case "nan":
		case "date":
		case "object":
		case "array":
		case "tuple":
		case "record":
		case "map":
		case "set":
		case "file":
		case "template_literal": return !1;
		case "nonoptional": return t.innerType ? qd(t.innerType) : !1;
		case "literal": return !!t.values?.includes(void 0);
		case "enum": return !!e._zod.values?.has(void 0);
		case "optional":
		case "nullable":
		case "readonly":
		case "success": return !t.innerType || qd(t.innerType);
		case "catch": return !0;
		case "union": return !t.options || t.options.some(qd);
		case "intersection": return !t.left || !t.right || qd(t.left) && qd(t.right);
		case "pipe": return !t.in || qd(t.in);
		default: return !0;
	}
}
function Jd(e) {
	return e._zod.optin === "optional" && e._zod.optout === "optional";
}
function Yd(e) {
	return (e._zod.optin !== "defaulted" || e._zod.optout === "optional") && Xd(e);
}
function Xd(e) {
	let t = e._zod.def;
	switch (t.type) {
		case "string":
		case "number":
		case "boolean":
		case "bigint":
		case "symbol":
		case "null":
		case "nan":
		case "date":
		case "object":
		case "array":
		case "tuple":
		case "record":
		case "map":
		case "set":
		case "file":
		case "template_literal":
		case "never":
		case "success": return !1;
		case "literal": return !!t.values?.includes(void 0);
		case "enum": return !!e._zod.values?.has(void 0);
		case "optional": return !0;
		case "nullable":
		case "readonly":
		case "nonoptional": return !t.innerType || Xd(t.innerType);
		case "union": return !t.options || t.options.some(Xd);
		case "intersection": return !t.left || !t.right || Xd(t.left) || Xd(t.right);
		case "pipe": return !t.out || Xd(t.out);
		default: return !0;
	}
}
function Zd(e, t, n, r, i = !0) {
	let a = n._zod.def, o = i ? z(t) : null;
	return o && e.write(`let ${o} = null;`), e.write(`if (${r} !== null) {`), e.indented((e) => {
		let n = H(e, t, a.innerType, r, i);
		o && n !== null && e.write(`${o} = ${n};`);
	}), e.write("}"), o;
}
function Qd(e, t, n, r, i = !0) {
	let a = n._zod.def;
	e.write(`if (!Array.isArray(${r})) return INVALID;`);
	let o = i ? z(t) : null, s = z(t), c = z(t);
	return o && e.write(`const ${o} = new Array(${r}.length);`), e.write(`for (let ${s} = 0; ${s} < ${r}.length; ${s}++) {`), e.indented((e) => {
		e.write(`const ${c} = ${r}[${s}];`);
		let n = B(e, t, a.element, c, i);
		o && n !== null && e.write(`${o}[${s}] = ${n};`);
	}), e.write("}"), o;
}
function $d(e, t, n, r) {
	let i = n._zod.def.values;
	if (i.length !== 1) {
		let n = L(t, new Set(i));
		return e.write(`if (!${n}.has(${r})) return INVALID;`), r;
	}
	let a = i[0];
	if (typeof a == "number" && Number.isNaN(a)) {
		let n = L(t, new Set(i));
		return e.write(`if (!${n}.has(${r})) return INVALID;`), r;
	}
	if (typeof a == "string") e.write(`if (${r} !== ${k(a)}) return INVALID;`);
	else if (typeof a == "number" || typeof a == "boolean") e.write(`if (${r} !== ${a}) return INVALID;`);
	else if (a === null) e.write(`if (${r} !== null) return INVALID;`);
	else if (a === void 0) e.write(`if (${r} !== undefined) return INVALID;`);
	else if (typeof a == "bigint") e.write(`if (${r} !== ${a}n) return INVALID;`);
	else throw new I(`literal type ${typeof a}`);
	return r;
}
function ef(e, t, n, r) {
	let i = n._zod.values;
	if (!i) throw new I("enum schema without enumerated values");
	let a = L(t, i);
	return e.write(`if (!${a}.has(${r})) return INVALID;`), r;
}
function tf(e, t, n, r) {
	let i = n._zod.def;
	return H(e, t, i.innerType, r);
}
function nf(e, t, n, r) {
	let i = n._zod.def, a = Object.getOwnPropertyDescriptor(n._zod.def, "defaultValue") ? () => n._zod.def.defaultValue : void 0;
	if (n._zod.def.type === "prefault") {
		if (!a) return H(e, t, i.innerType, r);
		let n = L(t, a), o = z(t);
		return e.write(`let ${o} = ${r};`), e.write(`if (${r} === undefined) ${o} = ${n}();`), H(e, t, i.innerType, o);
	}
	let o = z(t);
	if (a) {
		let n = L(t, a), s = L(t, $o);
		e.write(`let ${o};`), e.write(`if (${r} === undefined) {`), e.indented((e) => {
			e.write(`${o} = ${s}(${n}());`);
		}), e.write("} else {"), e.indented((e) => {
			let a = H(e, t, i.innerType, r);
			e.write(`${o} = ${a} === undefined ? ${s}(${n}()) : ${a};`);
		}), e.write("}");
	} else e.write(`let ${o};`), e.write(`if (${r} !== undefined) {`), e.indented((e) => {
		let n = H(e, t, i.innerType, r);
		e.write(`${o} = ${n};`);
	}), e.write("}");
	return o;
}
function rf(e, t, n, r) {
	let i = n._zod.def, a = H(e, t, i.innerType, r), o = z(t);
	return e.write(`const ${o} = ${a};`), e.write(`if (${o} === undefined) return INVALID;`), o;
}
function af(e, t, n, r) {
	let i = n._zod.def, a = i.items, o = i.rest;
	e.write(`if (!Array.isArray(${r})) return INVALID;`);
	let s = of(a, "optin"), c = of(a, "optout");
	o ? e.write(`if (${r}.length < ${s}) return INVALID;`) : e.write(`if (${r}.length < ${s} || ${r}.length > ${a.length}) return INVALID;`);
	let l = z(t);
	e.write(`const ${l} = [];`);
	for (let n = 0; n < a.length; n++) {
		let i = a[n];
		if (n >= c) e.write(`if (${l}.length === ${n}) {`), e.indented((e) => {
			e.write(`if (${n} < ${r}.length) {`), e.indented((e) => {
				let a = z(t);
				e.write(`const ${a} = ${r}[${n}];`);
				let o = B(e, t, i, a);
				e.write(`${l}[${n}] = ${o};`);
			}), e.write("} else {"), e.indented((e) => {
				if (Jd(i)) {
					e.write(`${l}.length = ${n};`);
					return;
				}
				let r = z(t), a = z(t);
				e.write(`const ${r} = undefined;`), e.write(`const ${a} = (() => {`), e.indented((e) => {
					let n = B(e, t, i, r);
					e.write(`return ${n};`);
				}), e.write("})();"), e.write(`if (${a} === INVALID || ${a} === undefined) ${l}.length = ${n};`), e.write(`else ${l}[${n}] = ${a};`);
			}), e.write("}");
		}), e.write("}");
		else {
			let a = z(t);
			e.write(`const ${a} = ${r}[${n}];`);
			let o = B(e, t, i, a);
			e.write(`${l}[${n}] = ${o};`);
		}
	}
	if (o) {
		let n = z(t), i = z(t);
		e.write(`for (let ${n} = ${a.length}; ${n} < ${r}.length; ${n}++) {`), e.indented((e) => {
			e.write(`const ${i} = ${r}[${n}];`);
			let a = B(e, t, o, i);
			e.write(`${l}[${n}] = ${a};`);
		}), e.write("}");
	}
	return l;
}
function of(e, t) {
	for (let n = e.length - 1; n >= 0; n--) if (!(t === "optin" ? e[n]._zod.optin !== void 0 : e[n]._zod.optout === "optional")) return n + 1;
	return 0;
}
function sf(e, t, n, r) {
	let i = n._zod.def, a = i.options;
	if (i.discriminator) return cf(e, t, i, r);
	if (i.inclusive === !1) throw new I("exclusive unions (z.xor)");
	if (a.length === 0) return e.write("return INVALID;"), r;
	if (a.length === 1) return H(e, t, a[0], r);
	if (a.every((e) => e._zod.def.type === "literal" && !e._zod.def.checks?.length)) {
		let n = L(t, new Set(a.flatMap((e) => e._zod.def.values)));
		return e.write(`if (!${n}.has(${r})) return INVALID;`), r;
	}
	let o = z(t);
	e.write(`let ${o};`);
	for (let n = 0; n < a.length; n++) {
		let i = a[n];
		n === 0 ? e.write(`${o} = (() => {`) : e.write(`if (${o} === INVALID) ${o} = (() => {`), e.indented((e) => {
			let n = H(e, t, i, r);
			e.write(`return ${n};`);
		}), e.write("})();");
	}
	return e.write(`if (${o} === INVALID) return INVALID;`), o;
}
function cf(e, t, n, r) {
	if (n.unionFallback) throw new I("discriminated union with unionFallback");
	if (n.options.length === 0) return e.write("return INVALID;"), r;
	let i = z(t), a = z(t);
	e.write(`const ${i} = ${r}?.[${k(n.discriminator)}];`), e.write(`let ${a};`);
	let o = !0, s = /* @__PURE__ */ new Set();
	for (let c of n.options) {
		let l = c._zod.propValues?.[n.discriminator];
		if (!l || l.size === 0) throw new I("discriminated union option without static discriminator values");
		for (let e of l) {
			if (s.has(e)) throw new I(`duplicate discriminator value ${String(e)}`);
			s.add(e);
		}
		let u = Array.from(l, (e) => lf(t, i, e)), d = o ? "if" : "else if";
		e.write(`${d} (${u.join(" || ")}) {`), e.indented((e) => {
			let n = H(e, t, c, r);
			e.write(`${a} = ${n};`);
		}), e.write("}"), o = !1;
	}
	return e.write("else { return INVALID; }"), a;
}
function lf(e, t, n) {
	if (typeof n == "string") return `${t} === ${k(n)}`;
	if (typeof n == "number") return Number.isNaN(n) ? `Number.isNaN(${t})` : `${t} === ${n}`;
	if (typeof n == "boolean") return `${t} === ${n}`;
	if (n === null) return `${t} === null`;
	if (n === void 0) return `${t} === undefined`;
	if (typeof n == "bigint") return `${t} === ${n}n`;
	if (typeof n == "symbol") return `${t} === ${L(e, n)}`;
	throw new I(`literal discriminator value ${String(n)}`);
}
function uf(e, t, n, r) {
	let i = n._zod.def;
	t.definite = !1;
	let a = B(e, t, i.left, r), o = B(e, t, i.right, r), s = L(t, Ut), c = z(t);
	return e.write(`const ${c} = ${s}(${a}, ${o});`), e.write(`if (!${c}.valid) return INVALID;`), `${c}.data`;
}
function df(e, t, n, r) {
	let i = n._zod.def, a = L(t, ho);
	e.write(`if (!${a}(${r})) return INVALID;`);
	let o = z(t), s = z(t), c = z(t);
	e.write(`const ${o} = {};`);
	let l = i, u = l.partial ? void 0 : i.keyType._zod.values;
	if (u) {
		let n = [];
		for (let a of u) {
			if (typeof a != "string" && typeof a != "number" && typeof a != "symbol") throw new I(`record key value ${String(a)}`);
			let s = typeof a == "number" ? a.toString() : a;
			if (s === "__proto__") throw new I("record key \"__proto__\"");
			n.push(s);
			let c = L(t, a), l = H(e, t, i.keyType, c), u = z(t);
			e.write(`const ${u} = ${r}[${pf(t, s)}];`);
			let d = B(e, t, i.valueType, u);
			e.write(`${o}[${l}] = ${d};`);
		}
		let a = L(t, new Set(n));
		return e.write(`for (const ${s} in ${r}) {`), e.indented((e) => {
			e.write(`if (${a}.has(${s})) continue;`), l.mode === "loose" ? e.write(`if (${s} !== "__proto__") ${o}[${s}] = ${r}[${s}];`) : e.write("return INVALID;");
		}), e.write("}"), o;
	}
	let d = i.keyType._zod.def;
	if (d.type !== "string" || d.format !== void 0 || d.coerce || (d.checks?.length ?? 0) !== 0) {
		let n = i.mode === "loose", a = fd(i.keyType);
		a.definite === !1 && (t.definite = !1);
		let c = L(t, a), l = L(t, Oa), u = z(t);
		return ff(e, t, r, s, (e) => {
			e.write(`let ${u} = ${c}(${s});`), e.write(`if (${u} === INVALID && typeof ${s} === "string" && ${l}.test(${s})) ${u} = ${c}(Number(${s}));`), n ? e.write(`if (${u} === INVALID) { ${o}[${s}] = ${r}[${s}]; continue; }`) : e.write(`if (${u} === INVALID) return INVALID;`), e.write(`if (${u} === "__proto__") continue;`);
			let a = z(t);
			e.write(`const ${a} = ${r}[${s}];`);
			let d = B(e, t, i.valueType, a);
			e.write(`${o}[${u}] = ${d};`);
		}), o;
	}
	return ff(e, t, r, s, (e) => {
		e.write(`const ${c} = ${r}[${s}];`);
		let n = B(e, t, i.valueType, c);
		e.write(`${o}[${s}] = ${n};`);
	}, "return INVALID;"), o;
}
function ff(e, t, n, r, i, a) {
	let o = L(t, Object.prototype.propertyIsEnumerable), s = z(t), c = z(t), l = z(t);
	e.write(`const ${s} = Object.getOwnPropertySymbols(${n});`), e.write(`const ${c} = Object.getOwnPropertyNames(${n});`), e.write(`for (let ${l} = 0; ${l} < ${c}.length; ${l}++) {`), e.indented((e) => {
		e.write(`const ${r} = ${c}[${l}];`), e.write(`if (${r} === "__proto__" || !${o}.call(${n}, ${r})) continue;`), i(e);
	}), e.write("}"), e.write(`for (let ${l} = 0; ${l} < ${s}.length; ${l}++) {`), e.indented((e) => {
		e.write(`const ${r} = ${s}[${l}];`), e.write(`if (!${o}.call(${n}, ${r})) continue;`), a ? e.write(a) : i(e);
	}), e.write("}");
}
function pf(e, t) {
	return typeof t == "string" ? k(t) : L(e, t);
}
function mf(e, t, n, r) {
	let i = n._zod.def;
	e.write(`if (!(${r} instanceof Map)) return INVALID;`);
	let a = z(t), o = z(t), s = z(t);
	return e.write(`const ${a} = new Map();`), e.write(`for (const [${o}, ${s}] of ${r}) {`), e.indented((e) => {
		let n = H(e, t, i.keyType, o), r = H(e, t, i.valueType, s);
		e.write(`${a}.set(${n}, ${r});`);
	}), e.write("}"), a;
}
function hf(e, t, n, r) {
	let i = n._zod.def;
	e.write(`if (!(${r} instanceof Set)) return INVALID;`);
	let a = z(t), o = z(t);
	return e.write(`const ${a} = new Set();`), e.write(`for (const ${o} of ${r}) {`), e.indented((e) => {
		let n = H(e, t, i.valueType, o);
		e.write(`${a}.add(${n});`);
	}), e.write("}"), a;
}
function gf(e, t) {
	return e.write(`if (!(${t} instanceof File)) return INVALID;`), t;
}
function _f(e, t, n, r) {
	e.write(`if (typeof ${r} !== "string") return INVALID;`);
	let i = n._zod.pattern;
	if (i) {
		let n = L(t, i);
		e.write(`${n}.lastIndex = 0;`), e.write(`if (!${n}.test(${r})) return INVALID;`);
	}
	return r;
}
function vf(e, t, n, r) {
	let i = n._zod.def, a = R(t, i.getter), o = L(t, { parser: null });
	e.write(`if (!${o}.parser) {`), e.indented((e) => {
		e.write(`const inner = ${a}();`), e.write(`${o}.parser = function(input) {`), e.indented((e) => {
			e.write("const result = inner._zod.run({ value: input, issues: [] }, {});"), e.write("return result.issues.length === 0 ? result.value : INVALID;");
		}), e.write("};");
	}), e.write("}");
	let s = z(t);
	return e.write(`const ${s} = ${o}.parser(${r});`), e.write(`if (${s} === INVALID) return INVALID;`), s;
}
function yf(e, t, n, r) {
	let i = n._zod.def, a = H(e, t, i.in, r);
	if (i.transform) {
		if (bf(i.transform)) throw new F("z.compile: async transforms in pipes are not supported");
		let n = i.transform, r = R(t, (e) => {
			let t = {
				value: e,
				issues: [],
				addIssue: kd
			}, r = n(e, t);
			return r instanceof Promise ? P : t.issues.length === 0 ? r : P;
		}), o = z(t);
		return e.write(`const ${o} = ${r}(${a});`), e.write(`if (${o} === INVALID) return INVALID;`), H(e, t, i.out, o);
	}
	return H(e, t, i.out, a);
}
function bf(e) {
	return typeof e == "function" && (e.constructor.name === "AsyncFunction" || e[Symbol.toStringTag] === "AsyncFunction");
}
function xf(e, t, n, r) {
	let i = n._zod.def;
	if (i.fn) {
		if (bf(i.fn)) throw new F("z.compile: async custom predicates are not supported");
		let n = R(t, i.fn), a = L(t, Od), o = z(t);
		e.write(`const ${o} = ${n}(${r});`), e.write(`if (${o} instanceof Promise) ${a}();`), e.write(`if (!${o}) return INVALID;`);
	} else throw new I("custom schema without a predicate function");
	return r;
}
function Sf(e, t, n) {
	let r = e._zod.run({
		value: n,
		issues: []
	}, {});
	if (r && typeof r.then == "function") return P;
	let i = r;
	return i.issues.length === 0 ? i.value : t();
}
function Cf(e, t, n, r) {
	let i = n._zod.def;
	if (!i.catchValue["~constantCatch"]) throw new I("catch with a callback (only a constant catch value compiles)", !1);
	let a = z(t);
	e.write(`let ${a} = (() => {`), e.indented((e) => {
		let n = B(e, t, i.innerType, r);
		e.write(`return ${n};`);
	}), e.write("})();");
	let o = L(t, i.innerType), s = R(t, i.catchValue), c = L(t, Sf);
	return e.write(`if (${a} === INVALID) {`), e.indented((e) => {
		e.write(`${a} = ${c}(${o}, ${s}, ${r});`), e.write(`if (${a} === INVALID) return INVALID;`);
	}), e.write("}"), a;
}
function wf(e, t, n, r) {
	let i = n._zod.def;
	if (i.transform) {
		if (bf(i.transform)) throw new F("z.compile: async transforms are not supported");
		let n = i.transform, a = R(t, (e) => {
			let t = {
				value: e,
				issues: [],
				addIssue: kd
			}, r = n(e, t);
			return r instanceof Promise ? P : t.issues.length === 0 ? r : P;
		}), o = z(t);
		return e.write(`const ${o} = ${a}(${r});`), e.write(`if (${o} === INVALID) return INVALID;`), o;
	}
	return r;
}
//#endregion
//#region node_modules/zod/v4/core/json-schema-generator.js
var Tf = class {
	get metadataRegistry() {
		return this.ctx.metadataRegistry;
	}
	get target() {
		return this.ctx.target;
	}
	get unrepresentable() {
		return this.ctx.unrepresentable;
	}
	get override() {
		return this.ctx.override;
	}
	get io() {
		return this.ctx.io;
	}
	get counter() {
		return this.ctx.counter;
	}
	set counter(e) {
		this.ctx.counter = e;
	}
	get seen() {
		return this.ctx.seen;
	}
	constructor(e) {
		let t = e?.target ?? "draft-2020-12";
		t === "draft-4" && (t = "draft-04"), t === "draft-7" && (t = "draft-07"), this.ctx = gt({
			processors: so,
			target: t,
			...e?.metadata && { metadata: e.metadata },
			...e?.unrepresentable && { unrepresentable: e.unrepresentable },
			...e?.override && { override: e.override },
			...e?.io && { io: e.io }
		});
	}
	process(e, t = {
		path: [],
		schemaPath: []
	}) {
		return Ht(e, this.ctx, t);
	}
	emit(e, t) {
		t && (t.cycles && (this.ctx.cycles = t.cycles), t.reused && (this.ctx.reused = t.reused), t.external && (this.ctx.external = t.external)), this.ctx.sharedDefsExtractedFor = void 0, this.ctx.sharedEmitDoneFor = void 0, on(this.ctx, e);
		let { "~standard": n, ...r } = Sn(this.ctx, e);
		return r;
	}
}, Ef = /* @__PURE__ */ e({}), Df = /* @__PURE__ */ e({
	$ZodAny: () => Ha,
	$ZodArray: () => yi,
	$ZodAsyncError: () => Pr,
	$ZodBase64: () => Ko,
	$ZodBase64URL: () => Os,
	$ZodBigInt: () => Ri,
	$ZodBigIntFormat: () => _o,
	$ZodBoolean: () => Ks,
	$ZodCIDRv4: () => Ji,
	$ZodCIDRv6: () => fa,
	$ZodCUID: () => ts,
	$ZodCUID2: () => Eo,
	$ZodCatch: () => Ma,
	$ZodCheck: () => Pt,
	$ZodCheckBigIntFormat: () => fs,
	$ZodCheckEndsWith: () => Tt,
	$ZodCheckGreaterThan: () => Jr,
	$ZodCheckIncludes: () => zr,
	$ZodCheckLengthEquals: () => ri,
	$ZodCheckLessThan: () => Mn,
	$ZodCheckLowerCase: () => l,
	$ZodCheckMaxLength: () => $i,
	$ZodCheckMaxSize: () => Ns,
	$ZodCheckMimeType: () => Mo,
	$ZodCheckMinLength: () => gs,
	$ZodCheckMinSize: () => Ra,
	$ZodCheckMultipleOf: () => gi,
	$ZodCheckNumberFormat: () => Ho,
	$ZodCheckOverwrite: () => Ts,
	$ZodCheckProperties: () => Pi,
	$ZodCheckProperty: () => po,
	$ZodCheckRegex: () => Us,
	$ZodCheckSizeEquals: () => Wi,
	$ZodCheckStartsWith: () => la,
	$ZodCheckStringFormat: () => Zo,
	$ZodCheckUpperCase: () => Co,
	$ZodCodec: () => Sa,
	$ZodCreditCard: () => ui,
	$ZodCustom: () => tc,
	$ZodCustomStringFormat: () => Dc,
	$ZodCyclicError: () => ra,
	$ZodDate: () => Oi,
	$ZodDefault: () => vc,
	$ZodDiscriminatedUnion: () => qn,
	$ZodE164: () => ve,
	$ZodEmail: () => uc,
	$ZodEmoji: () => nr,
	$ZodEncodeError: () => Qr,
	$ZodEnum: () => Ie,
	$ZodError: () => cr,
	$ZodExactOptional: () => De,
	$ZodFile: () => pn,
	$ZodFunction: () => oo,
	$ZodGUID: () => h,
	$ZodIBAN: () => Za,
	$ZodIPv4: () => Yt,
	$ZodIPv6: () => an,
	$ZodISODate: () => xn,
	$ZodISODateTime: () => Ue,
	$ZodISODuration: () => ht,
	$ZodISOTime: () => Vt,
	$ZodIntersection: () => zn,
	$ZodJWT: () => Ic,
	$ZodKSUID: () => le,
	$ZodLazy: () => yr,
	$ZodLiteral: () => st,
	$ZodMAC: () => ur,
	$ZodMap: () => Or,
	$ZodNaN: () => Qe,
	$ZodNanoID: () => Mt,
	$ZodNever: () => us,
	$ZodNonOptional: () => Ct,
	$ZodNull: () => Kr,
	$ZodNullable: () => Lr,
	$ZodNumber: () => ti,
	$ZodNumberFormat: () => An,
	$ZodObject: () => s,
	$ZodObjectJIT: () => aa,
	$ZodOptional: () => zs,
	$ZodPipe: () => Ro,
	$ZodPrefault: () => xs,
	$ZodPreprocess: () => Wa,
	$ZodPromise: () => xi,
	$ZodReadonly: () => qo,
	$ZodRealError: () => Tr,
	$ZodRecord: () => ks,
	$ZodRegistry: () => bt,
	$ZodSet: () => Bi,
	$ZodString: () => yo,
	$ZodStringFormat: () => Js,
	$ZodSuccess: () => Xi,
	$ZodSymbol: () => ma,
	$ZodTemplateLiteral: () => rs,
	$ZodTransform: () => Oo,
	$ZodTuple: () => Pa,
	$ZodType: () => wa,
	$ZodULID: () => fi,
	$ZodURL: () => rc,
	$ZodUUID: () => kc,
	$ZodUndefined: () => Ai,
	$ZodUnion: () => bc,
	$ZodUnknown: () => Yn,
	$ZodVoid: () => be,
	$ZodXID: () => fc,
	$ZodXor: () => ir,
	$brand: () => Dn,
	$constructor: () => i,
	$input: () => Hr,
	$output: () => Nr,
	Doc: () => et,
	INVALID: () => P,
	JSONSchema: () => Ef,
	JSONSchemaGenerator: () => Tf,
	NEVER: () => ta,
	TimePrecision: () => Bn,
	URL_BAD_FORMAT: () => 1,
	URL_UNPARSEABLE: () => 2,
	ZodCompileAsyncError: () => F,
	ZodCompileUnsupportedError: () => I,
	_any: () => Lc,
	_array: () => ue,
	_base64: () => br,
	_base64url: () => ct,
	_bigint: () => dr,
	_boolean: () => kr,
	_catch: () => $e,
	_check: () => Nt,
	_cidrv4: () => ds,
	_cidrv6: () => wt,
	_coercedBigint: () => qr,
	_coercedBoolean: () => Rr,
	_coercedDate: () => ni,
	_coercedNumber: () => jn,
	_coercedString: () => c,
	_creditCard: () => ea,
	_cuid: () => Ps,
	_cuid2: () => No,
	_custom: () => _s,
	_date: () => za,
	_decode: () => si,
	_decodeAsync: () => Qs,
	_default: () => _i,
	_discriminatedUnion: () => Uo,
	_e164: () => Es,
	_email: () => Fi,
	_emoji: () => mo,
	_encode: () => wc,
	_encodeAsync: () => Ti,
	_endsWith: () => Ws,
	_enum: () => Gi,
	_file: () => ua,
	_float32: () => Qo,
	_float64: () => wo,
	_gt: () => ka,
	_gte: () => ya,
	_guid: () => ci,
	_iban: () => $s,
	_includes: () => Tc,
	_int: () => Ei,
	_int32: () => _c,
	_int64: () => Kn,
	_intersection: () => ge,
	_ipv4: () => cc,
	_ipv6: () => er,
	_isoDate: () => Pe,
	_isoDateTime: () => Te,
	_isoDuration: () => dn,
	_isoTime: () => io,
	_jwt: () => p,
	_ksuid: () => Ya,
	_lazy: () => qt,
	_length: () => nn,
	_literal: () => bn,
	_lowercase: () => He,
	_lt: () => mt,
	_lte: () => zt,
	_mac: () => Ln,
	_map: () => Pc,
	_max: () => zt,
	_maxLength: () => se,
	_maxSize: () => _r,
	_mime: () => at,
	_min: () => ya,
	_minLength: () => lr,
	_minSize: () => Er,
	_multipleOf: () => Xe,
	_nan: () => At,
	_nanoid: () => cs,
	_nativeEnum: () => St,
	_negative: () => Wr,
	_never: () => Fr,
	_nonnegative: () => $r,
	_nonoptional: () => On,
	_nonpositive: () => a,
	_normalize: () => Qi,
	_null: () => Ms,
	_nullable: () => jo,
	_number: () => hs,
	_optional: () => La,
	_overwrite: () => hi,
	_parse: () => gc,
	_parseAsync: () => Gn,
	_pipe: () => Vo,
	_positive: () => ws,
	_promise: () => Ni,
	_properties: () => fo,
	_property: () => Hs,
	_readonly: () => Ui,
	_record: () => ca,
	_refine: () => Xo,
	_regex: () => So,
	_safeDecode: () => he,
	_safeDecodeAsync: () => sc,
	_safeEncode: () => $n,
	_safeEncodeAsync: () => Ne,
	_safeParse: () => we,
	_safeParseAsync: () => un,
	_set: () => Da,
	_size: () => _a,
	_slugify: () => oi,
	_startsWith: () => Zs,
	_string: () => Cc,
	_stringFormat: () => wi,
	_stringbool: () => hc,
	_success: () => Wn,
	_superRefine: () => me,
	_symbol: () => oc,
	_templateLiteral: () => Qn,
	_toLowerCase: () => Me,
	_toUpperCase: () => Ce,
	_transform: () => ln,
	_trim: () => no,
	_tuple: () => f,
	_uint32: () => qa,
	_uint64: () => Gt,
	_ulid: () => en,
	_undefined: () => vn,
	_union: () => Be,
	_unknown: () => ft,
	_uppercase: () => Lt,
	_url: () => Fn,
	_uuid: () => Mc,
	_uuidv4: () => ae,
	_uuidv6: () => hr,
	_uuidv7: () => rt,
	_void: () => sr,
	_xid: () => wr,
	_xor: () => Je,
	base64Charset: () => Re,
	base64urlCharset: () => ke,
	canParseURL: () => hn,
	clone: () => T,
	compile: () => ld,
	compileFn: () => fd,
	config: () => Fs,
	createStandardJSONSchemaMethod: () => Qa,
	createToJSONSchemaMethod: () => Xt,
	decode: () => ro,
	decodeAsync: () => ee,
	describe: () => Ot,
	encode: () => Ja,
	encodeAsync: () => Kt,
	extractDefs: () => on,
	finalize: () => Sn,
	flattenError: () => Ye,
	formatError: () => kt,
	getDiscriminatedOption: () => co,
	globalConfig: () => Po,
	globalRegistry: () => Zr,
	handleUnrepresentable: () => We,
	initializeContext: () => gt,
	isBackEdge: () => Ls,
	isRecursiveSchema: () => Io,
	isValidBase64: () => ne,
	isValidBase64URL: () => $a,
	isValidCIDRv6: () => Zt,
	isValidCreditCard: () => sn,
	isValidIBAN: () => Cn,
	isValidIPv6: () => Ge,
	isValidJWT: () => _t,
	locales: () => od,
	memoizer: () => ys,
	mergeValues: () => Ut,
	meta: () => os,
	parse: () => tn,
	parseAsync: () => yn,
	parseURLObject: () => Vn,
	prettifyError: () => ss,
	process: () => Ht,
	processSchema: () => Ht,
	regexes: () => va,
	registry: () => En,
	safeDecode: () => Ve,
	safeDecodeAsync: () => pt,
	safeEncode: () => Rt,
	safeEncodeAsync: () => In,
	safeParse: () => Nc,
	safeParseAsync: () => oe,
	standardProps: () => Rc,
	stripTabAndNewline: () => de,
	toDotPath: () => xt,
	toJSONSchema: () => te,
	toZod: () => Aa,
	treeifyError: () => Ur,
	urlHostnameOk: () => xr,
	urlProtocolOk: () => lt,
	util: () => ba,
	validate: () => gr,
	validateAsync: () => it,
	validateURL: () => fr,
	version: () => Ar,
	withParser: () => ud
}), Of = /* @__PURE__ */ e({
	endsWith: () => Ws,
	gt: () => ka,
	gte: () => ya,
	includes: () => Tc,
	length: () => nn,
	lowercase: () => He,
	lt: () => mt,
	lte: () => zt,
	maxLength: () => se,
	maxSize: () => _r,
	mime: () => at,
	minLength: () => lr,
	minSize: () => Er,
	multipleOf: () => Xe,
	negative: () => Wr,
	nonnegative: () => $r,
	nonpositive: () => a,
	normalize: () => Qi,
	overwrite: () => hi,
	positive: () => ws,
	properties: () => fo,
	property: () => Hs,
	regex: () => So,
	size: () => _a,
	slugify: () => oi,
	startsWith: () => Zs,
	toLowerCase: () => Me,
	toUpperCase: () => Ce,
	trim: () => no,
	uppercase: () => Lt
}), kf = {
	invalid_type: "invalid_type",
	too_big: "too_big",
	too_small: "too_small",
	invalid_format: "invalid_format",
	not_multiple_of: "not_multiple_of",
	unrecognized_keys: "unrecognized_keys",
	invalid_union: "invalid_union",
	invalid_key: "invalid_key",
	invalid_element: "invalid_element",
	invalid_value: "invalid_value",
	custom: "custom"
};
function Af(e) {
	Fs({ customError: e });
}
function jf() {
	return Fs().customError;
}
var Mf;
Mf || (Mf = {});
//#endregion
//#region node_modules/zod/v4/classic/iso.js
var Nf = /* @__PURE__ */ e({
	ZodISODate: () => Ka,
	ZodISODateTime: () => Wt,
	ZodISODuration: () => $t,
	ZodISOTime: () => _n,
	date: () => Ff,
	datetime: () => Pf,
	duration: () => Lf,
	time: () => If
});
function Pf(e) {
	return Te(Wt, e);
}
function Ff(e) {
	return Pe(Ka, e);
}
function If(e) {
	return io(_n, e);
}
function Lf(e) {
	return dn($t, e);
}
//#endregion
//#region node_modules/zod/v4/classic/from-json-schema.js
var U = {
	...Ze,
	...Of,
	iso: Nf
}, Rf = /*@__PURE__*/ new Set(/* @__PURE__ */ "$schema.$ref.$defs.definitions.$id.id.$comment.$anchor.$vocabulary.$dynamicRef.$dynamicAnchor.type.enum.const.anyOf.oneOf.allOf.not.properties.required.additionalProperties.patternProperties.propertyNames.minProperties.maxProperties.items.prefixItems.additionalItems.minItems.maxItems.uniqueItems.contains.minContains.maxContains.minLength.maxLength.pattern.format.minimum.maximum.exclusiveMinimum.exclusiveMaximum.multipleOf.description.default.contentEncoding.contentMediaType.contentSchema.unevaluatedItems.unevaluatedProperties.if.then.else.dependentSchemas.dependentRequired.nullable.readOnly".split("."));
function zf(e, t) {
	let n = e.$schema;
	return n === "https://json-schema.org/draft/2020-12/schema" ? "draft-2020-12" : n === "http://json-schema.org/draft-07/schema#" ? "draft-7" : n === "http://json-schema.org/draft-04/schema#" ? "draft-4" : t ?? "draft-2020-12";
}
function Bf(e, t) {
	return e.map((e, n) => n < t ? e : e.optional());
}
function Vf(e) {
	return e.replace(/~1/g, "/").replace(/~0/g, "~");
}
function Hf(e, t) {
	if (!e.startsWith("#")) throw Error("External $ref is not supported, only local refs (#/...) are allowed");
	let n = e.slice(1).split("/").filter(Boolean);
	if (n.length === 0) return t.rootSchema;
	let r = t.version === "draft-2020-12" ? "$defs" : "definitions";
	if (n[0] === r) {
		let r = n[1] === void 0 ? void 0 : Vf(n[1]);
		if (!r || !t.defs[r]) throw Error(`Reference not found: ${e}`);
		return t.defs[r];
	}
	throw Error(`Reference not found: ${e}`);
}
function Uf(e, t) {
	return U.transform((e) => e).check((n) => {
		let r = n.value;
		if (typeof r != "object" || !r || Array.isArray(r)) return;
		let i = Object.getOwnPropertyNames(r);
		if (t.minProperties !== void 0 && i.length < t.minProperties && n.issues.push({
			origin: "object",
			code: "too_small",
			minimum: t.minProperties,
			inclusive: !0,
			message: `Too small: expected object to have >=${t.minProperties} properties`,
			input: r,
			inst: e,
			continue: !0
		}), t.maxProperties !== void 0 && i.length > t.maxProperties && n.issues.push({
			origin: "object",
			code: "too_big",
			maximum: t.maxProperties,
			inclusive: !0,
			message: `Too big: expected object to have <=${t.maxProperties} properties`,
			input: r,
			inst: e,
			continue: !0
		}), t.keySchema) for (let e of i) {
			let r = t.keySchema.safeParse(e);
			r.success || n.issues.push({
				code: "invalid_key",
				origin: "record",
				issues: r.error.issues,
				input: e,
				path: [e],
				continue: !0
			});
		}
	}).pipe(e);
}
function Wf(e, t) {
	if (e === null) return "z";
	let n = typeof e;
	if (n !== "object") {
		if (n === "number" && Number.isNaN(e)) return null;
		let t = String(e);
		return `${n[0]}${t.length}:${t}`;
	}
	if (t.has(e)) return null;
	t.add(e);
	try {
		if (Array.isArray(e)) {
			let n = [];
			for (let r of e) {
				let e = Wf(r, t);
				if (e === null) return null;
				n.push(e);
			}
			return `a${n.length}:[${n.join(",")}]`;
		}
		let n = Object.keys(e).sort(), r = [];
		for (let i of n) {
			let n = Wf(e[i], t);
			if (n === null) return null;
			r.push(`${i.length}:${i}=${n}`);
		}
		return `o${r.length}:{${r.join(",")}}`;
	} finally {
		t.delete(e);
	}
}
var Gf = /*@__PURE__*/ new Set([
	"items",
	"prefixItems",
	"additionalItems",
	"additionalProperties",
	"contains",
	"propertyNames",
	"not",
	"if",
	"then",
	"else",
	"allOf",
	"anyOf",
	"oneOf",
	"unevaluatedItems",
	"unevaluatedProperties",
	"contentSchema"
]), Kf = /*@__PURE__*/ new Set([
	"properties",
	"patternProperties",
	"dependentSchemas",
	"dependencies",
	"$defs",
	"definitions"
]);
function qf(e) {
	return typeof e != "object" || !e ? !1 : Array.isArray(e) ? e.some(qf) : typeof e.$ref == "string" || Object.entries(e).some(([e, t]) => Gf.has(e) ? qf(t) : !Kf.has(e) || typeof t != "object" || !t ? !1 : Object.values(t).some(qf));
}
function Jf(e) {
	return e === 1 ? "element" : "elements";
}
function Yf(e, t) {
	return U.transform((e) => e).check((e) => {
		let n = e.value;
		if (Array.isArray(n)) {
			if (t.uniqueItems === !0) {
				let t = /* @__PURE__ */ new Map();
				for (let r = 0; r < n.length; r++) {
					let i = Wf(n[r], /* @__PURE__ */ new Set());
					if (i === null) continue;
					let a = t.get(i);
					if (a === void 0) {
						t.set(i, r);
						continue;
					}
					e.issues.push({
						code: "custom",
						message: `Array items must be unique: element at index ${r} duplicates the one at index ${a}`,
						input: n,
						path: [r],
						continue: !0
					});
				}
			}
			if (t.containsSchema) {
				let r = t.minContains ?? 1, i = t.maxContains === void 0 ? Infinity : t.maxContains + 1, a = 0;
				for (let e of n) if (t.containsSchema.safeParse(e).success && ++a >= i) break;
				a < r && e.issues.push({
					code: "custom",
					message: `Array must contain at least ${r} matching ${Jf(r)}; found ${a}`,
					input: n,
					continue: !0
				}), t.maxContains !== void 0 && a > t.maxContains && e.issues.push({
					code: "custom",
					message: `Array must contain at most ${t.maxContains} matching ${Jf(t.maxContains)}`,
					input: n,
					continue: !0
				});
			}
		}
	}).pipe(e);
}
function Xf(e, t) {
	if (e !== !1) return e === void 0 || e === !0 ? U.any() : W(e, t);
}
var Zf = /^(?:[01]\d|2[0-3]):[0-5]\d:[0-5]\d(?:\.\d+)?(?:Z|[+-](?:[01]\d|2[0-3]):[0-5]\d)$/;
function Qf(e, t) {
	if (e.not !== void 0) {
		if (typeof e.not == "object" && Object.keys(e.not).length === 0) return U.never();
		throw Error("not is not supported in Zod (except { not: {} } for never)");
	}
	if (e.unevaluatedItems !== void 0) throw Error("unevaluatedItems is not supported");
	if (e.unevaluatedProperties !== void 0) throw Error("unevaluatedProperties is not supported");
	if (e.if !== void 0 || e.then !== void 0 || e.else !== void 0) throw Error("Conditional schemas (if/then/else) are not supported");
	if (e.dependentSchemas !== void 0 || e.dependentRequired !== void 0) throw Error("dependentSchemas and dependentRequired are not supported");
	if (e.$ref) {
		let n = e.$ref;
		if (t.refs.has(n)) return t.refs.get(n);
		if (t.processing.has(n)) return U.lazy(() => {
			if (!t.refs.has(n)) throw Error(`Circular reference not resolved: ${n}`);
			return t.refs.get(n);
		});
		t.processing.add(n);
		let r = W(Hf(n, t), t);
		return t.refs.set(n, r), t.processing.delete(n), r;
	}
	if (e.enum !== void 0) {
		let n = e.enum;
		if (t.version === "openapi-3.0" && e.nullable === !0 && n.length === 1 && n[0] === null) return U.null();
		if (n.length === 0) return U.never();
		if (n.length === 1) return U.literal(n[0]);
		if (n.every((e) => typeof e == "string")) return U.enum(n);
		let r = n.map((e) => U.literal(e));
		return r.length < 2 ? r[0] : U.union([
			r[0],
			r[1],
			...r.slice(2)
		]);
	}
	if (e.const !== void 0) return U.literal(e.const);
	let n = e.type;
	if (Array.isArray(n)) {
		let r = n.map((n) => Qf({
			...e,
			type: n
		}, t));
		return r.length === 0 ? U.never() : r.length === 1 ? r[0] : U.union(r);
	}
	if (!n) return U.any();
	let r;
	switch (n) {
		case "string": {
			let t = U.string();
			if (e.format) {
				let n = e.format;
				n === "email" ? t = t.check(U.email()) : n === "uri" || n === "uri-reference" ? t = t.check(U.url()) : n === "uuid" || n === "guid" ? t = t.check(U.uuid()) : n === "date-time" ? t = t.check(U.iso.datetime({ offset: !0 })) : n === "date" ? t = t.check(U.iso.date()) : n === "time" ? t = t.check(U.regex(Zf)) : n === "duration" ? t = t.check(U.iso.duration()) : n === "hostname" ? t = t.check(U.hostname()) : n === "ipv4" ? t = t.check(U.ipv4()) : n === "ipv6" ? t = t.check(U.ipv6()) : n === "mac" ? t = t.check(U.mac()) : n === "cidr" ? t = t.check(U.cidrv4()) : n === "cidr-v6" ? t = t.check(U.cidrv6()) : n === "base64" ? t = t.check(U.base64()) : n === "base64url" ? t = t.check(U.base64url()) : n === "e164" ? t = t.check(U.e164()) : n === "credit_card" ? t = t.check(U.creditCard()) : n === "iban" ? t = t.check(U.iban()) : n === "jwt" ? t = t.check(U.jwt()) : n === "emoji" ? t = t.check(U.emoji()) : n === "nanoid" ? t = t.check(U.nanoid()) : n === "cuid" ? t = t.check(U.cuid()) : n === "cuid2" ? t = t.check(U.cuid2()) : n === "ulid" ? t = t.check(U.ulid()) : n === "xid" ? t = t.check(U.xid()) : n === "ksuid" && (t = t.check(U.ksuid()));
			}
			typeof e.minLength == "number" && (t = t.min(e.minLength)), typeof e.maxLength == "number" && (t = t.max(e.maxLength)), e.pattern && (t = t.regex(new RegExp(e.pattern))), r = t;
			break;
		}
		case "number":
		case "integer": {
			let t = n === "integer" ? U.number().int() : U.number();
			typeof e.minimum == "number" && e.exclusiveMinimum !== !0 && (t = t.min(e.minimum)), typeof e.maximum == "number" && e.exclusiveMaximum !== !0 && (t = t.max(e.maximum)), typeof e.exclusiveMinimum == "number" ? t = t.gt(e.exclusiveMinimum) : e.exclusiveMinimum === !0 && typeof e.minimum == "number" && (t = t.gt(e.minimum)), typeof e.exclusiveMaximum == "number" ? t = t.lt(e.exclusiveMaximum) : e.exclusiveMaximum === !0 && typeof e.maximum == "number" && (t = t.lt(e.maximum)), typeof e.multipleOf == "number" && (t = t.multipleOf(e.multipleOf)), r = t;
			break;
		}
		case "boolean":
			r = U.boolean();
			break;
		case "null":
			r = U.null();
			break;
		case "object": {
			let n = {}, i = e.properties || {}, a = new Set(e.required || []), o = typeof e.additionalProperties == "object" ? W(e.additionalProperties, t) : void 0;
			for (let [e, r] of Object.entries(i)) {
				let i = W(r, t);
				Ba(n, e, a.has(e) ? i : i.optional());
			}
			if (e.patternProperties) {
				let i = e.patternProperties, a = Object.keys(i), o = [];
				for (let e of a) {
					let n = W(i[e], t), r = U.string().regex(new RegExp(e));
					o.push(U.looseRecord(r, n));
				}
				let s = [];
				if (Object.keys(n).length > 0 && s.push(U.object(n).passthrough()), s.push(...o), s.length === 0) r = U.object({}).passthrough();
				else if (s.length === 1) r = s[0];
				else {
					let e = U.intersection(s[0], s[1]);
					for (let t = 2; t < s.length; t++) e = U.intersection(e, s[t]);
					r = e;
				}
				if (e.additionalProperties === !1) {
					let e = Object.keys(n), t = a.map((e) => new RegExp(e)), i = r;
					r = r.check((n) => {
						if (!ho(n.value)) return;
						let r = [];
						for (let i of Object.keys(n.value)) e.includes(i) || t.some((e) => e.test(i)) || r.push(i);
						r.length && n.issues.push({
							code: "unrecognized_keys",
							keys: r,
							input: n.value,
							inst: i
						});
					});
				}
			} else {
				let t = U.object(n);
				r = e.additionalProperties === !1 ? t.strict() : o ? t.catchall(o) : t.passthrough();
			}
			let s = e.propertyNames !== void 0 && e.propertyNames !== !0, c = typeof e.minProperties == "number" ? e.minProperties : void 0, l = typeof e.maxProperties == "number" ? e.maxProperties : void 0;
			if (s || c !== void 0 || l !== void 0) {
				let n;
				s && (n = W(typeof e.propertyNames == "object" && e.propertyNames.type === void 0 ? {
					type: "string",
					...e.propertyNames
				} : e.propertyNames, t)), r = Uf(r, {
					keySchema: n,
					minProperties: c,
					maxProperties: l
				});
			}
			break;
		}
		case "array": {
			let n = e.prefixItems, i = e.items;
			if (n && Array.isArray(n)) {
				let a = typeof e.minItems == "number" ? e.minItems : 0, o = Bf(n.map((e) => W(e, t)), a), s = Array.isArray(i) ? void 0 : Xf(i, t), c = U.tuple(o);
				r = s ? c.rest(s) : c, typeof e.minItems == "number" && (r = r.check(U.minLength(e.minItems))), typeof e.maxItems == "number" && (r = r.check(U.maxLength(e.maxItems)));
			} else if (Array.isArray(i)) {
				let n = typeof e.minItems == "number" ? e.minItems : 0, a = Bf(i.map((e) => W(e, t)), n), o = Xf(e.additionalItems, t), s = U.tuple(a);
				r = o ? s.rest(o) : s, typeof e.minItems == "number" && (r = r.check(U.minLength(e.minItems))), typeof e.maxItems == "number" && (r = r.check(U.maxLength(e.maxItems)));
			} else if (i !== void 0) {
				let n = W(i, t), a = U.array(n);
				typeof e.minItems == "number" && (a = a.min(e.minItems)), typeof e.maxItems == "number" && (a = a.max(e.maxItems)), r = a;
			} else r = U.array(U.any());
			(e.uniqueItems === !0 || e.contains !== void 0) && (r = Yf(r, {
				uniqueItems: e.uniqueItems === !0,
				containsSchema: e.contains === void 0 ? void 0 : W(e.contains, t),
				minContains: typeof e.minContains == "number" ? e.minContains : void 0,
				maxContains: typeof e.maxContains == "number" ? e.maxContains : void 0
			}));
			break;
		}
		default: throw Error(`Unsupported type: ${n}`);
	}
	return r;
}
function W(e, t) {
	if (typeof e == "boolean") return e ? U.any() : U.never();
	let n = Qf(e, t), r = e.type || e.enum !== void 0 || e.const !== void 0;
	if (e.anyOf && Array.isArray(e.anyOf)) {
		let i = e.anyOf.map((e) => W(e, t)), a = U.union(i);
		n = r ? U.intersection(n, a) : a;
	}
	if (e.oneOf && Array.isArray(e.oneOf)) {
		let i = e.oneOf.map((e) => W(e, t)), a = U.xor(i);
		n = r ? U.intersection(n, a) : a;
	}
	if (e.allOf && Array.isArray(e.allOf)) {
		if (e.allOf.length === 0) n = r ? n : U.any();
		else {
			let i = r ? n : W(e.allOf[0], t), a = +!r;
			for (let n = a; n < e.allOf.length; n++) i = U.intersection(i, W(e.allOf[n], t));
			n = i;
		}
	}
	e.nullable === !0 && t.version === "openapi-3.0" && (n = U.nullable(n)), e.readOnly === !0 && (n = U.readonly(n)), e.default !== void 0 && (n = n.default(e.default));
	let i = {};
	for (let t of [
		"$id",
		"id",
		"$comment",
		"$anchor",
		"$vocabulary",
		"$dynamicRef",
		"$dynamicAnchor"
	]) t in e && (i[t] = e[t]);
	for (let t of [
		"contentEncoding",
		"contentMediaType",
		"contentSchema"
	]) t in e && (i[t] = e[t]);
	if (e.type === "object" && e.$ref === void 0) {
		e.propertyNames !== void 0 && !qf(e.propertyNames) && (i.propertyNames = e.propertyNames);
		for (let t of ["minProperties", "maxProperties"]) e[t] !== void 0 && (i[t] = e[t]);
	}
	if (e.type === "array" && e.$ref === void 0) {
		e.contains !== void 0 && !qf(e.contains) && (i.contains = e.contains);
		for (let t of [
			"uniqueItems",
			"minContains",
			"maxContains"
		]) e[t] !== void 0 && (i[t] = e[t]);
	}
	for (let t of Object.keys(e)) Rf.has(t) || Ba(i, t, e[t]);
	return Object.keys(i).length > 0 && t.registry.add(n, i), e.description && (n = n.describe(e.description)), n;
}
function $f(e, t) {
	if (typeof e == "boolean") return e ? U.any() : U.never();
	let n;
	try {
		n = JSON.parse(JSON.stringify(e));
	} catch {
		throw Error("fromJSONSchema input is not valid JSON (possibly cyclic); use $defs/$ref for recursive schemas");
	}
	let r = {
		version: zf(n, t?.defaultTarget),
		defs: n.$defs || n.definitions || {},
		refs: /* @__PURE__ */ new Map(),
		processing: /* @__PURE__ */ new Set(),
		rootSchema: n,
		registry: t?.registry ?? Zr
	};
	return W(n, r);
}
//#endregion
//#region node_modules/zod/v4/core/visit.js
var ep = Symbol("z.visit/resolving");
function tp(e, t) {
	let n = typeof t == "function" ? t : (e, n) => {
		let r = t[e._zod.def.type];
		return r ? r(e, n) : e;
	}, r = /* @__PURE__ */ new Map();
	function i(e) {
		let t = r.get(e);
		if (t === ep) return new yr({
			type: "lazy",
			getter: () => r.get(e)
		});
		if (t !== void 0) return t;
		r.set(e, ep);
		let i = a(e), o = n(i, i !== e);
		return r.set(e, o), o;
	}
	function a(e) {
		let t = e._zod.def;
		switch (t.type) {
			case "object": {
				let n = t.shape, r = Object.keys(n), a = !1, o = {};
				for (let e of r) {
					let t = i(n[e]);
					t !== n[e] && (a = !0), o[e] = t;
				}
				let s = t.catchall;
				return t.catchall && (s = i(t.catchall), s !== t.catchall && (a = !0)), a ? T(e, {
					...t,
					shape: o,
					catchall: s
				}) : e;
			}
			case "array": {
				let n = i(t.element);
				return n === t.element ? e : T(e, {
					...t,
					element: n
				});
			}
			case "tuple": {
				let n = t.items, r = !1, a = [];
				for (let e of n) {
					let t = i(e);
					t !== e && (r = !0), a.push(t);
				}
				let o = t.rest;
				return t.rest && (o = i(t.rest), o !== t.rest && (r = !0)), r ? T(e, {
					...t,
					items: a,
					rest: o
				}) : e;
			}
			case "record":
			case "map": {
				let n = i(t.keyType), r = i(t.valueType);
				return n === t.keyType && r === t.valueType ? e : T(e, {
					...t,
					keyType: n,
					valueType: r
				});
			}
			case "set": {
				let n = i(t.valueType);
				return n === t.valueType ? e : T(e, {
					...t,
					valueType: n
				});
			}
			case "union": {
				let n = t.options, r = !1, a = [];
				for (let e of n) {
					let t = i(e);
					t !== e && (r = !0), a.push(t);
				}
				return r ? T(e, {
					...t,
					options: a
				}) : e;
			}
			case "intersection": {
				let n = i(t.left), r = i(t.right);
				return n === t.left && r === t.right ? e : T(e, {
					...t,
					left: n,
					right: r
				});
			}
			case "optional":
			case "nullable":
			case "default":
			case "prefault":
			case "catch":
			case "readonly":
			case "nonoptional":
			case "promise":
			case "success": {
				let n = i(t.innerType);
				return n === t.innerType ? e : T(e, {
					...t,
					innerType: n
				});
			}
			case "pipe": {
				let n = i(t.in), r = i(t.out);
				return n === t.in && r === t.out ? e : T(e, {
					...t,
					in: n,
					out: r
				});
			}
			case "function": {
				let n = i(t.input), r = i(t.output);
				return n === t.input && r === t.output ? e : T(e, {
					...t,
					input: n,
					output: r
				});
			}
			case "lazy": {
				let n = t.getter, { _cachedInner: r, ...a } = t;
				return T(e, {
					...a,
					getter: () => i(n())
				});
			}
			case "template_literal":
			case "string":
			case "number":
			case "int":
			case "boolean":
			case "bigint":
			case "symbol":
			case "undefined":
			case "null":
			case "void":
			case "never":
			case "any":
			case "unknown":
			case "date":
			case "nan":
			case "enum":
			case "literal":
			case "file":
			case "transform":
			case "custom": return e;
			default: return e;
		}
	}
	return i(e);
}
//#endregion
//#region node_modules/zod/v4/classic/deep-partial.js
function np(e) {
	return tp(e, {
		object: (e) => e.partial(),
		union: (e) => {
			let t = e._zod.def;
			return t.discriminator === void 0 ? e : O(t.options);
		}
	});
}
//#endregion
//#region node_modules/zod/v4/classic/in-out.js
function rp(e, t) {
	if (!t?.length) return e;
	let n = e._zod.def;
	return T(e, Ki(n, { checks: [...n.checks ?? [], ...t] }), { parent: !0 });
}
function ip(e) {
	return rp(e.out, e.checks);
}
function ap(e) {
	return e.in._zod.traits.has("$ZodTransform") ? ip(e) : e.in;
}
function op(e) {
	return tp(e, {
		pipe: (e) => ap(e._zod.def),
		default: (e, t) => t ? v(e._zod.def.innerType) : e,
		catch: (e, t) => t ? e._zod.def.innerType : e
	});
}
function sp(e) {
	return tp(e, {
		pipe: (e) => ip(e._zod.def),
		prefault: (e, t) => t ? e._zod.def.innerType : e
	});
}
//#endregion
//#region node_modules/zod/v4/classic/coerce.js
var cp = /* @__PURE__ */ e({
	bigint: () => fp,
	boolean: () => dp,
	date: () => pp,
	number: () => up,
	string: () => lp
});
function lp(e) {
	return c(Jo, e);
}
function up(e) {
	return jn(Vr, e);
}
function dp(e) {
	return Rr(Cs, e);
}
function fp(e) {
	return qr(mi, e);
}
function pp(e) {
	return ni(ai, e);
}
//#endregion
//#region node_modules/zod/v4/classic/external.js
var mp = /* @__PURE__ */ e({
	$brand: () => Dn,
	$input: () => Hr,
	$output: () => Nr,
	INVALID: () => P,
	NEVER: () => ta,
	TimePrecision: () => Bn,
	ZodAny: () => js,
	ZodArray: () => Ao,
	ZodBase64: () => ms,
	ZodBase64URL: () => Ia,
	ZodBigInt: () => mi,
	ZodBigIntFormat: () => Bo,
	ZodBoolean: () => Cs,
	ZodCIDRv4: () => Mi,
	ZodCIDRv6: () => uo,
	ZodCUID: () => Vs,
	ZodCUID2: () => Hi,
	ZodCatch: () => sa,
	ZodCodec: () => Yo,
	ZodCompileAsyncError: () => F,
	ZodCompileUnsupportedError: () => I,
	ZodCreditCard: () => xo,
	ZodCustom: () => Ea,
	ZodCustomStringFormat: () => ga,
	ZodDate: () => ai,
	ZodDefault: () => Xs,
	ZodDiscriminatedUnion: () => Sc,
	ZodE164: () => Ci,
	ZodEmail: () => mc,
	ZodEmoji: () => Un,
	ZodEnum: () => pe,
	ZodError: () => Oe,
	ZodExactOptional: () => ac,
	ZodFile: () => Zn,
	ZodFirstPartyTypeKind: () => Mf,
	ZodFunction: () => je,
	ZodGUID: () => Se,
	ZodIBAN: () => cn,
	ZodIPv4: () => to,
	ZodIPv6: () => d,
	ZodISODate: () => Ka,
	ZodISODateTime: () => Wt,
	ZodISODuration: () => $t,
	ZodISOTime: () => _n,
	ZodInstanceOf: () => ze,
	ZodIntersection: () => dt,
	ZodIssueCode: () => kf,
	ZodJWT: () => It,
	ZodKSUID: () => Pn,
	ZodLazy: () => jc,
	ZodLiteral: () => ie,
	ZodMAC: () => mr,
	ZodMap: () => nt,
	ZodNaN: () => or,
	ZodNanoID: () => Cr,
	ZodNever: () => qe,
	ZodNonOptional: () => Dt,
	ZodNull: () => as,
	ZodNullable: () => yt,
	ZodNumber: () => Vr,
	ZodNumberFormat: () => Mr,
	ZodObject: () => Xr,
	ZodOptional: () => Tn,
	ZodPipe: () => n,
	ZodPrefault: () => oa,
	ZodPreprocess: () => Bs,
	ZodPromise: () => zo,
	ZodReadonly: () => Ss,
	ZodRealError: () => mn,
	ZodRecord: () => Ga,
	ZodSet: () => Si,
	ZodString: () => Jo,
	ZodStringFormat: () => As,
	ZodSuccess: () => Vi,
	ZodSymbol: () => bo,
	ZodTemplateLiteral: () => Ys,
	ZodTransform: () => Zi,
	ZodTuple: () => ha,
	ZodType: () => is,
	ZodULID: () => ko,
	ZodURL: () => Fa,
	ZodUUID: () => Ta,
	ZodUndefined: () => pi,
	ZodUnion: () => ic,
	ZodUnknown: () => Ac,
	ZodVoid: () => ji,
	ZodXID: () => xc,
	ZodXor: () => Xn,
	_ZodString: () => xe,
	_default: () => ar,
	_function: () => Ae,
	any: () => Qt,
	array: () => x,
	base64: () => wn,
	base64url: () => Ke,
	bigint: () => vt,
	boolean: () => b,
	catch: () => pc,
	check: () => Hn,
	cidrv4: () => zc,
	cidrv6: () => fe,
	clone: () => T,
	codec: () => Sr,
	coerce: () => cp,
	compile: () => ld,
	config: () => Fs,
	core: () => Df,
	creditCard: () => ut,
	cuid: () => pr,
	cuid2: () => jr,
	currencyCode: () => tt,
	custom: () => Ft,
	date: () => ps,
	decode: () => Na,
	decodeAsync: () => Ca,
	deepPartial: () => np,
	describe: () => Et,
	discriminatedUnion: () => Yr,
	e164: () => Br,
	email: () => ii,
	emoji: () => Nn,
	encode: () => di,
	encodeAsync: () => nc,
	endsWith: () => Ws,
	enum: () => g,
	exactOptional: () => u,
	file: () => na,
	flattenError: () => Ye,
	float32: () => Is,
	float64: () => Fo,
	formatError: () => kt,
	fromJSONSchema: () => $f,
	function: () => Ae,
	getDiscriminatedOption: () => co,
	getErrorMap: () => jf,
	globalRegistry: () => Zr,
	gt: () => ka,
	gte: () => ya,
	guid: () => vs,
	hash: () => Va,
	hex: () => vi,
	hostname: () => Go,
	httpUrl: () => Ds,
	iban: () => Li,
	includes: () => Tc,
	input: () => op,
	instanceof: () => gn,
	int: () => go,
	int32: () => Gs,
	int64: () => qi,
	intersection: () => da,
	invertCodec: () => es,
	ipv4: () => To,
	ipv6: () => ja,
	iso: () => Nf,
	json: () => xa,
	jwt: () => li,
	keyof: () => ec,
	ksuid: () => Ec,
	lazy: () => Di,
	length: () => nn,
	literal: () => M,
	locales: () => od,
	looseObject: () => C,
	looseRecord: () => _e,
	lowercase: () => He,
	lt: () => mt,
	lte: () => zt,
	mac: () => lc,
	map: () => tr,
	maxLength: () => se,
	maxSize: () => _r,
	memoizer: () => ys,
	meta: () => Fe,
	mime: () => at,
	minLength: () => lr,
	minSize: () => Er,
	multipleOf: () => Xe,
	nan: () => Ee,
	nanoid: () => fn,
	nativeEnum: () => ao,
	negative: () => Wr,
	never: () => m,
	nonnegative: () => $r,
	nonoptional: () => Xa,
	nonpositive: () => a,
	normalize: () => Qi,
	null: () => lo,
	nullable: () => Jt,
	nullish: () => rn,
	number: () => S,
	object: () => _,
	optional: () => v,
	output: () => sp,
	overwrite: () => hi,
	parse: () => Oc,
	parseAsync: () => ki,
	partialRecord: () => Bt,
	pipe: () => Rn,
	positive: () => ws,
	prefault: () => Fc,
	preprocess: () => ce,
	prettifyError: () => ss,
	promise: () => vr,
	properties: () => fo,
	property: () => Hs,
	readonly: () => ot,
	record: () => w,
	refine: () => Dr,
	regex: () => So,
	regexes: () => va,
	registry: () => En,
	safeDecode: () => yc,
	safeDecodeAsync: () => Jn,
	safeEncode: () => ye,
	safeEncodeAsync: () => dc,
	safeParse: () => rr,
	safeParseAsync: () => Le,
	set: () => jt,
	setErrorMap: () => Af,
	size: () => _a,
	slugify: () => oi,
	startsWith: () => Zs,
	strictObject: () => ls,
	string: () => y,
	stringFormat: () => Gr,
	stringbool: () => Ir,
	success: () => ei,
	superRefine: () => kn,
	symbol: () => o,
	templateLiteral: () => ia,
	toJSONSchema: () => te,
	toLowerCase: () => Me,
	toUpperCase: () => Ce,
	toZod: () => Aa,
	transform: () => Rs,
	treeifyError: () => Ur,
	trim: () => no,
	tuple: () => Lo,
	uint32: () => bs,
	uint64: () => Ua,
	ulid: () => bi,
	undefined: () => re,
	union: () => O,
	unknown: () => A,
	uppercase: () => Lt,
	url: () => zi,
	util: () => ba,
	uuid: () => vo,
	uuidv4: () => qs,
	uuidv6: () => Yi,
	uuidv7: () => pa,
	validate: () => gr,
	validateAsync: () => it,
	void: () => eo,
	withParser: () => ud,
	xid: () => ns,
	xor: () => Do
}), G = "io.modelcontextprotocol/related-task", K = Ft((e) => e !== null && (typeof e == "object" || typeof e == "function")), hp = O([y(), S().int()]), gp = y();
C({
	ttl: S().optional(),
	pollInterval: S().optional()
});
var _p = _({ ttl: S().optional() }), vp = _({ taskId: y() }), yp = C({
	progressToken: hp.optional(),
	[G]: vp.optional()
}), q = _({ _meta: yp.optional() }), bp = q.extend({ task: _p.optional() }), xp = (e) => bp.safeParse(e).success, J = _({
	method: y(),
	params: q.loose().optional()
}), Y = _({ _meta: yp.optional() }), X = _({
	method: y(),
	params: Y.loose().optional()
}), Z = C({ _meta: yp.optional() }), Sp = O([y(), S().int()]), Cp = _({
	jsonrpc: M("2.0"),
	id: Sp,
	...J.shape
}).strict(), wp = (e) => Cp.safeParse(e).success, Tp = _({
	jsonrpc: M("2.0"),
	...X.shape
}).strict(), Ep = (e) => Tp.safeParse(e).success, Dp = _({
	jsonrpc: M("2.0"),
	id: Sp,
	result: Z
}).strict(), Op = (e) => Dp.safeParse(e).success, Q;
(function(e) {
	e[e.ConnectionClosed = -32e3] = "ConnectionClosed", e[e.RequestTimeout = -32001] = "RequestTimeout", e[e.ParseError = -32700] = "ParseError", e[e.InvalidRequest = -32600] = "InvalidRequest", e[e.MethodNotFound = -32601] = "MethodNotFound", e[e.InvalidParams = -32602] = "InvalidParams", e[e.InternalError = -32603] = "InternalError", e[e.UrlElicitationRequired = -32042] = "UrlElicitationRequired";
})(Q || (Q = {}));
var kp = _({
	jsonrpc: M("2.0"),
	id: Sp.optional(),
	error: _({
		code: S().int(),
		message: y(),
		data: A().optional()
	})
}).strict(), Ap = (e) => kp.safeParse(e).success, jp = O([
	Cp,
	Tp,
	Dp,
	kp
]);
O([Dp, kp]);
var Mp = Z.strict(), Np = Y.extend({
	requestId: Sp.optional(),
	reason: y().optional()
}), Pp = X.extend({
	method: M("notifications/cancelled"),
	params: Np
}), Fp = _({
	src: y(),
	mimeType: y().optional(),
	sizes: x(y()).optional(),
	theme: g(["light", "dark"]).optional()
}), Ip = _({ icons: x(Fp).optional() }), Lp = _({
	name: y(),
	title: y().optional()
}), Rp = Lp.extend({
	...Lp.shape,
	...Ip.shape,
	version: y(),
	websiteUrl: y().optional(),
	description: y().optional()
}), zp = da(_({ applyDefaults: b().optional() }), w(y(), A())), Bp = ce((e) => e && typeof e == "object" && !Array.isArray(e) && Object.keys(e).length === 0 ? { form: {} } : e, da(_({
	form: zp.optional(),
	url: K.optional()
}), w(y(), A()).optional())), Vp = C({
	list: K.optional(),
	cancel: K.optional(),
	requests: C({
		sampling: C({ createMessage: K.optional() }).optional(),
		elicitation: C({ create: K.optional() }).optional()
	}).optional()
}), Hp = C({
	list: K.optional(),
	cancel: K.optional(),
	requests: C({ tools: C({ call: K.optional() }).optional() }).optional()
}), Up = _({
	experimental: w(y(), K).optional(),
	sampling: _({
		context: K.optional(),
		tools: K.optional()
	}).optional(),
	elicitation: Bp.optional(),
	roots: _({ listChanged: b().optional() }).optional(),
	tasks: Vp.optional(),
	extensions: w(y(), K).optional()
}), Wp = q.extend({
	protocolVersion: y(),
	capabilities: Up,
	clientInfo: Rp
}), Gp = J.extend({
	method: M("initialize"),
	params: Wp
}), Kp = _({
	experimental: w(y(), K).optional(),
	logging: K.optional(),
	completions: K.optional(),
	prompts: _({ listChanged: b().optional() }).optional(),
	resources: _({
		subscribe: b().optional(),
		listChanged: b().optional()
	}).optional(),
	tools: _({ listChanged: b().optional() }).optional(),
	tasks: Hp.optional(),
	extensions: w(y(), K).optional()
}), qp = Z.extend({
	protocolVersion: y(),
	capabilities: Kp,
	serverInfo: Rp,
	instructions: y().optional()
}), Jp = X.extend({
	method: M("notifications/initialized"),
	params: Y.optional()
}), Yp = J.extend({
	method: M("ping"),
	params: q.optional()
}), Xp = _({
	progress: S(),
	total: v(S()),
	message: v(y())
}), Zp = _({
	...Y.shape,
	...Xp.shape,
	progressToken: hp
}), Qp = X.extend({
	method: M("notifications/progress"),
	params: Zp
}), $p = q.extend({ cursor: gp.optional() }), em = J.extend({ params: $p.optional() }), tm = Z.extend({ nextCursor: gp.optional() }), nm = g([
	"working",
	"input_required",
	"completed",
	"failed",
	"cancelled"
]), rm = _({
	taskId: y(),
	status: nm,
	ttl: O([S(), lo()]),
	createdAt: y(),
	lastUpdatedAt: y(),
	pollInterval: v(S()),
	statusMessage: v(y())
}), im = Z.extend({ task: rm }), am = Y.merge(rm), om = X.extend({
	method: M("notifications/tasks/status"),
	params: am
}), sm = J.extend({
	method: M("tasks/get"),
	params: q.extend({ taskId: y() })
}), cm = Z.merge(rm), lm = J.extend({
	method: M("tasks/result"),
	params: q.extend({ taskId: y() })
});
Z.loose();
var um = em.extend({ method: M("tasks/list") }), dm = tm.extend({ tasks: x(rm) }), fm = J.extend({
	method: M("tasks/cancel"),
	params: q.extend({ taskId: y() })
}), pm = Z.merge(rm), mm = _({
	uri: y(),
	mimeType: v(y()),
	_meta: w(y(), A()).optional()
}), hm = mm.extend({ text: y() }), gm = y().refine((e) => {
	try {
		return atob(e), !0;
	} catch {
		return !1;
	}
}, { message: "Invalid Base64 string" }), _m = mm.extend({ blob: gm }), vm = g(["user", "assistant"]), ym = _({
	audience: x(vm).optional(),
	priority: S().min(0).max(1).optional(),
	lastModified: Pf({ offset: !0 }).optional()
}), bm = _({
	...Lp.shape,
	...Ip.shape,
	uri: y(),
	description: v(y()),
	mimeType: v(y()),
	size: v(S()),
	annotations: ym.optional(),
	_meta: v(C({}))
}), xm = _({
	...Lp.shape,
	...Ip.shape,
	uriTemplate: y(),
	description: v(y()),
	mimeType: v(y()),
	annotations: ym.optional(),
	_meta: v(C({}))
}), Sm = em.extend({ method: M("resources/list") }), Cm = tm.extend({ resources: x(bm) }), wm = em.extend({ method: M("resources/templates/list") }), Tm = tm.extend({ resourceTemplates: x(xm) }), Em = q.extend({ uri: y() }), Dm = Em, Om = J.extend({
	method: M("resources/read"),
	params: Dm
}), km = Z.extend({ contents: x(O([hm, _m])) }), Am = X.extend({
	method: M("notifications/resources/list_changed"),
	params: Y.optional()
}), jm = Em, Mm = J.extend({
	method: M("resources/subscribe"),
	params: jm
}), Nm = Em, Pm = J.extend({
	method: M("resources/unsubscribe"),
	params: Nm
}), Fm = Y.extend({ uri: y() }), Im = X.extend({
	method: M("notifications/resources/updated"),
	params: Fm
}), Lm = _({
	name: y(),
	description: v(y()),
	required: v(b())
}), Rm = _({
	...Lp.shape,
	...Ip.shape,
	description: v(y()),
	arguments: v(x(Lm)),
	_meta: v(C({}))
}), zm = em.extend({ method: M("prompts/list") }), Bm = tm.extend({ prompts: x(Rm) }), Vm = q.extend({
	name: y(),
	arguments: w(y(), y()).optional()
}), Hm = J.extend({
	method: M("prompts/get"),
	params: Vm
}), Um = _({
	type: M("text"),
	text: y(),
	annotations: ym.optional(),
	_meta: w(y(), A()).optional()
}), Wm = _({
	type: M("image"),
	data: gm,
	mimeType: y(),
	annotations: ym.optional(),
	_meta: w(y(), A()).optional()
}), Gm = _({
	type: M("audio"),
	data: gm,
	mimeType: y(),
	annotations: ym.optional(),
	_meta: w(y(), A()).optional()
}), Km = _({
	type: M("tool_use"),
	name: y(),
	id: y(),
	input: w(y(), A()),
	_meta: w(y(), A()).optional()
}), qm = _({
	type: M("resource"),
	resource: O([hm, _m]),
	annotations: ym.optional(),
	_meta: w(y(), A()).optional()
}), Jm = bm.extend({ type: M("resource_link") }), Ym = O([
	Um,
	Wm,
	Gm,
	Jm,
	qm
]), Xm = _({
	role: vm,
	content: Ym
}), Zm = Z.extend({
	description: y().optional(),
	messages: x(Xm)
}), Qm = X.extend({
	method: M("notifications/prompts/list_changed"),
	params: Y.optional()
}), $m = _({
	title: y().optional(),
	readOnlyHint: b().optional(),
	destructiveHint: b().optional(),
	idempotentHint: b().optional(),
	openWorldHint: b().optional()
}), eh = _({ taskSupport: g([
	"required",
	"optional",
	"forbidden"
]).optional() }), th = _({
	...Lp.shape,
	...Ip.shape,
	description: y().optional(),
	inputSchema: _({
		type: M("object"),
		properties: w(y(), K).optional(),
		required: x(y()).optional()
	}).catchall(A()),
	outputSchema: _({
		type: M("object"),
		properties: w(y(), K).optional(),
		required: x(y()).optional()
	}).catchall(A()).optional(),
	annotations: $m.optional(),
	execution: eh.optional(),
	_meta: w(y(), A()).optional()
}), nh = em.extend({ method: M("tools/list") }), rh = tm.extend({ tools: x(th) }), ih = Z.extend({
	content: x(Ym).default([]),
	structuredContent: w(y(), A()).optional(),
	isError: b().optional()
});
ih.or(Z.extend({ toolResult: A() }));
var ah = bp.extend({
	name: y(),
	arguments: w(y(), A()).optional()
}), oh = J.extend({
	method: M("tools/call"),
	params: ah
}), sh = X.extend({
	method: M("notifications/tools/list_changed"),
	params: Y.optional()
});
_({
	autoRefresh: b().default(!0),
	debounceMs: S().int().nonnegative().default(300)
});
var ch = g([
	"debug",
	"info",
	"notice",
	"warning",
	"error",
	"critical",
	"alert",
	"emergency"
]), lh = q.extend({ level: ch }), uh = J.extend({
	method: M("logging/setLevel"),
	params: lh
}), dh = Y.extend({
	level: ch,
	logger: y().optional(),
	data: A()
}), fh = X.extend({
	method: M("notifications/message"),
	params: dh
}), ph = _({ name: y().optional() }), mh = _({
	hints: x(ph).optional(),
	costPriority: S().min(0).max(1).optional(),
	speedPriority: S().min(0).max(1).optional(),
	intelligencePriority: S().min(0).max(1).optional()
}), hh = _({ mode: g([
	"auto",
	"required",
	"none"
]).optional() }), gh = _({
	type: M("tool_result"),
	toolUseId: y().describe("The unique identifier for the corresponding tool call."),
	content: x(Ym).default([]),
	structuredContent: _({}).loose().optional(),
	isError: b().optional(),
	_meta: w(y(), A()).optional()
}), _h = Yr("type", [
	Um,
	Wm,
	Gm
]), vh = Yr("type", [
	Um,
	Wm,
	Gm,
	Km,
	gh
]), yh = _({
	role: vm,
	content: O([vh, x(vh)]),
	_meta: w(y(), A()).optional()
}), bh = bp.extend({
	messages: x(yh),
	modelPreferences: mh.optional(),
	systemPrompt: y().optional(),
	includeContext: g([
		"none",
		"thisServer",
		"allServers"
	]).optional(),
	temperature: S().optional(),
	maxTokens: S().int(),
	stopSequences: x(y()).optional(),
	metadata: K.optional(),
	tools: x(th).optional(),
	toolChoice: hh.optional()
}), xh = J.extend({
	method: M("sampling/createMessage"),
	params: bh
}), Sh = Z.extend({
	model: y(),
	stopReason: v(g([
		"endTurn",
		"stopSequence",
		"maxTokens"
	]).or(y())),
	role: vm,
	content: _h
}), Ch = Z.extend({
	model: y(),
	stopReason: v(g([
		"endTurn",
		"stopSequence",
		"maxTokens",
		"toolUse"
	]).or(y())),
	role: vm,
	content: O([vh, x(vh)])
}), wh = _({
	type: M("boolean"),
	title: y().optional(),
	description: y().optional(),
	default: b().optional()
}), Th = _({
	type: M("string"),
	title: y().optional(),
	description: y().optional(),
	minLength: S().optional(),
	maxLength: S().optional(),
	format: g([
		"email",
		"uri",
		"date",
		"date-time"
	]).optional(),
	default: y().optional()
}), Eh = _({
	type: g(["number", "integer"]),
	title: y().optional(),
	description: y().optional(),
	minimum: S().optional(),
	maximum: S().optional(),
	default: S().optional()
}), Dh = _({
	type: M("string"),
	title: y().optional(),
	description: y().optional(),
	enum: x(y()),
	default: y().optional()
}), Oh = _({
	type: M("string"),
	title: y().optional(),
	description: y().optional(),
	oneOf: x(_({
		const: y(),
		title: y()
	})),
	default: y().optional()
}), kh = _({
	type: M("string"),
	title: y().optional(),
	description: y().optional(),
	enum: x(y()),
	enumNames: x(y()).optional(),
	default: y().optional()
}), Ah = O([Dh, Oh]), jh = _({
	type: M("array"),
	title: y().optional(),
	description: y().optional(),
	minItems: S().optional(),
	maxItems: S().optional(),
	items: _({
		type: M("string"),
		enum: x(y())
	}),
	default: x(y()).optional()
}), Mh = _({
	type: M("array"),
	title: y().optional(),
	description: y().optional(),
	minItems: S().optional(),
	maxItems: S().optional(),
	items: _({ anyOf: x(_({
		const: y(),
		title: y()
	})) }),
	default: x(y()).optional()
}), Nh = O([jh, Mh]), Ph = O([
	kh,
	Ah,
	Nh
]), Fh = O([
	Ph,
	wh,
	Th,
	Eh
]), Ih = bp.extend({
	mode: M("form").optional(),
	message: y(),
	requestedSchema: _({
		type: M("object"),
		properties: w(y(), Fh),
		required: x(y()).optional()
	})
}), Lh = bp.extend({
	mode: M("url"),
	message: y(),
	elicitationId: y(),
	url: y().url()
}), Rh = O([Ih, Lh]), zh = J.extend({
	method: M("elicitation/create"),
	params: Rh
}), Bh = Y.extend({ elicitationId: y() }), Vh = X.extend({
	method: M("notifications/elicitation/complete"),
	params: Bh
}), Hh = Z.extend({
	action: g([
		"accept",
		"decline",
		"cancel"
	]),
	content: ce((e) => e === null ? void 0 : e, w(y(), O([
		y(),
		S(),
		b(),
		x(y())
	])).optional())
}), Uh = _({
	type: M("ref/resource"),
	uri: y()
}), Wh = _({
	type: M("ref/prompt"),
	name: y()
}), Gh = q.extend({
	ref: O([Wh, Uh]),
	argument: _({
		name: y(),
		value: y()
	}),
	context: _({ arguments: w(y(), y()).optional() }).optional()
}), Kh = J.extend({
	method: M("completion/complete"),
	params: Gh
}), qh = Z.extend({ completion: C({
	values: x(y()).max(100),
	total: v(S().int()),
	hasMore: v(b())
}) }), Jh = _({
	uri: y().startsWith("file://"),
	name: y().optional(),
	_meta: w(y(), A()).optional()
}), Yh = J.extend({
	method: M("roots/list"),
	params: q.optional()
}), Xh = Z.extend({ roots: x(Jh) }), Zh = X.extend({
	method: M("notifications/roots/list_changed"),
	params: Y.optional()
});
O([
	Yp,
	Gp,
	Kh,
	uh,
	Hm,
	zm,
	Sm,
	wm,
	Om,
	Mm,
	Pm,
	oh,
	nh,
	sm,
	lm,
	um,
	fm
]), O([
	Pp,
	Qp,
	Jp,
	Zh,
	om
]), O([
	Mp,
	Sh,
	Ch,
	Hh,
	Xh,
	cm,
	dm,
	im
]), O([
	Yp,
	xh,
	zh,
	Yh,
	sm,
	lm,
	um,
	fm
]), O([
	Pp,
	Qp,
	fh,
	Im,
	Am,
	sh,
	Qm,
	om,
	Vh
]), O([
	Mp,
	qp,
	qh,
	Zm,
	Bm,
	Cm,
	Tm,
	km,
	ih,
	rh,
	cm,
	dm,
	im
]);
var $ = class e extends Error {
	constructor(e, t, n) {
		super(`MCP error ${e}: ${t}`), this.code = e, this.data = n, this.name = "McpError";
	}
	static fromError(t, n, r) {
		if (t === Q.UrlElicitationRequired && r) {
			let e = r;
			if (e.elicitations) return new Qh(e.elicitations, n);
		}
		return new e(t, n, r);
	}
}, Qh = class extends $ {
	constructor(e, t = `URL elicitation${e.length > 1 ? "s" : ""} required`) {
		super(Q.UrlElicitationRequired, t, { elicitations: e });
	}
	get elicitations() {
		return this.data?.elicitations ?? [];
	}
};
//#endregion
//#region node_modules/@modelcontextprotocol/sdk/dist/esm/server/zod-compat.js
function $h(e) {
	return !!e._zod;
}
function eg(e, t) {
	return $h(e) ? Nc(e, t) : e.safeParse(t);
}
function tg(e) {
	if (!e) return;
	let t;
	if (t = $h(e) ? e._zod?.def?.shape : e.shape, t) {
		if (typeof t == "function") try {
			return t();
		} catch {
			return;
		}
		return t;
	}
}
function ng(e) {
	if ($h(e)) {
		let t = e._zod?.def;
		if (t) {
			if (t.value !== void 0) return t.value;
			if (Array.isArray(t.values) && t.values.length > 0) return t.values[0];
		}
	}
	let t = e._def;
	if (t) {
		if (t.value !== void 0) return t.value;
		if (Array.isArray(t.values) && t.values.length > 0) return t.values[0];
	}
	let n = e.value;
	if (n !== void 0) return n;
}
//#endregion
//#region node_modules/@modelcontextprotocol/sdk/dist/esm/experimental/tasks/interfaces.js
function rg(e) {
	return e === "completed" || e === "failed" || e === "cancelled";
}
//#endregion
//#region node_modules/@modelcontextprotocol/sdk/dist/esm/server/zod-json-schema-compat.js
function ig(e) {
	let t = tg(e)?.method;
	if (!t) throw Error("Schema is missing a method literal");
	let n = ng(t);
	if (typeof n != "string") throw Error("Schema method literal must be a string");
	return n;
}
function ag(e, t) {
	let n = eg(e, t);
	if (!n.success) throw n.error;
	return n.data;
}
var og = class {
	constructor(e) {
		this._options = e, this._requestMessageId = 0, this._requestHandlers = /* @__PURE__ */ new Map(), this._requestHandlerAbortControllers = /* @__PURE__ */ new Map(), this._notificationHandlers = /* @__PURE__ */ new Map(), this._responseHandlers = /* @__PURE__ */ new Map(), this._progressHandlers = /* @__PURE__ */ new Map(), this._timeoutInfo = /* @__PURE__ */ new Map(), this._pendingDebouncedNotifications = /* @__PURE__ */ new Set(), this._taskProgressTokens = /* @__PURE__ */ new Map(), this._requestResolvers = /* @__PURE__ */ new Map(), this.setNotificationHandler(Pp, (e) => {
			this._oncancel(e);
		}), this.setNotificationHandler(Qp, (e) => {
			this._onprogress(e);
		}), this.setRequestHandler(Yp, (e) => ({})), this._taskStore = e?.taskStore, this._taskMessageQueue = e?.taskMessageQueue, this._taskStore && (this.setRequestHandler(sm, async (e, t) => {
			let n = await this._taskStore.getTask(e.params.taskId, t.sessionId);
			if (!n) throw new $(Q.InvalidParams, "Failed to retrieve task: Task not found");
			return { ...n };
		}), this.setRequestHandler(lm, async (e, t) => {
			let n = async () => {
				let r = e.params.taskId;
				if (this._taskMessageQueue) {
					let e;
					for (; e = await this._taskMessageQueue.dequeue(r, t.sessionId);) {
						if (e.type === "response" || e.type === "error") {
							let t = e.message, n = t.id, r = this._requestResolvers.get(n);
							if (r) {
								if (this._requestResolvers.delete(n), e.type === "response") r(t);
								else {
									let e = t;
									r(new $(e.error.code, e.error.message, e.error.data));
								}
							} else {
								let t = e.type === "response" ? "Response" : "Error";
								this._onerror(/* @__PURE__ */ Error(`${t} handler missing for request ${n}`));
							}
							continue;
						}
						await this._transport?.send(e.message, { relatedRequestId: t.requestId });
					}
				}
				let i = await this._taskStore.getTask(r, t.sessionId);
				if (!i) throw new $(Q.InvalidParams, `Task not found: ${r}`);
				if (!rg(i.status)) return await this._waitForTaskUpdate(r, t.signal), await n();
				if (rg(i.status)) {
					let e = await this._taskStore.getTaskResult(r, t.sessionId);
					return this._clearTaskQueue(r), {
						...e,
						_meta: {
							...e._meta,
							[G]: { taskId: r }
						}
					};
				}
				return await n();
			};
			return await n();
		}), this.setRequestHandler(um, async (e, t) => {
			try {
				let { tasks: n, nextCursor: r } = await this._taskStore.listTasks(e.params?.cursor, t.sessionId);
				return {
					tasks: n,
					nextCursor: r,
					_meta: {}
				};
			} catch (e) {
				throw new $(Q.InvalidParams, `Failed to list tasks: ${e instanceof Error ? e.message : String(e)}`);
			}
		}), this.setRequestHandler(fm, async (e, t) => {
			try {
				let n = await this._taskStore.getTask(e.params.taskId, t.sessionId);
				if (!n) throw new $(Q.InvalidParams, `Task not found: ${e.params.taskId}`);
				if (rg(n.status)) throw new $(Q.InvalidParams, `Cannot cancel task in terminal status: ${n.status}`);
				await this._taskStore.updateTaskStatus(e.params.taskId, "cancelled", "Client cancelled task execution.", t.sessionId), this._clearTaskQueue(e.params.taskId);
				let r = await this._taskStore.getTask(e.params.taskId, t.sessionId);
				if (!r) throw new $(Q.InvalidParams, `Task not found after cancellation: ${e.params.taskId}`);
				return {
					_meta: {},
					...r
				};
			} catch (e) {
				throw e instanceof $ ? e : new $(Q.InvalidRequest, `Failed to cancel task: ${e instanceof Error ? e.message : String(e)}`);
			}
		}));
	}
	async _oncancel(e) {
		e.params.requestId && this._requestHandlerAbortControllers.get(e.params.requestId)?.abort(e.params.reason);
	}
	_setupTimeout(e, t, n, r, i = !1) {
		this._timeoutInfo.set(e, {
			timeoutId: setTimeout(r, t),
			startTime: Date.now(),
			timeout: t,
			maxTotalTimeout: n,
			resetTimeoutOnProgress: i,
			onTimeout: r
		});
	}
	_resetTimeout(e) {
		let t = this._timeoutInfo.get(e);
		if (!t) return !1;
		let n = Date.now() - t.startTime;
		if (t.maxTotalTimeout && n >= t.maxTotalTimeout) throw this._timeoutInfo.delete(e), $.fromError(Q.RequestTimeout, "Maximum total timeout exceeded", {
			maxTotalTimeout: t.maxTotalTimeout,
			totalElapsed: n
		});
		return clearTimeout(t.timeoutId), t.timeoutId = setTimeout(t.onTimeout, t.timeout), !0;
	}
	_cleanupTimeout(e) {
		let t = this._timeoutInfo.get(e);
		t && (clearTimeout(t.timeoutId), this._timeoutInfo.delete(e));
	}
	async connect(e) {
		if (this._transport) throw Error("Already connected to a transport. Call close() before connecting to a new transport, or use a separate Protocol instance per connection.");
		this._transport = e;
		let t = this.transport?.onclose;
		this._transport.onclose = () => {
			t?.(), this._onclose();
		};
		let n = this.transport?.onerror;
		this._transport.onerror = (e) => {
			n?.(e), this._onerror(e);
		};
		let r = this._transport?.onmessage;
		this._transport.onmessage = (e, t) => {
			r?.(e, t), Op(e) || Ap(e) ? this._onresponse(e) : wp(e) ? this._onrequest(e, t) : Ep(e) ? this._onnotification(e) : this._onerror(/* @__PURE__ */ Error(`Unknown message type: ${JSON.stringify(e)}`));
		}, await this._transport.start();
	}
	_onclose() {
		let e = this._responseHandlers;
		this._responseHandlers = /* @__PURE__ */ new Map(), this._progressHandlers.clear(), this._taskProgressTokens.clear(), this._pendingDebouncedNotifications.clear();
		for (let e of this._timeoutInfo.values()) clearTimeout(e.timeoutId);
		this._timeoutInfo.clear();
		for (let e of this._requestHandlerAbortControllers.values()) e.abort();
		this._requestHandlerAbortControllers.clear();
		let t = $.fromError(Q.ConnectionClosed, "Connection closed");
		this._transport = void 0, this.onclose?.();
		for (let n of e.values()) n(t);
	}
	_onerror(e) {
		this.onerror?.(e);
	}
	_onnotification(e) {
		let t = this._notificationHandlers.get(e.method) ?? this.fallbackNotificationHandler;
		t !== void 0 && Promise.resolve().then(() => t(e)).catch((e) => this._onerror(/* @__PURE__ */ Error(`Uncaught error in notification handler: ${e}`)));
	}
	_onrequest(e, t) {
		let n = this._requestHandlers.get(e.method) ?? this.fallbackRequestHandler, r = this._transport, i = e.params?._meta?.[G]?.taskId;
		if (n === void 0) {
			let t = {
				jsonrpc: "2.0",
				id: e.id,
				error: {
					code: Q.MethodNotFound,
					message: "Method not found"
				}
			};
			i && this._taskMessageQueue ? this._enqueueTaskMessage(i, {
				type: "error",
				message: t,
				timestamp: Date.now()
			}, r?.sessionId).catch((e) => this._onerror(/* @__PURE__ */ Error(`Failed to enqueue error response: ${e}`))) : r?.send(t).catch((e) => this._onerror(/* @__PURE__ */ Error(`Failed to send an error response: ${e}`)));
			return;
		}
		let a = new AbortController();
		this._requestHandlerAbortControllers.set(e.id, a);
		let o = xp(e.params) ? e.params.task : void 0, s = this._taskStore ? this.requestTaskStore(e, r?.sessionId) : void 0, c = {
			signal: a.signal,
			sessionId: r?.sessionId,
			_meta: e.params?._meta,
			sendNotification: async (t) => {
				if (a.signal.aborted) return;
				let n = { relatedRequestId: e.id };
				i && (n.relatedTask = { taskId: i }), await this.notification(t, n);
			},
			sendRequest: async (t, n, r) => {
				if (a.signal.aborted) throw new $(Q.ConnectionClosed, "Request was cancelled");
				let o = {
					...r,
					relatedRequestId: e.id
				};
				i && !o.relatedTask && (o.relatedTask = { taskId: i });
				let c = o.relatedTask?.taskId ?? i;
				return c && s && await s.updateTaskStatus(c, "input_required"), await this.request(t, n, o);
			},
			authInfo: t?.authInfo,
			requestId: e.id,
			requestInfo: t?.requestInfo,
			taskId: i,
			taskStore: s,
			taskRequestedTtl: o?.ttl,
			closeSSEStream: t?.closeSSEStream,
			closeStandaloneSSEStream: t?.closeStandaloneSSEStream
		};
		Promise.resolve().then(() => {
			o && this.assertTaskHandlerCapability(e.method);
		}).then(() => n(e, c)).then(async (t) => {
			if (a.signal.aborted) return;
			let n = {
				result: t,
				jsonrpc: "2.0",
				id: e.id
			};
			i && this._taskMessageQueue ? await this._enqueueTaskMessage(i, {
				type: "response",
				message: n,
				timestamp: Date.now()
			}, r?.sessionId) : await r?.send(n);
		}, async (t) => {
			if (a.signal.aborted) return;
			let n = {
				jsonrpc: "2.0",
				id: e.id,
				error: {
					code: Number.isSafeInteger(t.code) ? t.code : Q.InternalError,
					message: t.message ?? "Internal error",
					...t.data !== void 0 && { data: t.data }
				}
			};
			i && this._taskMessageQueue ? await this._enqueueTaskMessage(i, {
				type: "error",
				message: n,
				timestamp: Date.now()
			}, r?.sessionId) : await r?.send(n);
		}).catch((e) => this._onerror(/* @__PURE__ */ Error(`Failed to send response: ${e}`))).finally(() => {
			this._requestHandlerAbortControllers.get(e.id) === a && this._requestHandlerAbortControllers.delete(e.id);
		});
	}
	_onprogress(e) {
		let { progressToken: t, ...n } = e.params, r = Number(t), i = this._progressHandlers.get(r);
		if (!i) {
			this._onerror(/* @__PURE__ */ Error(`Received a progress notification for an unknown token: ${JSON.stringify(e)}`));
			return;
		}
		let a = this._responseHandlers.get(r), o = this._timeoutInfo.get(r);
		if (o && a && o.resetTimeoutOnProgress) try {
			this._resetTimeout(r);
		} catch (e) {
			this._responseHandlers.delete(r), this._progressHandlers.delete(r), this._cleanupTimeout(r), a(e);
			return;
		}
		i(n);
	}
	_onresponse(e) {
		let t = Number(e.id), n = this._requestResolvers.get(t);
		if (n) {
			this._requestResolvers.delete(t), Op(e) ? n(e) : n(new $(e.error.code, e.error.message, e.error.data));
			return;
		}
		let r = this._responseHandlers.get(t);
		if (r === void 0) {
			this._onerror(/* @__PURE__ */ Error(`Received a response for an unknown message ID: ${JSON.stringify(e)}`));
			return;
		}
		this._responseHandlers.delete(t), this._cleanupTimeout(t);
		let i = !1;
		if (Op(e) && e.result && typeof e.result == "object") {
			let n = e.result;
			if (n.task && typeof n.task == "object") {
				let e = n.task;
				typeof e.taskId == "string" && (i = !0, this._taskProgressTokens.set(e.taskId, t));
			}
		}
		i || this._progressHandlers.delete(t), Op(e) ? r(e) : r($.fromError(e.error.code, e.error.message, e.error.data));
	}
	get transport() {
		return this._transport;
	}
	async close() {
		await this._transport?.close();
	}
	async *requestStream(e, t, n) {
		let { task: r } = n ?? {};
		if (!r) {
			try {
				yield {
					type: "result",
					result: await this.request(e, t, n)
				};
			} catch (e) {
				yield {
					type: "error",
					error: e instanceof $ ? e : new $(Q.InternalError, String(e))
				};
			}
			return;
		}
		let i;
		try {
			let r = await this.request(e, im, n);
			if (r.task) i = r.task.taskId, yield {
				type: "taskCreated",
				task: r.task
			};
			else throw new $(Q.InternalError, "Task creation did not return a task");
			for (;;) {
				let e = await this.getTask({ taskId: i }, n);
				if (yield {
					type: "taskStatus",
					task: e
				}, rg(e.status)) {
					e.status === "completed" ? yield {
						type: "result",
						result: await this.getTaskResult({ taskId: i }, t, n)
					} : e.status === "failed" ? yield {
						type: "error",
						error: new $(Q.InternalError, `Task ${i} failed`)
					} : e.status === "cancelled" && (yield {
						type: "error",
						error: new $(Q.InternalError, `Task ${i} was cancelled`)
					});
					return;
				}
				if (e.status === "input_required") {
					yield {
						type: "result",
						result: await this.getTaskResult({ taskId: i }, t, n)
					};
					return;
				}
				let r = e.pollInterval ?? this._options?.defaultTaskPollInterval ?? 1e3;
				await new Promise((e) => setTimeout(e, r)), n?.signal?.throwIfAborted();
			}
		} catch (e) {
			yield {
				type: "error",
				error: e instanceof $ ? e : new $(Q.InternalError, String(e))
			};
		}
	}
	request(e, t, n) {
		let { relatedRequestId: r, resumptionToken: i, onresumptiontoken: a, task: o, relatedTask: s } = n ?? {};
		return new Promise((c, l) => {
			let u = (e) => {
				l(e);
			};
			if (!this._transport) {
				u(/* @__PURE__ */ Error("Not connected"));
				return;
			}
			if (this._options?.enforceStrictCapabilities === !0) try {
				this.assertCapabilityForMethod(e.method), o && this.assertTaskCapability(e.method);
			} catch (e) {
				u(e);
				return;
			}
			n?.signal?.throwIfAborted();
			let d = this._requestMessageId++, f = {
				...e,
				jsonrpc: "2.0",
				id: d
			};
			n?.onprogress && (this._progressHandlers.set(d, n.onprogress), f.params = {
				...e.params,
				_meta: {
					...e.params?._meta || {},
					progressToken: d
				}
			}), o && (f.params = {
				...f.params,
				task: o
			}), s && (f.params = {
				...f.params,
				_meta: {
					...f.params?._meta || {},
					[G]: s
				}
			});
			let ee = (e) => {
				this._responseHandlers.delete(d), this._progressHandlers.delete(d), this._cleanupTimeout(d), this._transport?.send({
					jsonrpc: "2.0",
					method: "notifications/cancelled",
					params: {
						requestId: d,
						reason: String(e)
					}
				}, {
					relatedRequestId: r,
					resumptionToken: i,
					onresumptiontoken: a
				}).catch((e) => this._onerror(/* @__PURE__ */ Error(`Failed to send cancellation: ${e}`))), l(e instanceof $ ? e : new $(Q.RequestTimeout, String(e)));
			};
			this._responseHandlers.set(d, (e) => {
				if (!n?.signal?.aborted) {
					if (e instanceof Error) return l(e);
					try {
						let n = eg(t, e.result);
						n.success ? c(n.data) : l(n.error);
					} catch (e) {
						l(e);
					}
				}
			}), n?.signal?.addEventListener("abort", () => {
				ee(n?.signal?.reason);
			});
			let p = n?.timeout ?? 6e4;
			this._setupTimeout(d, p, n?.maxTotalTimeout, () => ee($.fromError(Q.RequestTimeout, "Request timed out", { timeout: p })), n?.resetTimeoutOnProgress ?? !1);
			let m = s?.taskId;
			m ? (this._requestResolvers.set(d, (e) => {
				let t = this._responseHandlers.get(d);
				t ? t(e) : this._onerror(/* @__PURE__ */ Error(`Response handler missing for side-channeled request ${d}`));
			}), this._enqueueTaskMessage(m, {
				type: "request",
				message: f,
				timestamp: Date.now()
			}).catch((e) => {
				this._cleanupTimeout(d), l(e);
			})) : this._transport.send(f, {
				relatedRequestId: r,
				resumptionToken: i,
				onresumptiontoken: a
			}).catch((e) => {
				this._cleanupTimeout(d), l(e);
			});
		});
	}
	async getTask(e, t) {
		return this.request({
			method: "tasks/get",
			params: e
		}, cm, t);
	}
	async getTaskResult(e, t, n) {
		return this.request({
			method: "tasks/result",
			params: e
		}, t, n);
	}
	async listTasks(e, t) {
		return this.request({
			method: "tasks/list",
			params: e
		}, dm, t);
	}
	async cancelTask(e, t) {
		return this.request({
			method: "tasks/cancel",
			params: e
		}, pm, t);
	}
	async notification(e, t) {
		if (!this._transport) throw Error("Not connected");
		this.assertNotificationCapability(e.method);
		let n = t?.relatedTask?.taskId;
		if (n) {
			let r = {
				...e,
				jsonrpc: "2.0",
				params: {
					...e.params,
					_meta: {
						...e.params?._meta || {},
						[G]: t.relatedTask
					}
				}
			};
			await this._enqueueTaskMessage(n, {
				type: "notification",
				message: r,
				timestamp: Date.now()
			});
			return;
		}
		if ((this._options?.debouncedNotificationMethods ?? []).includes(e.method) && !e.params && !t?.relatedRequestId && !t?.relatedTask) {
			if (this._pendingDebouncedNotifications.has(e.method)) return;
			this._pendingDebouncedNotifications.add(e.method), Promise.resolve().then(() => {
				if (this._pendingDebouncedNotifications.delete(e.method), !this._transport) return;
				let n = {
					...e,
					jsonrpc: "2.0"
				};
				t?.relatedTask && (n = {
					...n,
					params: {
						...n.params,
						_meta: {
							...n.params?._meta || {},
							[G]: t.relatedTask
						}
					}
				}), this._transport?.send(n, t).catch((e) => this._onerror(e));
			});
			return;
		}
		let r = {
			...e,
			jsonrpc: "2.0"
		};
		t?.relatedTask && (r = {
			...r,
			params: {
				...r.params,
				_meta: {
					...r.params?._meta || {},
					[G]: t.relatedTask
				}
			}
		}), await this._transport.send(r, t);
	}
	setRequestHandler(e, t) {
		let n = ig(e);
		this.assertRequestHandlerCapability(n), this._requestHandlers.set(n, (n, r) => {
			let i = ag(e, n);
			return Promise.resolve(t(i, r));
		});
	}
	removeRequestHandler(e) {
		this._requestHandlers.delete(e);
	}
	assertCanSetRequestHandler(e) {
		if (this._requestHandlers.has(e)) throw Error(`A request handler for ${e} already exists, which would be overridden`);
	}
	setNotificationHandler(e, t) {
		let n = ig(e);
		this._notificationHandlers.set(n, (n) => {
			let r = ag(e, n);
			return Promise.resolve(t(r));
		});
	}
	removeNotificationHandler(e) {
		this._notificationHandlers.delete(e);
	}
	_cleanupTaskProgressHandler(e) {
		let t = this._taskProgressTokens.get(e);
		t !== void 0 && (this._progressHandlers.delete(t), this._taskProgressTokens.delete(e));
	}
	async _enqueueTaskMessage(e, t, n) {
		if (!this._taskStore || !this._taskMessageQueue) throw Error("Cannot enqueue task message: taskStore and taskMessageQueue are not configured");
		let r = this._options?.maxTaskQueueSize;
		await this._taskMessageQueue.enqueue(e, t, n, r);
	}
	async _clearTaskQueue(e, t) {
		if (this._taskMessageQueue) {
			let n = await this._taskMessageQueue.dequeueAll(e, t);
			for (let t of n) if (t.type === "request" && wp(t.message)) {
				let n = t.message.id, r = this._requestResolvers.get(n);
				r ? (r(new $(Q.InternalError, "Task cancelled or completed")), this._requestResolvers.delete(n)) : this._onerror(/* @__PURE__ */ Error(`Resolver missing for request ${n} during task ${e} cleanup`));
			}
		}
	}
	async _waitForTaskUpdate(e, t) {
		let n = this._options?.defaultTaskPollInterval ?? 1e3;
		try {
			let t = await this._taskStore?.getTask(e);
			t?.pollInterval && (n = t.pollInterval);
		} catch {}
		return new Promise((e, r) => {
			if (t.aborted) {
				r(new $(Q.InvalidRequest, "Request cancelled"));
				return;
			}
			let i = setTimeout(e, n);
			t.addEventListener("abort", () => {
				clearTimeout(i), r(new $(Q.InvalidRequest, "Request cancelled"));
			}, { once: !0 });
		});
	}
	requestTaskStore(e, t) {
		let n = this._taskStore;
		if (!n) throw Error("No task store configured");
		return {
			createTask: async (r) => {
				if (!e) throw Error("No request provided");
				return await n.createTask(r, e.id, {
					method: e.method,
					params: e.params
				}, t);
			},
			getTask: async (e) => {
				let r = await n.getTask(e, t);
				if (!r) throw new $(Q.InvalidParams, "Failed to retrieve task: Task not found");
				return r;
			},
			storeTaskResult: async (e, r, i) => {
				await n.storeTaskResult(e, r, i, t);
				let a = await n.getTask(e, t);
				if (a) {
					let t = om.parse({
						method: "notifications/tasks/status",
						params: a
					});
					await this.notification(t), rg(a.status) && this._cleanupTaskProgressHandler(e);
				}
			},
			getTaskResult: (e) => n.getTaskResult(e, t),
			updateTaskStatus: async (e, r, i) => {
				let a = await n.getTask(e, t);
				if (!a) throw new $(Q.InvalidParams, `Task "${e}" not found - it may have been cleaned up`);
				if (rg(a.status)) throw new $(Q.InvalidParams, `Cannot update task "${e}" from terminal status "${a.status}" to "${r}". Terminal states (completed, failed, cancelled) cannot transition to other states.`);
				await n.updateTaskStatus(e, r, i, t);
				let o = await n.getTask(e, t);
				if (o) {
					let t = om.parse({
						method: "notifications/tasks/status",
						params: o
					});
					await this.notification(t), rg(o.status) && this._cleanupTaskProgressHandler(e);
				}
			},
			listTasks: (e) => n.listTasks(e, t)
		};
	}
};
function sg(e) {
	return typeof e == "object" && !!e && !Array.isArray(e);
}
function cg(e, t) {
	let n = { ...e };
	for (let e in t) {
		let r = e, i = t[r];
		if (i === void 0) continue;
		let a = n[r];
		n[r] = sg(a) && sg(i) ? {
			...a,
			...i
		} : i;
	}
	return n;
}
//#endregion
//#region node_modules/@modelcontextprotocol/ext-apps/dist/src/app-bridge.js
((e) => typeof t < "u" ? t : typeof Proxy < "u" ? new Proxy(e, { get: (e, n) => (typeof t < "u" ? t : e)[n] }) : e)(function(e) {
	if (typeof t < "u") return t.apply(this, arguments);
	throw Error("Dynamic require of \"" + e + "\" is not supported");
});
var lg = class extends og {
	constructor(...e) {
		super(...e), N(this, "_registeredMethods", /* @__PURE__ */ new Set()), N(this, "_eventSlots", /* @__PURE__ */ new Map()), N(this, "setRequestHandler", (e, t) => {
			this._assertMethodNotRegistered(e, "setRequestHandler"), super.setRequestHandler(e, t);
		}), N(this, "setNotificationHandler", (e, t) => {
			this._assertMethodNotRegistered(e, "setNotificationHandler"), super.setNotificationHandler(e, t);
		}), N(this, "replaceRequestHandler", (e, t) => {
			let n = e.shape.method.value;
			this._registeredMethods.add(n), super.setRequestHandler(e, t);
		});
	}
	onEventDispatch(e, t) {}
	_ensureEventSlot(e) {
		let t = this._eventSlots.get(e);
		if (!t) {
			let n = this.eventSchemas[e];
			if (!n) throw Error(`Unknown event: ${String(e)}`);
			t = { listeners: [] }, this._eventSlots.set(e, t);
			let r = n.shape.method.value;
			this._registeredMethods.add(r);
			let i = t;
			super.setNotificationHandler(n, (t) => {
				let n = t.params;
				this.onEventDispatch(e, n), i.onHandler?.(n);
				for (let e of [...i.listeners]) e(n);
			});
		}
		return t;
	}
	setEventHandler(e, t) {
		let n = this._ensureEventSlot(e);
		n.onHandler && t && console.warn(`[MCP Apps] on${String(e)} handler replaced. Use addEventListener("${String(e)}", …) to add multiple listeners without replacing.`), n.onHandler = t;
	}
	getEventHandler(e) {
		return this._eventSlots.get(e)?.onHandler;
	}
	addEventListener(e, t) {
		this._ensureEventSlot(e).listeners.push(t);
	}
	removeEventListener(e, t) {
		let n = this._eventSlots.get(e);
		if (!n) return;
		let r = n.listeners.indexOf(t);
		r !== -1 && n.listeners.splice(r, 1);
	}
	warnIfRequestHandlerReplaced(e, t, n) {
		t && n && console.warn(`[MCP Apps] ${e} handler replaced. Previous handler will no longer be called.`);
	}
	_assertMethodNotRegistered(e, t) {
		let n = e.shape.method.value;
		if (this._registeredMethods.has(n)) throw Error(`Handler for "${n}" already registered (via ${t}). Use addEventListener() to attach multiple listeners, or the on* setter for replace semantics.`);
		this._registeredMethods.add(n);
	}
}, ug = "2026-01-26", dg = "ui/open-link", fg = "ui/download-file", pg = "ui/message", mg = "ui/notifications/sandbox-proxy-ready", hg = "ui/notifications/sandbox-resource-ready", gg = "ui/notifications/size-changed", _g = "ui/notifications/tool-input", vg = "ui/notifications/tool-input-partial", yg = "ui/notifications/tool-result", bg = "ui/notifications/tool-cancelled", xg = "ui/notifications/host-context-changed", Sg = "ui/notifications/request-teardown", Cg = "ui/resource-teardown", wg = "ui/initialize", Tg = "ui/notifications/initialized", Eg = "ui/request-display-mode", Dg = O([M("light"), M("dark")]).describe("Color theme preference for the host environment."), Og = O([
	M("inline"),
	M("fullscreen"),
	M("pip")
]).describe("Display mode for UI presentation."), kg = O([
	M("--color-background-primary"),
	M("--color-background-secondary"),
	M("--color-background-tertiary"),
	M("--color-background-inverse"),
	M("--color-background-ghost"),
	M("--color-background-info"),
	M("--color-background-danger"),
	M("--color-background-success"),
	M("--color-background-warning"),
	M("--color-background-disabled"),
	M("--color-text-primary"),
	M("--color-text-secondary"),
	M("--color-text-tertiary"),
	M("--color-text-inverse"),
	M("--color-text-ghost"),
	M("--color-text-info"),
	M("--color-text-danger"),
	M("--color-text-success"),
	M("--color-text-warning"),
	M("--color-text-disabled"),
	M("--color-border-primary"),
	M("--color-border-secondary"),
	M("--color-border-tertiary"),
	M("--color-border-inverse"),
	M("--color-border-ghost"),
	M("--color-border-info"),
	M("--color-border-danger"),
	M("--color-border-success"),
	M("--color-border-warning"),
	M("--color-border-disabled"),
	M("--color-ring-primary"),
	M("--color-ring-secondary"),
	M("--color-ring-inverse"),
	M("--color-ring-info"),
	M("--color-ring-danger"),
	M("--color-ring-success"),
	M("--color-ring-warning"),
	M("--font-sans"),
	M("--font-mono"),
	M("--font-weight-normal"),
	M("--font-weight-medium"),
	M("--font-weight-semibold"),
	M("--font-weight-bold"),
	M("--font-text-xs-size"),
	M("--font-text-sm-size"),
	M("--font-text-md-size"),
	M("--font-text-lg-size"),
	M("--font-heading-xs-size"),
	M("--font-heading-sm-size"),
	M("--font-heading-md-size"),
	M("--font-heading-lg-size"),
	M("--font-heading-xl-size"),
	M("--font-heading-2xl-size"),
	M("--font-heading-3xl-size"),
	M("--font-text-xs-line-height"),
	M("--font-text-sm-line-height"),
	M("--font-text-md-line-height"),
	M("--font-text-lg-line-height"),
	M("--font-heading-xs-line-height"),
	M("--font-heading-sm-line-height"),
	M("--font-heading-md-line-height"),
	M("--font-heading-lg-line-height"),
	M("--font-heading-xl-line-height"),
	M("--font-heading-2xl-line-height"),
	M("--font-heading-3xl-line-height"),
	M("--border-radius-xs"),
	M("--border-radius-sm"),
	M("--border-radius-md"),
	M("--border-radius-lg"),
	M("--border-radius-xl"),
	M("--border-radius-full"),
	M("--border-width-regular"),
	M("--shadow-hairline"),
	M("--shadow-sm"),
	M("--shadow-md"),
	M("--shadow-lg")
]).describe("CSS variable keys available to MCP apps for theming."), Ag = w(kg.describe("Style variables for theming MCP apps.\n\nIndividual style keys are optional - hosts may provide any subset of these values.\nValues are strings containing CSS values (colors, sizes, font stacks, etc.).\n\nNote: This type uses `Record<K, string | undefined>` rather than `Partial<Record<K, string>>`\nfor compatibility with Zod schema generation. Both are functionally equivalent for validation."), O([y(), re()]).describe("Style variables for theming MCP apps.\n\nIndividual style keys are optional - hosts may provide any subset of these values.\nValues are strings containing CSS values (colors, sizes, font stacks, etc.).\n\nNote: This type uses `Record<K, string | undefined>` rather than `Partial<Record<K, string>>`\nfor compatibility with Zod schema generation. Both are functionally equivalent for validation.")).describe("Style variables for theming MCP apps.\n\nIndividual style keys are optional - hosts may provide any subset of these values.\nValues are strings containing CSS values (colors, sizes, font stacks, etc.).\n\nNote: This type uses `Record<K, string | undefined>` rather than `Partial<Record<K, string>>`\nfor compatibility with Zod schema generation. Both are functionally equivalent for validation."), jg = _({
	method: M("ui/open-link"),
	params: _({ url: y().describe("URL to open in the host's browser") })
}), Mg = _({ isError: b().optional().describe("True if the host failed to open the URL (e.g., due to security policy).") }).passthrough(), Ng = _({ isError: b().optional().describe("True if the download failed (e.g., user cancelled or host denied).") }).passthrough(), Pg = _({ isError: b().optional().describe("True if the host rejected or failed to deliver the message.") }).passthrough(), Fg = _({
	method: M("ui/notifications/sandbox-proxy-ready"),
	params: _({})
}), Ig = _({
	connectDomains: x(y()).optional().describe("Origins for network requests (fetch/XHR/WebSocket).\n\n- Maps to CSP `connect-src` directive\n- Empty or omitted → no network connections (secure default)"),
	resourceDomains: x(y()).optional().describe("Origins for static resources (images, scripts, stylesheets, fonts, media).\n\n- Maps to CSP `img-src`, `script-src`, `style-src`, `font-src`, `media-src` directives\n- Wildcard subdomains supported: `https://*.example.com`\n- Empty or omitted → no network resources (secure default)"),
	frameDomains: x(y()).optional().describe("Origins for nested iframes.\n\n- Maps to CSP `frame-src` directive\n- Empty or omitted → no nested iframes allowed (`frame-src 'none'`)"),
	baseUriDomains: x(y()).optional().describe("Allowed base URIs for the document.\n\n- Maps to CSP `base-uri` directive\n- Empty or omitted → only same origin allowed (`base-uri 'self'`)")
}), Lg = _({
	camera: _({}).optional().describe("Request camera access.\n\nMaps to Permission Policy `camera` feature."),
	microphone: _({}).optional().describe("Request microphone access.\n\nMaps to Permission Policy `microphone` feature."),
	geolocation: _({}).optional().describe("Request geolocation access.\n\nMaps to Permission Policy `geolocation` feature."),
	clipboardWrite: _({}).optional().describe("Request clipboard write access.\n\nMaps to Permission Policy `clipboard-write` feature.")
}), Rg = _({
	method: M("ui/notifications/size-changed"),
	params: _({
		width: S().optional().describe("New width in pixels."),
		height: S().optional().describe("New height in pixels.")
	})
}), zg = _({
	method: M("ui/notifications/tool-input"),
	params: _({ arguments: w(y(), A().describe("Complete tool call arguments as key-value pairs.")).optional().describe("Complete tool call arguments as key-value pairs.") })
}), Bg = _({
	method: M("ui/notifications/tool-input-partial"),
	params: _({ arguments: w(y(), A().describe("Partial tool call arguments (incomplete, may change).")).optional().describe("Partial tool call arguments (incomplete, may change).") })
}), Vg = _({
	method: M("ui/notifications/tool-cancelled"),
	params: _({ reason: y().optional().describe("Optional reason for the cancellation (e.g., \"user action\", \"timeout\").") })
}), Hg = _({ fonts: y().optional() }), Ug = _({
	variables: Ag.optional().describe("CSS variables for theming the app."),
	css: Hg.optional().describe("CSS blocks that apps can inject.")
}), Wg = _({
	method: M("ui/resource-teardown"),
	params: _({})
}), Gg = w(y(), A()), Kg = _({
	text: _({}).optional().describe("Host supports text content blocks."),
	image: _({}).optional().describe("Host supports image content blocks."),
	audio: _({}).optional().describe("Host supports audio content blocks."),
	resource: _({}).optional().describe("Host supports resource content blocks."),
	resourceLink: _({}).optional().describe("Host supports resource link content blocks."),
	structuredContent: _({}).optional().describe("Host supports structured content.")
}), qg = _({
	method: M("ui/notifications/request-teardown"),
	params: _({}).optional()
}), Jg = _({
	experimental: w(y(), w(y(), Qt()).describe("Experimental features keyed by identifier.")).optional().describe("Experimental features keyed by identifier."),
	openLinks: _({}).optional().describe("Host supports opening external URLs."),
	downloadFile: _({}).optional().describe("Host supports file downloads via ui/download-file."),
	serverTools: _({ listChanged: b().optional().describe("Host supports tools/list_changed notifications.") }).optional().describe("Host can proxy tool calls to the MCP server."),
	serverResources: _({ listChanged: b().optional().describe("Host supports resources/list_changed notifications.") }).optional().describe("Host can proxy resource reads to the MCP server."),
	logging: _({}).optional().describe("Host accepts log messages."),
	sandbox: _({
		permissions: Lg.optional().describe("Permissions granted by the host (camera, microphone, geolocation)."),
		csp: Ig.optional().describe("CSP domains approved by the host.")
	}).optional().describe("Sandbox configuration applied by the host."),
	updateModelContext: Kg.optional().describe("Host accepts context updates (ui/update-model-context) to be included in the model's context for future turns."),
	message: Kg.optional().describe("Host supports receiving content messages (ui/message) from the view."),
	sampling: _({ tools: _({}).optional().describe("Host supports tool use via `tools` and `toolChoice` parameters.") }).optional().describe("Host supports LLM sampling (sampling/createMessage) from the view.\nMirrors the MCP `ClientCapabilities.sampling` shape so hosts can pass it through.")
}), Yg = _({
	experimental: w(y(), w(y(), Qt()).describe("Experimental features keyed by identifier.")).optional().describe("Experimental features keyed by identifier."),
	tools: _({ listChanged: b().optional().describe("App supports tools/list_changed notifications.") }).optional().describe("App exposes MCP-style tools that the host can call."),
	availableDisplayModes: x(Og).optional().describe("Display modes the app supports.")
}), Xg = _({
	method: M("ui/notifications/initialized"),
	params: _({}).optional()
}), Zg = _({
	csp: Ig.optional().describe("Content Security Policy configuration for UI resources."),
	permissions: Lg.optional().describe("Sandbox permissions requested by the UI resource."),
	domain: y().optional().describe("Dedicated origin for view sandbox.\n\nUseful when views need stable, dedicated origins for OAuth callbacks, CORS policies, or API key allowlists.\n\n**Host-dependent:** The format and validation rules for this field are determined by each host. Servers MUST consult host-specific documentation for the expected domain format. Common patterns include:\n- Hash-based subdomains (e.g., `{hash}.claudemcpcontent.com`)\n- URL-derived subdomains (e.g., `www-example-com.oaiusercontent.com`)\n\nIf omitted, host uses default sandbox origin (typically per-conversation)."),
	prefersBorder: b().optional().describe("Visual boundary preference - true if view prefers a visible border.\n\nBoolean requesting whether a visible border and background is provided by the host. Specifying an explicit value for this is recommended because hosts' defaults may vary.\n\n- `true`: request visible border + background\n- `false`: request no visible border + background\n- omitted: host decides border")
}), Qg = _({
	method: M("ui/request-display-mode"),
	params: _({ mode: Og.describe("The display mode being requested.") })
}), $g = _({ mode: Og.describe("The display mode that was actually set. May differ from requested if not supported.") }).passthrough(), e_ = O([M("model"), M("app")]).describe("Tool visibility scope - who can access the tool."), t_ = _({
	resourceUri: y().optional(),
	visibility: x(e_).optional().describe("Who can access this tool. Default: [\"model\", \"app\"]\n- \"model\": Tool visible to and callable by the agent\n- \"app\": Tool callable by the app from this server only"),
	csp: m().optional(),
	permissions: m().optional()
});
_({ mimeTypes: x(y()).optional().describe("Array of supported MIME types for UI resources.\nMust include `\"text/html;profile=mcp-app\"` for MCP Apps support.") });
var n_ = _({
	method: M("ui/download-file"),
	params: _({ contents: x(O([qm, Jm])).describe("Resource contents to download — embedded (inline data) or linked (host fetches). Uses standard MCP resource types.") })
}), r_ = _({
	method: M("ui/message"),
	params: _({
		role: M("user").describe("Message role, currently only \"user\" is supported."),
		content: x(Ym).describe("Message content blocks (text, image, etc.).")
	})
}), i_ = _({
	method: M("ui/notifications/sandbox-resource-ready"),
	params: _({
		html: y().describe("HTML content to load into the inner iframe."),
		sandbox: y().optional().describe("Optional override for the inner iframe's sandbox attribute."),
		csp: Ig.optional().describe("CSP configuration from resource metadata."),
		permissions: Lg.optional().describe("Sandbox permissions from resource metadata.")
	})
}), a_ = _({
	method: M("ui/notifications/tool-result"),
	params: ih.describe("Standard MCP tool execution result.")
}), o_ = _({
	toolInfo: _({
		id: Sp.optional().describe("JSON-RPC id of the tools/call request."),
		tool: th.describe("Tool definition including name, inputSchema, etc.")
	}).optional().describe("Metadata of the tool call that instantiated this App."),
	theme: Dg.optional().describe("Current color theme preference."),
	styles: Ug.optional().describe("Style configuration for theming the app."),
	displayMode: Og.optional().describe("How the UI is currently displayed."),
	availableDisplayModes: x(Og).optional().describe("Display modes the host supports."),
	containerDimensions: O([_({ height: S().describe("Fixed container height in pixels.") }), _({ maxHeight: O([S(), re()]).optional().describe("Maximum container height in pixels.") })]).and(O([_({ width: S().describe("Fixed container width in pixels.") }), _({ maxWidth: O([S(), re()]).optional().describe("Maximum container width in pixels.") })])).optional().describe("Container dimensions. Represents the dimensions of the iframe or other\ncontainer holding the app. Specify either width or maxWidth, and either height or maxHeight."),
	locale: y().optional().describe("User's language and region preference in BCP 47 format."),
	timeZone: y().optional().describe("User's timezone in IANA format."),
	userAgent: y().optional().describe("Host application identifier."),
	platform: O([
		M("web"),
		M("desktop"),
		M("mobile")
	]).optional().describe("Platform type for responsive design decisions."),
	deviceCapabilities: _({
		touch: b().optional().describe("Whether the device supports touch input."),
		hover: b().optional().describe("Whether the device supports hover interactions.")
	}).optional().describe("Device input capabilities."),
	safeAreaInsets: _({
		top: S().describe("Top safe area inset in pixels."),
		right: S().describe("Right safe area inset in pixels."),
		bottom: S().describe("Bottom safe area inset in pixels."),
		left: S().describe("Left safe area inset in pixels.")
	}).optional().describe("Mobile safe area boundaries in pixels.")
}).passthrough(), s_ = _({
	method: M("ui/notifications/host-context-changed"),
	params: o_.describe("Partial context update containing only changed fields.")
}), c_ = _({
	method: M("ui/update-model-context"),
	params: _({
		content: x(Ym).optional().describe("Context content blocks (text, image, etc.)."),
		structuredContent: w(y(), A().describe("Structured content for machine-readable context data.")).optional().describe("Structured content for machine-readable context data.")
	})
}), l_ = _({
	method: M("ui/initialize"),
	params: _({
		appInfo: Rp.describe("App identification (name and version)."),
		appCapabilities: Yg.describe("Features and capabilities this app provides."),
		protocolVersion: y().describe("Protocol version this app supports.")
	})
}), u_ = _({
	protocolVersion: y().describe("Negotiated protocol version string (e.g., \"2025-11-21\")."),
	hostInfo: Rp.describe("Host application identification and version."),
	hostCapabilities: Jg.describe("Features and capabilities provided by the host."),
	hostContext: o_.describe("Rich context about the host environment.")
}).passthrough(), d_ = class {
	constructor(e = window.parent, t) {
		N(this, "eventTarget", void 0), N(this, "eventSource", void 0), N(this, "messageListener", void 0), N(this, "onclose", void 0), N(this, "onerror", void 0), N(this, "onmessage", void 0), N(this, "sessionId", void 0), N(this, "setProtocolVersion", void 0), this.eventTarget = e, this.eventSource = t, this.messageListener = (e) => {
			if (t && e.source !== this.eventSource) {
				console.debug("Ignoring message from unknown source", e);
				return;
			}
			let n = jp.safeParse(e.data);
			n.success ? (console.debug("Parsed message", n.data), this.onmessage?.(n.data)) : e.data?.jsonrpc === "2.0" ? (console.error("Failed to parse message", n.error.message, e), this.onerror?.(Error("Invalid JSON-RPC message received: " + n.error.message))) : console.debug("Ignoring non-JSON-RPC message", n.error.message, e);
		};
	}
	async start() {
		window.addEventListener("message", this.messageListener);
	}
	async send(e, t) {
		e.method !== "ui/notifications/tool-input-partial" && console.debug("Sending message", e), this.eventTarget.postMessage(e, "*");
	}
	async close() {
		window.removeEventListener("message", this.messageListener), this.onclose?.();
	}
}, f_ = { target: "draft-2020-12" };
async function p_(e, t) {
	let n = e["~standard"];
	if (n.jsonSchema) return n.jsonSchema[t](f_);
	if (n.vendor === "zod") {
		let { z: n } = await import("./v4-CmfYQIuR.mjs");
		return n.toJSONSchema(e, { io: t });
	}
	throw Error(`Schema (vendor: ${n.vendor}) does not implement Standard JSON Schema (~standard.jsonSchema). Use a library that does (zod v4, ArkType, Valibot) or wrap your schema accordingly.`);
}
async function m_(e, t, n = "") {
	let r = await e["~standard"].validate(t);
	if (r.issues) {
		let e = r.issues.map((e) => {
			let t = e.path?.map((e) => typeof e == "object" ? e.key : e).join(".");
			return t ? `${t}: ${e.message}` : e.message;
		}).join("; ");
		throw Error(n + e);
	}
	return r.value;
}
var h_ = "ui/resourceUri", g_ = "text/html;profile=mcp-app";
N(class e extends lg {
	_assertInitialized(e) {
		if (this._initializedSent) return;
		let t = `[ext-apps] App.${e}() called before connect() completed the ui/initialize handshake. Await app.connect() before calling this method, or move data loading to an ontoolresult handler.`;
		if (this.options?.strict) throw Error(t);
		console.warn(`${t}. This will throw in a future release.`);
	}
	_assertHandlerTiming(t) {
		if (!e.ONE_SHOT_EVENTS.has(t) || this._everHadListener.has(t) || (this._everHadListener.add(t), !this._initializedSent)) return;
		let n = `[ext-apps] "${String(t)}" handler registered after connect() completed the ui/initialize handshake. The host may have already sent this notification. Register handlers before calling app.connect().`;
		if (this.options?.strict) throw Error(n);
		console.warn(n);
	}
	setEventHandler(e, t) {
		t && this._assertHandlerTiming(e), super.setEventHandler(e, t);
	}
	addEventListener(e, t) {
		this._assertHandlerTiming(e), super.addEventListener(e, t);
	}
	onEventDispatch(e, t) {
		e === "hostcontextchanged" && (this._hostContext = {
			...this._hostContext,
			...t
		});
	}
	constructor(e, t = {}, n = { autoResize: !0 }) {
		super(n), N(this, "_appInfo", void 0), N(this, "_capabilities", void 0), N(this, "options", void 0), N(this, "_hostCapabilities", void 0), N(this, "_hostInfo", void 0), N(this, "_hostContext", void 0), N(this, "_registeredTools", {}), N(this, "_initializedSent", !1), N(this, "eventSchemas", {
			toolinput: zg,
			toolinputpartial: Bg,
			toolresult: a_,
			toolcancelled: Vg,
			hostcontextchanged: s_
		}), N(this, "_everHadListener", /* @__PURE__ */ new Set()), N(this, "_toolHandlersInitialized", !1), N(this, "_onteardown", void 0), N(this, "_oncalltool", void 0), N(this, "_onlisttools", void 0), N(this, "sendOpenLink", this.openLink), this._appInfo = e, this._capabilities = t, this.options = n, n.allowUnsafeEval || Fs({ jitless: !0 }), this.setRequestHandler(Yp, (e) => (console.log("Received ping:", e.params), {})), this.setEventHandler("hostcontextchanged", void 0);
	}
	registerCapabilities(e) {
		if (this.transport) throw Error("Cannot register capabilities after transport is established");
		this._capabilities = cg(this._capabilities, e);
	}
	registerTool(e, t, n) {
		if (this._registeredTools[e]) throw Error(`Tool ${e} is already registered`);
		let r = this, i = () => {
			r._initializedSent && r._capabilities.tools?.listChanged && r.sendToolListChanged();
		}, a = t.inputSchema !== void 0, o = {
			title: t.title,
			description: t.description,
			inputSchema: t.inputSchema,
			outputSchema: t.outputSchema,
			annotations: t.annotations,
			_meta: t._meta,
			enabled: !0,
			enable() {
				this.enabled = !0, i();
			},
			disable() {
				this.enabled = !1, i();
			},
			update(e) {
				Object.assign(this, e), i();
			},
			remove() {
				r._registeredTools[e] === o && (delete r._registeredTools[e], i());
			},
			handler: async (t, r) => {
				if (!o.enabled) throw Error(`Tool ${e} is disabled`);
				let i;
				if (a) {
					let a = o.inputSchema;
					i = await n(a ? await m_(a, t ?? {}, `Invalid input for tool ${e}: `) : t ?? {}, r);
				} else i = await n(r);
				return o.outputSchema && !i.isError && (i.structuredContent = await m_(o.outputSchema, i.structuredContent, `Invalid output for tool ${e}: `)), i;
			}
		};
		return this._registeredTools[e] = o, !this._capabilities.tools && !this.transport && this.registerCapabilities({ tools: { listChanged: !0 } }), this.ensureToolHandlersInitialized(), i(), o;
	}
	ensureToolHandlersInitialized() {
		this._toolHandlersInitialized || (this._toolHandlersInitialized = !0, this.oncalltool = async (e, t) => {
			let n = this._registeredTools[e.name];
			if (!n) throw Error(`Tool ${e.name} not found`);
			return n.handler(e.arguments, t);
		}, this.onlisttools = async (e, t) => ({ tools: await Promise.all(Object.entries(this._registeredTools).filter(([e, t]) => t.enabled).map(async ([e, t]) => {
			let n = {
				name: e,
				title: t.title,
				description: t.description,
				inputSchema: t.inputSchema ? await p_(t.inputSchema, "input") : {
					type: "object",
					properties: {}
				}
			};
			return t.outputSchema && (n.outputSchema = await p_(t.outputSchema, "output")), t.annotations && (n.annotations = t.annotations), t._meta && (n._meta = t._meta), n;
		})) }));
	}
	async sendToolListChanged(e = {}) {
		this._assertInitialized("sendToolListChanged"), await this.notification({
			method: "notifications/tools/list_changed",
			params: e
		});
	}
	getHostCapabilities() {
		return this._hostCapabilities;
	}
	getHostVersion() {
		return this._hostInfo;
	}
	getHostContext() {
		return this._hostContext;
	}
	get ontoolinput() {
		return this.getEventHandler("toolinput");
	}
	set ontoolinput(e) {
		this.setEventHandler("toolinput", e);
	}
	get ontoolinputpartial() {
		return this.getEventHandler("toolinputpartial");
	}
	set ontoolinputpartial(e) {
		this.setEventHandler("toolinputpartial", e);
	}
	get ontoolresult() {
		return this.getEventHandler("toolresult");
	}
	set ontoolresult(e) {
		this.setEventHandler("toolresult", e);
	}
	get ontoolcancelled() {
		return this.getEventHandler("toolcancelled");
	}
	set ontoolcancelled(e) {
		this.setEventHandler("toolcancelled", e);
	}
	get onhostcontextchanged() {
		return this.getEventHandler("hostcontextchanged");
	}
	set onhostcontextchanged(e) {
		this.setEventHandler("hostcontextchanged", e);
	}
	get onteardown() {
		return this._onteardown;
	}
	set onteardown(e) {
		this.warnIfRequestHandlerReplaced("onteardown", this._onteardown, e), this._onteardown = e, this.replaceRequestHandler(Wg, (e, t) => {
			if (!this._onteardown) throw Error("No onteardown handler set");
			return this._onteardown(e.params, t);
		});
	}
	get oncalltool() {
		return this._oncalltool;
	}
	set oncalltool(e) {
		this.warnIfRequestHandlerReplaced("oncalltool", this._oncalltool, e), this._oncalltool = e, this.replaceRequestHandler(oh, (e, t) => {
			if (!this._oncalltool) throw Error("No oncalltool handler set");
			return this._oncalltool(e.params, t);
		});
	}
	get onlisttools() {
		return this._onlisttools;
	}
	set onlisttools(e) {
		this.warnIfRequestHandlerReplaced("onlisttools", this._onlisttools, e), this._onlisttools = e, this.replaceRequestHandler(nh, (e, t) => {
			if (!this._onlisttools) throw Error("No onlisttools handler set");
			return this._onlisttools(e.params, t);
		});
	}
	assertCapabilityForMethod(e) {
		if (e === "sampling/createMessage" && !this._hostCapabilities?.sampling) throw Error(`Host does not support sampling (required for ${e})`);
	}
	assertRequestHandlerCapability(e) {
		switch (e) {
			case "tools/call":
			case "tools/list":
				if (!this._capabilities.tools) throw Error(`Client does not support tool capability (required for ${e})`);
				return;
			case "ping":
			case "ui/resource-teardown": return;
			default: throw Error(`No handler for method ${e} registered`);
		}
	}
	assertNotificationCapability(e) {}
	assertTaskCapability(e) {
		throw Error("Tasks are not supported in MCP Apps");
	}
	assertTaskHandlerCapability(e) {
		throw Error("Task handlers are not supported in MCP Apps");
	}
	async callServerTool(e, t) {
		if (this._assertInitialized("callServerTool"), typeof e == "string") throw Error(`callServerTool() expects an object as its first argument, but received a string ("${e}"). Did you mean: callServerTool({ name: "${e}", arguments: { ... } })?`);
		return await this.request({
			method: "tools/call",
			params: e
		}, ih, {
			onprogress: () => {},
			resetTimeoutOnProgress: !0,
			...t
		});
	}
	async readServerResource(e, t) {
		return this._assertInitialized("readServerResource"), await this.request({
			method: "resources/read",
			params: e
		}, km, t);
	}
	async listServerResources(e, t) {
		return this._assertInitialized("listServerResources"), await this.request({
			method: "resources/list",
			params: e
		}, Cm, t);
	}
	async createSamplingMessage(e, t) {
		this._assertInitialized("createSamplingMessage");
		let n = e.tools ? Ch : Sh;
		return await this.request({
			method: "sampling/createMessage",
			params: e
		}, n, t);
	}
	sendMessage(e, t) {
		return this._assertInitialized("sendMessage"), this.request({
			method: "ui/message",
			params: e
		}, Pg, t);
	}
	sendLog(e) {
		return this.notification({
			method: "notifications/message",
			params: e
		});
	}
	updateModelContext(e, t) {
		return this._assertInitialized("updateModelContext"), this.request({
			method: "ui/update-model-context",
			params: e
		}, Mp, t);
	}
	openLink(e, t) {
		return this._assertInitialized("openLink"), this.request({
			method: "ui/open-link",
			params: e
		}, Mg, t);
	}
	downloadFile(e, t) {
		return this._assertInitialized("downloadFile"), this.request({
			method: "ui/download-file",
			params: e
		}, Ng, t);
	}
	requestTeardown(e = {}) {
		return this.notification({
			method: "ui/notifications/request-teardown",
			params: e
		});
	}
	requestDisplayMode(e, t) {
		return this._assertInitialized("requestDisplayMode"), this.request({
			method: "ui/request-display-mode",
			params: e
		}, $g, t);
	}
	sendSizeChanged(e) {
		return this.notification({
			method: "ui/notifications/size-changed",
			params: e
		});
	}
	setupSizeChangedNotifications() {
		let e = !1, t = 0, n = 0, r = () => {
			e || (e = !0, requestAnimationFrame(() => {
				e = !1;
				let r = document.documentElement, i = r.style.height;
				r.style.height = "max-content";
				let a = Math.ceil(r.getBoundingClientRect().height);
				r.style.height = i;
				let o = Math.ceil(window.innerWidth);
				(o !== t || a !== n) && (t = o, n = a, this.sendSizeChanged({
					width: o,
					height: a
				}));
			}));
		};
		r();
		let i = new ResizeObserver(r);
		return i.observe(document.documentElement), i.observe(document.body), () => i.disconnect();
	}
	async connect(e = new d_(window.parent, window.parent), t) {
		if (this.transport) throw Error("App is already connected. Call close() before connecting again.");
		this._initializedSent = !1, await super.connect(e);
		try {
			let e = await this.request({
				method: "ui/initialize",
				params: {
					appCapabilities: this._capabilities,
					appInfo: this._appInfo,
					protocolVersion: ug
				}
			}, u_, t);
			if (e === void 0) throw Error(`Server sent invalid initialize result: ${e}`);
			this._hostCapabilities = e.hostCapabilities, this._hostInfo = e.hostInfo, this._hostContext = e.hostContext, await this.notification({ method: "ui/notifications/initialized" }), this._initializedSent = !0, this.options?.autoResize && this.setupSizeChangedNotifications();
		} catch (e) {
			throw this.close(), e;
		}
	}
}, "ONE_SHOT_EVENTS", /* @__PURE__ */ new Set([
	"toolinput",
	"toolinputpartial",
	"toolresult",
	"toolcancelled"
]));
function __(e) {
	let t = e._meta?.ui?.resourceUri;
	if (t === void 0 && (t = e._meta?.[h_]), typeof t == "string" && t.startsWith("ui://")) return t;
	if (t !== void 0) throw Error(`Invalid UI resource URI: ${JSON.stringify(t)}`);
}
function v_(e) {
	let t = e._meta?.ui?.visibility;
	return t ? t.length === 1 && t[0] === "model" : !1;
}
function y_(e) {
	let t = e._meta?.ui?.visibility;
	return t ? t.length === 1 && t[0] === "app" : !1;
}
function b_(e) {
	if (!e) return "";
	let t = [];
	return e.camera && t.push("camera"), e.microphone && t.push("microphone"), e.geolocation && t.push("geolocation"), e.clipboardWrite && t.push("clipboard-write"), t.join("; ");
}
var x_ = [ug], S_ = class extends lg {
	constructor(e, t, n, r) {
		super(r), N(this, "_client", void 0), N(this, "_hostInfo", void 0), N(this, "_capabilities", void 0), N(this, "_appCapabilities", void 0), N(this, "_hostContext", {}), N(this, "_appInfo", void 0), N(this, "_initializedReceived", !1), N(this, "_baseReplaceRequestHandler", this.replaceRequestHandler), N(this, "replaceRequestHandler", (e, t) => {
			this._baseReplaceRequestHandler(e, (e, n) => (this._initializedReceived || console.warn(`[ext-apps] AppBridge received '${e.method}' before ui/notifications/initialized. The View is calling host methods before completing the handshake; it should await app.connect() first.`), t(e, n)));
		}), N(this, "eventSchemas", {
			sizechange: Rg,
			sandboxready: Fg,
			initialized: Xg,
			requestteardown: qg,
			loggingmessage: fh
		}), N(this, "onping", void 0), N(this, "_onmessage", void 0), N(this, "_onopenlink", void 0), N(this, "_ondownloadfile", void 0), N(this, "_onrequestdisplaymode", void 0), N(this, "_onupdatemodelcontext", void 0), N(this, "_oncalltool", void 0), N(this, "_onlistresources", void 0), N(this, "_onlistresourcetemplates", void 0), N(this, "_onreadresource", void 0), N(this, "_onlistprompts", void 0), N(this, "sendResourceTeardown", this.teardownResource), this._client = e, this._hostInfo = t, this._capabilities = n, this.addEventListener("initialized", () => {
			this._initializedReceived = !0;
		}), this._hostContext = r?.hostContext || {}, this.setRequestHandler(l_, (e) => this._oninitialize(e)), this.setRequestHandler(Yp, (e, t) => (this.onping?.(e.params, t), {})), this.replaceRequestHandler(Qg, (e) => ({ mode: this._hostContext.displayMode ?? "inline" }));
	}
	getAppCapabilities() {
		return this._appCapabilities;
	}
	getAppVersion() {
		return this._appInfo;
	}
	get onsizechange() {
		return this.getEventHandler("sizechange");
	}
	set onsizechange(e) {
		this.setEventHandler("sizechange", e);
	}
	get onsandboxready() {
		return this.getEventHandler("sandboxready");
	}
	set onsandboxready(e) {
		this.setEventHandler("sandboxready", e);
	}
	get oninitialized() {
		return this.getEventHandler("initialized");
	}
	set oninitialized(e) {
		this.setEventHandler("initialized", e);
	}
	get onmessage() {
		return this._onmessage;
	}
	set onmessage(e) {
		this.warnIfRequestHandlerReplaced("onmessage", this._onmessage, e), this._onmessage = e, this.replaceRequestHandler(r_, async (e, t) => {
			if (!this._onmessage) throw Error("No onmessage handler set");
			return this._onmessage(e.params, t);
		});
	}
	get onopenlink() {
		return this._onopenlink;
	}
	set onopenlink(e) {
		this.warnIfRequestHandlerReplaced("onopenlink", this._onopenlink, e), this._onopenlink = e, this.replaceRequestHandler(jg, async (e, t) => {
			if (!this._onopenlink) throw Error("No onopenlink handler set");
			return this._onopenlink(e.params, t);
		});
	}
	get ondownloadfile() {
		return this._ondownloadfile;
	}
	set ondownloadfile(e) {
		this.warnIfRequestHandlerReplaced("ondownloadfile", this._ondownloadfile, e), this._ondownloadfile = e, this.replaceRequestHandler(n_, async (e, t) => {
			if (!this._ondownloadfile) throw Error("No ondownloadfile handler set");
			return this._ondownloadfile(e.params, t);
		});
	}
	get onrequestteardown() {
		return this.getEventHandler("requestteardown");
	}
	set onrequestteardown(e) {
		this.setEventHandler("requestteardown", e);
	}
	get onrequestdisplaymode() {
		return this._onrequestdisplaymode;
	}
	set onrequestdisplaymode(e) {
		this.warnIfRequestHandlerReplaced("onrequestdisplaymode", this._onrequestdisplaymode, e), this._onrequestdisplaymode = e, this.replaceRequestHandler(Qg, async (e, t) => {
			if (!this._onrequestdisplaymode) throw Error("No onrequestdisplaymode handler set");
			return this._onrequestdisplaymode(e.params, t);
		});
	}
	get onloggingmessage() {
		return this.getEventHandler("loggingmessage");
	}
	set onloggingmessage(e) {
		this.setEventHandler("loggingmessage", e);
	}
	get onupdatemodelcontext() {
		return this._onupdatemodelcontext;
	}
	set onupdatemodelcontext(e) {
		this.warnIfRequestHandlerReplaced("onupdatemodelcontext", this._onupdatemodelcontext, e), this._onupdatemodelcontext = e, this.replaceRequestHandler(c_, async (e, t) => {
			if (!this._onupdatemodelcontext) throw Error("No onupdatemodelcontext handler set");
			return this._onupdatemodelcontext(e.params, t);
		});
	}
	get oncalltool() {
		return this._oncalltool;
	}
	set oncalltool(e) {
		this.warnIfRequestHandlerReplaced("oncalltool", this._oncalltool, e), this._oncalltool = e, this.replaceRequestHandler(oh, async (e, t) => {
			if (!this._oncalltool) throw Error("No oncalltool handler set");
			return this._oncalltool(e.params, t);
		});
	}
	set oncreatesamplingmessage(e) {
		this.setRequestHandler(xh, async (t, n) => e(t.params, n));
	}
	sendToolListChanged(e = {}) {
		return this.notification({
			method: "notifications/tools/list_changed",
			params: e
		});
	}
	get onlistresources() {
		return this._onlistresources;
	}
	set onlistresources(e) {
		this.warnIfRequestHandlerReplaced("onlistresources", this._onlistresources, e), this._onlistresources = e, this.replaceRequestHandler(Sm, async (e, t) => {
			if (!this._onlistresources) throw Error("No onlistresources handler set");
			return this._onlistresources(e.params, t);
		});
	}
	get onlistresourcetemplates() {
		return this._onlistresourcetemplates;
	}
	set onlistresourcetemplates(e) {
		this.warnIfRequestHandlerReplaced("onlistresourcetemplates", this._onlistresourcetemplates, e), this._onlistresourcetemplates = e, this.replaceRequestHandler(wm, async (e, t) => {
			if (!this._onlistresourcetemplates) throw Error("No onlistresourcetemplates handler set");
			return this._onlistresourcetemplates(e.params, t);
		});
	}
	get onreadresource() {
		return this._onreadresource;
	}
	set onreadresource(e) {
		this.warnIfRequestHandlerReplaced("onreadresource", this._onreadresource, e), this._onreadresource = e, this.replaceRequestHandler(Om, async (e, t) => {
			if (!this._onreadresource) throw Error("No onreadresource handler set");
			return this._onreadresource(e.params, t);
		});
	}
	sendResourceListChanged(e = {}) {
		return this.notification({
			method: "notifications/resources/list_changed",
			params: e
		});
	}
	get onlistprompts() {
		return this._onlistprompts;
	}
	set onlistprompts(e) {
		this.warnIfRequestHandlerReplaced("onlistprompts", this._onlistprompts, e), this._onlistprompts = e, this.replaceRequestHandler(zm, async (e, t) => {
			if (!this._onlistprompts) throw Error("No onlistprompts handler set");
			return this._onlistprompts(e.params, t);
		});
	}
	sendPromptListChanged(e = {}) {
		return this.notification({
			method: "notifications/prompts/list_changed",
			params: e
		});
	}
	assertCapabilityForMethod(e) {}
	assertRequestHandlerCapability(e) {}
	assertNotificationCapability(e) {}
	assertTaskCapability(e) {
		throw Error("Tasks are not supported in MCP Apps");
	}
	assertTaskHandlerCapability(e) {
		throw Error("Task handlers are not supported in MCP Apps");
	}
	getCapabilities() {
		return this._capabilities;
	}
	async _oninitialize(e) {
		let t = e.params.protocolVersion;
		return this._appInfo !== void 0 && console.warn("[ext-apps] AppBridge received a second ui/initialize. The View may be double-mounting (e.g. React StrictMode in dev) without closing the previous App instance. Responding normally; the latest appInfo/appCapabilities replace the previous values."), this._appCapabilities = e.params.appCapabilities, this._appInfo = e.params.appInfo, {
			protocolVersion: x_.includes(t) ? t : ug,
			hostCapabilities: this.getCapabilities(),
			hostInfo: this._hostInfo,
			hostContext: this._hostContext
		};
	}
	setHostContext(e) {
		let t = {}, n = !1;
		for (let r of Object.keys(e)) {
			let i = this._hostContext[r], a = e[r];
			C_(i, a) || (t[r] = a, n = !0);
		}
		n && (this._hostContext = e, this.sendHostContextChange(t));
	}
	sendHostContextChange(e) {
		return this.notification({
			method: "ui/notifications/host-context-changed",
			params: e
		});
	}
	sendToolInput(e) {
		return this.notification({
			method: "ui/notifications/tool-input",
			params: e
		});
	}
	sendToolInputPartial(e) {
		return this.notification({
			method: "ui/notifications/tool-input-partial",
			params: e
		});
	}
	sendToolResult(e) {
		return this.notification({
			method: "ui/notifications/tool-result",
			params: e
		});
	}
	sendToolCancelled(e) {
		return this.notification({
			method: "ui/notifications/tool-cancelled",
			params: e
		});
	}
	sendSandboxResourceReady(e) {
		return this.notification({
			method: "ui/notifications/sandbox-resource-ready",
			params: e
		});
	}
	teardownResource(e, t) {
		return this.request({
			method: "ui/resource-teardown",
			params: e
		}, Gg, t);
	}
	callTool(e, t) {
		return this.request({
			method: "tools/call",
			params: e
		}, ih, t);
	}
	listTools(e, t) {
		return this.request({
			method: "tools/list",
			params: e
		}, rh, t);
	}
	async connect(e) {
		if (this.transport) throw Error("AppBridge is already connected. Call close() before connecting again.");
		if (this._initializedReceived = !1, this._client) {
			let e = this._client.getServerCapabilities();
			if (!e) throw Error("Client server capabilities not available");
			e.tools && (this.oncalltool = async (e, t) => this._client.request({
				method: "tools/call",
				params: e
			}, ih, { signal: t.signal }), e.tools.listChanged && this._client.setNotificationHandler(sh, (e) => this.sendToolListChanged(e.params))), e.resources && (this.onlistresources = async (e, t) => this._client.request({
				method: "resources/list",
				params: e
			}, Cm, { signal: t.signal }), this.onlistresourcetemplates = async (e, t) => this._client.request({
				method: "resources/templates/list",
				params: e
			}, Tm, { signal: t.signal }), this.onreadresource = async (e, t) => this._client.request({
				method: "resources/read",
				params: e
			}, km, { signal: t.signal }), e.resources.listChanged && this._client.setNotificationHandler(Am, (e) => this.sendResourceListChanged(e.params))), e.prompts && (this.onlistprompts = async (e, t) => this._client.request({
				method: "prompts/list",
				params: e
			}, Bm, { signal: t.signal }), e.prompts.listChanged && this._client.setNotificationHandler(Qm, (e) => this.sendPromptListChanged(e.params)));
		}
		return super.connect(e);
	}
};
function C_(e, t) {
	return JSON.stringify(e) === JSON.stringify(t);
}
//#endregion
export { S_ as AppBridge, fg as DOWNLOAD_FILE_METHOD, xg as HOST_CONTEXT_CHANGED_METHOD, Tg as INITIALIZED_METHOD, wg as INITIALIZE_METHOD, ug as LATEST_PROTOCOL_VERSION, pg as MESSAGE_METHOD, Yg as McpUiAppCapabilitiesSchema, Og as McpUiDisplayModeSchema, n_ as McpUiDownloadFileRequestSchema, Ng as McpUiDownloadFileResultSchema, Jg as McpUiHostCapabilitiesSchema, s_ as McpUiHostContextChangedNotificationSchema, o_ as McpUiHostContextSchema, Hg as McpUiHostCssSchema, Ug as McpUiHostStylesSchema, l_ as McpUiInitializeRequestSchema, u_ as McpUiInitializeResultSchema, Xg as McpUiInitializedNotificationSchema, r_ as McpUiMessageRequestSchema, Pg as McpUiMessageResultSchema, jg as McpUiOpenLinkRequestSchema, Mg as McpUiOpenLinkResultSchema, Qg as McpUiRequestDisplayModeRequestSchema, $g as McpUiRequestDisplayModeResultSchema, qg as McpUiRequestTeardownNotificationSchema, Ig as McpUiResourceCspSchema, Zg as McpUiResourceMetaSchema, Lg as McpUiResourcePermissionsSchema, Wg as McpUiResourceTeardownRequestSchema, Gg as McpUiResourceTeardownResultSchema, Fg as McpUiSandboxProxyReadyNotificationSchema, i_ as McpUiSandboxResourceReadyNotificationSchema, Rg as McpUiSizeChangedNotificationSchema, Kg as McpUiSupportedContentBlockModalitiesSchema, Dg as McpUiThemeSchema, Vg as McpUiToolCancelledNotificationSchema, zg as McpUiToolInputNotificationSchema, Bg as McpUiToolInputPartialNotificationSchema, t_ as McpUiToolMetaSchema, a_ as McpUiToolResultNotificationSchema, e_ as McpUiToolVisibilitySchema, c_ as McpUiUpdateModelContextRequestSchema, dg as OPEN_LINK_METHOD, d_ as PostMessageTransport, Eg as REQUEST_DISPLAY_MODE_METHOD, Sg as REQUEST_TEARDOWN_METHOD, g_ as RESOURCE_MIME_TYPE, Cg as RESOURCE_TEARDOWN_METHOD, h_ as RESOURCE_URI_META_KEY, mg as SANDBOX_PROXY_READY_METHOD, hg as SANDBOX_RESOURCE_READY_METHOD, gg as SIZE_CHANGED_METHOD, x_ as SUPPORTED_PROTOCOL_VERSIONS, bg as TOOL_CANCELLED_METHOD, _g as TOOL_INPUT_METHOD, vg as TOOL_INPUT_PARTIAL_METHOD, yg as TOOL_RESULT_METHOD, ud as _, np as a, b_ as buildAllowAttribute, Mf as c, Af as d, Df as f, ld as g, __ as getToolUiResourceUri, I as h, sp as i, y_ as isToolVisibilityAppOnly, v_ as isToolVisibilityModelOnly, kf as l, F as m, cp as n, $f as o, P as p, op as r, Nf as s, mp as t, jf as u, od as v };
